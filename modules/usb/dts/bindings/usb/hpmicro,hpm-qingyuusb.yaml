description: HPMicro QingyuUSB Controller (self-implemented stack)

compatible: "hpmicro,hpm-qingyuusb"

include: ["usb-ep.yaml", "pinctrl-device.yaml"]

properties:
  reg:
    required: true

  interrupts:
    required: true

  clk-name:
    type: int
    required: true
    description: USB clock name
