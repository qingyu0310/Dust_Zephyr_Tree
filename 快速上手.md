# zephyr_user 快速上手

> 拿到代码 → 30 分钟跑起来 → 10 分钟加一个业务线程。
> 完整环境搭建见 [doc/zephyr_子树架构搭建指南.md](doc/zephyr_子树架构搭建指南.md) 与 [doc/zephyr_HPM_搭建指南.md](doc/zephyr_HPM_搭建指南.md)。
> 架构总览见 [README.md](README.md)。

---

## 0. 这是什么（30 秒）

Zephyr 子树上的一套电控架构。核心思想：**复杂部分已经封装在架构层，你只写业务线程。**

- `framework/` —— 架构层（drivers / algorithm / modules / topic / cmd / init 六个子模块），**只读**，维护者管
- `project/` —— 业务工程，你在这里写业务线程
- 生命周期是**注册式**的：用 `REGISTER_INIT` / `REGISTER_THREAD` 声明一个线程，启动器自动按阶段执行，不用自己写启动流程

工作区结构：

```text
E:\Zephyr\
├── zephyr\           官方 Zephyr v4.3.0（不动）
├── zephyr-sdk-0.16.8 Zephyr SDK
├── zephyr_user\      本架构
│   ├── framework\    架构层（6 子模块，只读）
│   ├── project\      业务工程（你的战场）
│   ├── boards\ / dts\ / platform\ / doc\
└── projects\<你>      使用者工作区（可选，复制 project 结构）
```

---

## 1. 环境准备（一次性）

命令式清单，照着做即可。详细踩坑见搭建指南：

```powershell
# 工具
winget install Git.Git
winget install astral-sh.uv
uv python install 3.12
winget install Kitware.CMake Ninja-build.Ninja open-source-win.dtc

# venv + west
cd E:\Zephyr
uv venv .venv --python 3.12
.\.venv\Scripts\Activate.ps1
python -m pip install -U pip west

# 官方 zephyr + SDK
git clone --depth 1 --branch v4.3.0 https://github.com/zephyrproject-rtos/zephyr.git zephyr
# SDK 0.16.8 从 GitHub release 下载解压到 E:\Zephyr\zephyr-sdk-0.16.8

# 环境变量（追加到 .venv\Scripts\Activate.ps1 末尾，激活即自动生效）
$env:ZEPHYR_BASE              = "E:\Zephyr\zephyr"
$env:ZEPHYR_SDK_INSTALL_DIR   = "E:\Zephyr\zephyr-sdk-0.16.8"
$env:ZEPHYR_TOOLCHAIN_VARIANT = "zephyr"
```

> HPM 板（hpm5361icb）额外需要 `SDK_GLUE_DIR` 指向 HPM 的 sdk_glue，见 `project/CMakeLists.txt` 顶部逻辑与 HPM 搭建指南。

---

## 2. 拉代码

```powershell
cd E:\Zephyr
git clone git@github-qingyu0310:qingyu0310/Dust_Zephyr_Tree.git zephyr_user
cd zephyr_user
git submodule update --init --recursive     # 拉齐 framework 6 层 + project
```

---

## 3. 编译现有工程

```powershell
cd E:\Zephyr\zephyr_user\project

# HPM 板（当前比赛平台）
dust build hpm5361icb

# STM32 板（等价手动命令）
west build -b stm32f407igh6 -- -DBOARD_CFG=board_rm_c
```

- `dust` 是 `framework/cmd/build/` 下的封装脚本，按板卡配置名反推 BOARD
- `-DBOARD_CFG=xxx` 对应 `project/boards/*/xxx/` 下的 overlay / conf
- **成功标志**：无 FAILED，`Memory region` 表格打印后 `zephyr.elf` 链接完成
- 产物：`build/zephyr/zephyr.elf`

---

## 4. 加一个业务线程（核心）

四步，照抄。以 `xxx` 为例，参考现成的 [trd_test.cpp](project/thread/test/trd_test.cpp)。

### 4.1 写线程文件 `project/thread/xxx/trd_xxx.cpp`

```cpp
#include "thread.hpp"
#include "Init_entry.hpp"

namespace thread::xxx {

static Thread<2048> thread_{};

static void Task(void*, void*, void*)
{
    for (;;)
    {
        // 你的业务循环
        k_msleep(10);
    }
}

bool thread_init()
{
    // 初始化资源；失败返回 false，系统会按 InitLevel 处理
    return true;
}

bool thread_start()
{
    thread_.Start(Task, ThreadPrio::Low);
    return true;
}

REGISTER_INIT  (thread_init,  LateInit,   Low, "xxx_init");
REGISTER_THREAD(thread_start, LateThread, Low, "xxx_start");

} // namespace thread::xxx
```

要点：
- `thread.hpp` 提供 `Thread<StackSize>` 模板，`thread_.Start(Task, ThreadPrio::X)` 即创建线程
- `REGISTER_INIT` / `REGISTER_THREAD` 声明后由启动器自动跑，**不需要自己调**
- 阶段（stage）决定何时执行：`Pre / Early / Mid / Late / App` × `Init / Thread`

### 4.2 注册进 CMake：`project/thread/CMakeLists.txt`

```cmake
if(CONFIG_TRD_XXX)
    target_sources(app PRIVATE ${CMAKE_CURRENT_SOURCE_DIR}/xxx/trd_xxx.cpp)
    target_include_directories(app PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}
        ${CMAKE_CURRENT_SOURCE_DIR}/xxx
    )
endif()
```

### 4.3 加开关：`project/Kconfig`

在 `if PRJ_MAIN` 块内追加：

```kconfig
config TRD_XXX
    bool "xxx thread"
    default y
    help
      Your xxx thread.
```

### 4.4 编译验证

```powershell
dust build hpm5361icb -p      # -p 干净编译
```

启动日志里应出现：

```text
==== thread stage: LateThread ====
xxx_start
```

---

## 5. 常见坑

| 现象 | 原因 | 解决 |
|------|------|------|
| `build directory targets board X` | build 缓存属于另一块板 | 删 `build/` 或加 `-p` |
| 线程没启动 | 阶段不对 / Kconfig 没开 / CMake 没匹配 | 检查 stage 名、`TRD_XXX=y`、CMakeLists 目录名一致 |
| 找不到 `thread.hpp` | include 路径缺失 | 确认 CMakeLists 里有 `${CMAKE_CURRENT_SOURCE_DIR}` 且编译过干净 build |
| `Permission denied` (git) | SSH key 不是 qingyu0310 | 配置 `github-qingyu0310` host，见搭建指南 §5.1 |
| 想要新功能但 framework 没有 | 架构层不允许使用者改 | 向维护者提需求；稳定后由维护者合入 |

---

## 6. 规则（三条）

1. **framework/ 只读**。架构层任何子模块都不要改，有需求走维护者。
2. **业务线程只在 `project/thread/` 写**。每个线程 = 一个 cpp + 一个 Kconfig 开关。
3. **公共模块不复制**。需要架构能力用 `select DUST_XXX`，不要自己复制一份 drivers/modules/init。
