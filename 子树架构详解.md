# zephyr_user 子树架构详解

> 文档版本：当前源码树版
>
> 更新时间：2026-08-01
>
> 适用路径：`E:\Zephyr\zephyr_user`
>
> 文档定位：以当前仓库真实文件、真实注册宏、真实 Kconfig、真实 CMake 和真实设备树为依据，解释这棵用户自研子树为什么能够把硬件、算法、设备模块、线程和业务组合成多个可裁剪产品。

---

## 阅读声明

这不是一个只描述目录名称的 README。

这是一份架构证据文档。

文档中的每一条关键判断，都尽量落到下面几类证据之一：

| 证据类型 | 典型文件 | 说明 |
| --- | --- | --- |
| 源码入口 | `framework/init/main.c` | 证明系统从哪里进入 |
| 构建入口 | `project/CMakeLists.txt` | 证明哪些目录被装配进应用 |
| 配置入口 | `project/Kconfig` | 证明功能如何被选择 |
| 注册宏 | `framework/init/Init_entry.hpp` | 证明组件如何进入注册表 |
| 链接脚本 | `framework/cmd/linker/dust_init.ld` | 证明注册表如何被保留和导出 |
| 设备树 | `project/boards/*/*.overlay` | 证明硬件实例如何命名 |
| 运行实现 | `project/thread/*.cpp` | 证明业务如何接入架构 |
| 数据契约 | `framework/topic/*` | 证明线程之间传递什么 |
| 模块实现 | `framework/modules/*` | 证明设备能力如何被复用 |

这份文档会把“架构能力”和“当前产品接线完成度”分开。

一个注册机制已经存在，不代表当前所有产品都已经使用它。

一个目录已经建立，也不代表所有边界都已经自动验证。

正因为如此，本文既会强调这套设计的厉害之处，也会把当前源码中仍需验证的接线点单独标出来。

---

## 目录

- [第一部分 文档结论](#第一部分-文档结论)
- [第二部分 什么是子树架构](#第二部分-什么是子树架构)
- [第三部分 当前仓库的真实形态](#第三部分-当前仓库的真实形态)
- [第四部分 维护者工作区与使用者工作区](#第四部分-维护者工作区与使用者工作区)
- [第五部分 四张架构地图](#第五部分-四张架构地图)
- [第六部分 顶层目录与责任边界](#第六部分-顶层目录与责任边界)
- [第七部分 framework 六个独立子树](#第七部分-framework-六个独立子树)
- [第八部分 project 业务装配子树](#第八部分-project-业务装配子树)
- [第九部分 platform 平台兼容子树](#第九部分-platform-平台兼容子树)
- [第十部分 boards 与 dts 用户扩展子树](#第十部分-boards-与-dts-用户扩展子树)
- [第十一部分 构建系统装配链](#第十一部分-构建系统装配链)
- [第十二部分 Kconfig 配置门禁](#第十二部分-kconfig-配置门禁)
- [第十三部分 CMake 源码装配](#第十三部分-cmake-源码装配)
- [第十四部分 设备树硬件契约](#第十四部分-设备树硬件契约)
- [第十五部分 init 启动子树](#第十五部分-init-启动子树)
- [第十六部分 链接段注册系统](#第十六部分-链接段注册系统)
- [第十七部分 当前启动阶段](#第十七部分-当前启动阶段)
- [第十八部分 注册表为什么强大](#第十八部分-注册表为什么强大)
- [第十九部分 当前架构的关键优势](#第十九部分-当前架构的关键优势)
- [第二十部分 驱动子树](#第二十部分-驱动子树)
- [第二十一部分 Stream 通信抽象](#第二十一部分-stream-通信抽象)
- [第二十二部分 UART DMA 子树](#第二十二部分-uart-dma-子树)
- [第二十三部分 CAN 子树](#第二十三部分-can-子树)
- [第二十四部分 SPI GPIO PWM 子树](#第二十四部分-spi-gpio-pwm-子树)
- [第二十五部分 USB 分层子树](#第二十五部分-usb-分层子树)
- [第二十六部分 algorithm 算法子树](#第二十六部分-algorithm-算法子树)
- [第二十七部分 modules 设备模块子树](#第二十七部分-modules-设备模块子树)
- [第二十八部分 IMU 组合子树](#第二十八部分-imu-组合子树)
- [第二十九部分 Remote 协议子树](#第二十九部分-remote-协议子树)
- [第三十部分 电机与功率计子树](#第三十部分-电机与功率计子树)
- [第三十一部分 topic 数据契约子树](#第三十一部分-topic-数据契约子树)
- [第三十二部分 cmd 横切能力子树](#第三十二部分-cmd-横切能力子树)
- [第三十三部分 project/thread 业务线程子树](#第三十三部分-projectthread-业务线程子树)
- [第三十四部分 端到端数据流](#第三十四部分-端到端数据流)
- [第三十五部分 运行时顺序与开发顺序](#第三十五部分-运行时顺序与开发顺序)
- [第三十六部分 如何新增一个子树叶子](#第三十六部分-如何新增一个子树叶子)
- [第三十七部分 如何新增一块板卡](#第三十七部分-如何新增一块板卡)
- [第三十八部分 如何新增一个驱动](#第三十八部分-如何新增一个驱动)
- [第三十九部分 如何新增一个设备模块](#第三十九部分-如何新增一个设备模块)
- [第四十部分 如何新增一个业务线程](#第四十部分-如何新增一个业务线程)
- [第四十一部分 如何新增一个协议插件](#第四十一部分-如何新增一个协议插件)
- [第四十二部分 如何新增一个算法](#第四十二部分-如何新增一个算法)
- [第四十三部分 如何新增一个 topic](#第四十三部分-如何新增一个-topic)
- [第四十四部分 如何新增一个 cmd 能力](#第四十四部分-如何新增一个-cmd-能力)
- [第四十五部分 启动问题证据链](#第四十五部分-启动问题证据链)
- [第四十六部分 数据不通问题证据链](#第四十六部分-数据不通问题证据链)
- [第四十七部分 当前源码中的接线状态](#第四十七部分-当前源码中的接线状态)
- [第四十八部分 维护边界与演进方向](#第四十八部分-维护边界与演进方向)
- [附录 A 关键文件索引](#附录-a-关键文件索引)
- [附录 B 符号、配置与链接段映射](#附录-b-符号配置与链接段映射)
- [附录 C 架构检查清单](#附录-c-架构检查清单)

---

# 第一部分 文档结论

## 1.1 一句话结论

`zephyr_user` 的厉害之处，不是目录多。

它真正厉害的地方是：

```text
一个功能只需要在自己的叶子节点声明一次，
配置系统决定它是否存在，
构建系统决定它是否进入镜像，
链接器决定它如何进入注册表，
启动器决定它何时运行，
上层只依赖稳定接口。
```

这是一套把“软件组织问题”拆成多个编译期和运行期边界的系统。

它不是把所有东西集中到一个 `main()`。

它也不是把所有硬件都塞进一个巨大的 `project`。

它更接近一种面向嵌入式产品线的组合式架构。

---

## 1.2 这棵树解决了什么问题

传统机器人固件经常会出现下面的形态：

```text
main.cpp
    -> 初始化 GPIO
    -> 初始化 UART
    -> 初始化 CAN
    -> 初始化 IMU
    -> 创建线程
    -> 创建另一个线程
    -> 注册回调
    -> 创建调试命令
    -> 创建第三方协议
```

随着项目变大，中心入口会出现四种膨胀：

| 膨胀类型 | 具体表现 |
| --- | --- |
| 设备膨胀 | `main()` 认识每一个设备 |
| 线程膨胀 | `main()` 认识每一个线程 |
| 配置膨胀 | `main()` 到处出现 `#ifdef` |
| 业务膨胀 | 不同机器人策略互相污染 |

当前子树架构把这些膨胀拆开：

```text
硬件差异       -> boards/ + dts/ + SDK glue
底层访问       -> framework/drivers
设备能力       -> framework/modules
纯计算         -> framework/algorithm
数据契约       -> framework/topic
启动遍历       -> framework/init
辅助横切能力   -> framework/cmd
业务组合       -> project/thread
```

每一层都知道自己应该知道的事情。

每一层也有明确的“不应该知道”。

---

## 1.3 当前架构的五个核心能力

### 能力一：编译期裁剪

没有打开的功能不会进入对应的 C++ 编译单元。

没有进入编译单元的功能不会产生注册项。

没有注册项的功能不会被启动器遍历。

这条链把“功能关闭”变成了真正的固件裁剪，而不是运行时 `if (false)`。

### 能力二：本地注册

组件在自己拥有的 `.cpp` 里注册自己。

新增组件不需要修改中心数组。

新增协议不需要修改中心分派器。

新增 IMU 不需要修改 `ImuManager` 的设备选择代码。

### 能力三：阶段化启动

初始化和线程启动被拆成不同阶段。

资源初始化与线程循环分离。

失败策略通过 `InitLevel` 显式声明。

启动器不需要了解每个业务对象的具体实现。

### 能力四：硬件适配隔离

设备树描述硬件连接。

驱动封装 Zephyr API。

模块封装设备协议。

项目线程组合多个模块。

更换板卡时，很多业务源码可以保持不动。

### 能力五：数据流可解释

遥控器不是直接控制电机。

IMU 不是直接修改云台变量。

底盘不是直接读取 UART 字节。

数据经过明确的 topic、快照、队列或接口。

这使得实时性、所有权和故障定位都有落点。

---

## 1.4 最重要的阅读方式

阅读这棵树时，不要只按目录从上到下看。

应当同时问四个问题：

```text
这个文件属于哪一棵树？
这个功能由谁决定是否存在？
这个对象如何被运行时找到？
这个数据最后由谁消费？
```

例如 `project/thread/remote/trd_remote.cpp`：

```text
源码树位置：project/thread/remote
配置门禁：TRD_REMOTE
依赖能力：DUST_MOD_DEV_REMOTE + DUST_COM_UART_DMA
硬件来源：DT_ALIAS(remote_uart)
运行注册：REGISTER_INIT + REGISTER_THREAD
数据出口：Remote -> pub_remote_to
```

一个文件因此同时连接了：

```text
Kconfig
    +
CMake
    +
DeviceTree
    +
init section
    +
topic
    +
线程运行时
```

这就是“子树”比普通目录划分更有力量的地方。

---

# 第二部分 什么是子树架构

## 2.1 子树不是简单的文件夹

普通文件夹只回答：

```text
文件放在哪里？
```

一个完整子树必须回答：

```text
谁拥有它？
如何启用它？
如何编译它？
如何初始化它？
如何被调用？
如何被替换？
如何验证它？
```

所以本文把一个可复用子树定义为：

```text
目录边界
    +
Kconfig 边界
    +
CMake 边界
    +
头文件接口边界
    +
运行时生命周期边界
    +
测试与诊断边界
```

缺少其中任意一项，都可能只是目录分组，而不是完整架构节点。

---

## 2.2 子树的五种节点

当前工程中的节点可以分成五类。

| 节点类型 | 例子 | 核心问题 |
| --- | --- | --- |
| 根节点 | `zephyr_user/` | 这棵用户树如何接入 Zephyr |
| 层节点 | `framework/drivers/` | 这一层负责什么 |
| 组件节点 | `modules/imu/` | 一个设备能力如何组合 |
| 叶子节点 | `modules/remotes/dr16/dr16.cpp` | 一个具体实现如何注册 |
| 装配节点 | `project/thread/chassis/` | 当前产品如何组合能力 |

根节点不应该知道每个协议细节。

层节点不应该偷走其他层的职责。

组件节点不应该把所有项目策略写进去。

叶子节点应该尽可能自描述。

装配节点才决定当前机器人到底使用哪些能力。

---

## 2.3 子树的边界不是越细越好

拆分的目的不是追求目录数量。

合理拆分的标准是变化方向。

如果两段代码总是一起变化，它们可能属于同一个子树。

如果两段代码变化原因不同，它们应该尽量分开。

当前工程中：

```text
SPI API 与 IMU 寄存器协议
    变化原因不同
    -> 分成 drivers/communication/spi 与 modules/imu/devices
```

```text
电机反馈解析与底盘运动学
    变化原因不同
    -> 分成 modules/motors 与 project/thread/chassis
```

```text
遥控器原始帧与底盘控制命令
    变化原因不同
    -> 分成 remotes、topic、chassis thread
```

```text
启动遍历与具体线程逻辑
    变化原因不同
    -> 分成 init 与 project/thread
```

这不是为了形式好看。

这是为了让未来的改动只穿过必要的边界。

---

## 2.4 叶子节点的最小契约

一个新的可选架构能力，至少应考虑以下文件：

```text
<layer>/Kconfig
<layer>/CMakeLists.txt
<layer>/<component>/include or public header
<layer>/<component>/implementation
<layer>/<component>/README.md or architecture note
```

如果它需要设备树：

```text
boards/<vendor>/<board>/*
project/boards/<vendor>/<board_cfg>/*
dts/bindings/*
```

如果它需要启动：

```text
REGISTER_INIT()
REGISTER_THREAD()
```

如果它需要运行时发现：

```text
专用 section
链接脚本 KEEP
start/end 符号
消费者遍历
```

如果它需要线程间通信：

```text
topic/<name>/*
```

一个子树只有把这些事情连起来，才真正成为可装配能力。

---

## 2.5 当前工程的中心思想

当前工程不是“中心控制一切”。

当前工程更接近：

```text
中心只定义协议和框架。
叶子负责声明自己。
配置决定叶子是否存在。
链接器负责把叶子收集起来。
消费者负责遍历收集结果。
```

这个思想在多个地方重复出现：

| 机制 | 本地声明 | 运行时消费者 |
| --- | --- | --- |
| 初始化 | `REGISTER_INIT` | `RunStage` |
| 线程启动 | `REGISTER_THREAD` | `RunStage(ThreadStage)` |
| CAN 分发 | `CAN_RX_HANDLER` | `user_canN_rx_callback` |
| 遥控协议 | `REGISTER_REMOTE` | `Remote::HandleDetecting` |
| IMU 数据源 | `REGISTER_IMU` | `ImuManager::InitSource` |
| 调试变量 | `REGISTER_SHELL_VAR` | `DbgConsole::FindVar` |

同一个架构思想覆盖多个不同领域。

这说明它不是某个单独宏的技巧。

它已经形成了统一的组织语言。

---

# 第三部分 当前仓库的真实形态

## 3.1 当前根目录

当前仓库根目录是：

```text
E:\Zephyr\zephyr_user
```

当前顶层目录和文件为：

```text
zephyr_user/
├── .git/
├── .gitignore
├── .gitmodules
├── README.md
├── boards/
├── dts/
├── framework/
├── platform/
├── project/
└── 子树架构详解.md
```

这棵树有一个很关键的事实：

```text
根目录没有 CMakeLists.txt。
```

当前应用构建入口是：

```text
project/CMakeLists.txt
```

因此，不能把根目录描述成可以直接执行 `west build` 的普通 Zephyr 应用根。

根目录是用户自研资源根。

`project/` 才是当前维护者业务应用根。

---

## 3.2 当前根目录的四种资源

根目录中的资源有四种不同性质：

| 资源 | 当前路径 | 作用 |
| --- | --- | --- |
| 用户板卡 | `boards/` | 给 Zephyr 提供额外 board |
| 用户 binding | `dts/bindings/` | 给设备树提供额外 compatible |
| 架构模块 | `framework/` | 提供可复用软件能力 |
| 业务应用 | `project/` | 选择板卡和线程并生成镜像 |

`platform/` 介于资源根和架构模块之间。

它当前提供 CMSIS 兼容模块。

它不等同于完整的 SoC 适配仓库。

---

## 3.3 当前 framework 形态

`framework/` 当前通过 `.gitmodules` 挂载六个独立子仓库：

```text
framework/
├── .gitmodules
├── algorithm/
├── cmd/
├── drivers/
├── init/
├── modules/
├── topic/
└── zephyr/module.yml
```

根 `.gitmodules` 中的六个路径分别是：

| 路径 | 独立仓库 |
| --- | --- |
| `framework/drivers` | `Dust_Zephyr_Architecture_Drivers.git` |
| `framework/algorithm` | `Dust_Zephyr_Architecture_Algorithm.git` |
| `framework/modules` | `Dust_Zephyr_Architecture_Modules.git` |
| `framework/topic` | `Dust_Zephyr_Architecture_Topic.git` |
| `framework/cmd` | `Dust_Zephyr_Architecture_Cmd.git` |
| `framework/init` | `Dust_Zephyr_Architecture_Init.git` |

这意味着：

```text
zephyr_user 是组合仓库。
framework 下的六个层是独立演进单元。
project 是当前应用装配单元。
```

这比把所有架构代码放进一个仓库更适合长期复用。

---

## 3.4 当前 submodule 状态的意义

每个 framework 子树都有自己的提交边界。

根仓库记录的是子模块指针。

因此，架构升级可以有两个层次：

```text
层内开发
    -> 在对应 framework 子仓库提交

组合升级
    -> 在 zephyr_user 更新 submodule 指针
```

这让一个应用仓库可以固定一组已经验证过的架构版本。

它也让多个应用仓库能够共享同一套层实现。

这是一种非常适合嵌入式团队的版本隔离方式。

---

## 3.5 当前 project 形态

`project/` 是当前维护者业务项目：

```text
project/
├── .clangd
├── CMakeLists.txt
├── Kconfig
├── prj.conf
├── boards/
└── thread/
```

它拥有：

```text
板级配置选择
业务功能门禁
业务线程源码
业务对象实例
当前机器人控制策略
```

它不拥有：

```text
统一启动器实现
公共驱动实现
通用算法实现
通用设备协议实现
公共 topic 定义
```

这就是 `project/` 与 `framework/` 的核心边界。

---

## 3.6 当前 platform 形态

当前平台子树为：

```text
platform/
└── cmsis/
    ├── Kconfig
    ├── cmsis_core.h
    └── zephyr/module.yml
```

它的核心作用是：

```text
为 Cortex-M 架构提供 CMSIS core 头文件兼容入口。
```

`cmsis_core.h` 根据：

```c
CONFIG_CPU_CORTEX_M
```

选择：

```c
#include <soc.h>
#include <core_cm4.h>
```

它还提供：

```c
#define SHPR SHP
```

用于兼容 Zephyr v4.3 与 CMSIS 新旧命名差异。

这是一种典型的“薄平台层”。

它不试图替代 Zephyr 的完整架构。

它只修复当前用户树需要的兼容缺口。

---

## 3.7 当前 board 资源

根 `boards/` 当前包含：

```text
boards/st/stm32f407igh6/
```

该板卡描述：

```text
型号：STM32F407IGH6
封装：176-pin BGA
架构：ARM Cortex-M
RAM：128 KB
Flash：1024 KB
```

板卡目录包含：

```text
board.yml
stm32f407igh6.yaml
stm32f407igh6.dts
stm32f407igh6_defconfig
stm32f407igh6-pinctrl.dtsi
Kconfig.stm32f407igh6
board.cmake
support/openocd.cfg
README.md
```

这说明根 `boards/` 是 Zephyr 官方 board tree 的用户扩展。

它不是项目线程目录。

它也不是业务配置目录。

---

## 3.8 当前 dts binding 资源

根 `dts/bindings/` 当前包含：

```text
dts/bindings/mtd/winbond,w25q128.yaml
```

这个 binding 声明：

```yaml
compatible: "winbond,w25q128"
on-bus: spi
include: [spi-device.yaml]
```

它的意义是让设备树可以描述：

```dts
w25q128: w25q128@0 {
    compatible = "winbond,w25q128";
    reg = <0>;
    spi-max-frequency = <50000000>;
};
```

然后 C++ 代码可以使用：

```cpp
SPI_DT_SPEC_GET(DT_ALIAS(flash_spi), ...);
```

这条链把：

```text
硬件型号
    -> DTS compatible
    -> alias
    -> C++ 设备规格
    -> W25Q128 模块
```

完整地连了起来。

---

# 第四部分 维护者工作区与使用者工作区

## 4.1 这一部分为什么必须放在架构主线

`zephyr_user` 不只是一个普通应用目录。

它同时承担两种身份：

```text
第一身份：可被多个项目复用的基础子树
第二身份：维护者或电控组组长的工作区
```

这两个身份叠加在一起，才解释了为什么要花很大力气重构：

```text
framework
platform
boards
dts
project
thread
init
drivers
modules
algorithm
topic
```

如果所有成员都在同一个目录里随意修改所有层，短期看起来很快。

长期一定会出现：

```text
公共驱动被业务临时需求污染
一个人的修复影响所有产品
不同成员各自复制一套模块
板卡和线程修改混进基础框架
同一个功能出现多个不兼容版本
```

因此当前架构真正要建立的，不只是代码分层。

它要建立的是：

```text
维护者拥有父级能力树
使用者拥有子级应用工作区
使用者通过 CMake 消费父级能力
公共模块由维护者集中演进
用户变化集中在板卡和 thread
```

这是一种工程组织架构。

它把“谁负责什么”也纳入了软件边界。

## 4.2 两类工作区

当前设计应该明确区分两类工作区。

### 维护者工作区

维护者工作区就是：

```text
E:\Zephyr\zephyr_user
```

维护者或电控组组长在这里负责：

```text
公共驱动
公共算法
设备模块
topic 契约
启动注册
链接段
平台兼容
公共板卡支持
构建工具
架构文档
```

维护者工作区的目标不是承载某一个人的临时应用。

它的目标是：

```text
把一次实现沉淀成多个用户工作区都能使用的能力。
```

### 使用者工作区

普通使用者或电控成员的工作区位于 `zephyr_user` 同级目录。

例如：

```text
E:\Zephyr\
├── zephyr_user\
├── robot_a_user\
├── robot_b_user\
├── test_board_user\
└── experiment_user\
```

这些目录是不同项目、不同成员或不同产品的应用工作区。

它们通过 CMake 引入：

```text
E:\Zephyr\zephyr_user\framework
E:\Zephyr\zephyr_user\platform
```

而不是复制一份：

```text
drivers
algorithm
modules
topic
init
```

## 4.3 预期的父子目录关系

理想的目录关系是：

```text
E:\Zephyr\
├── zephyr_user\                 <- 父级子树和维护者工作区
│   ├── framework\
│   ├── platform\
│   ├── boards\
│   ├── dts\
│   ├── framework\cmd\
│   └── 子树架构详解.md
│
├── robot_a_user\                <- 普通使用者工作区
│   ├── CMakeLists.txt
│   ├── prj.conf
│   ├── Kconfig
│   ├── boards\
│   ├── thread\
│   └── app-specific files
│
├── robot_b_user\
│   ├── CMakeLists.txt
│   ├── prj.conf
│   ├── boards\
│   └── thread\
│
└── experiment_user\
    ├── CMakeLists.txt
    ├── boards\
    └── thread\
```

上面最重要的不是目录名称。

最重要的是依赖方向：

```text
robot_a_user
    -> zephyr_user

robot_b_user
    -> zephyr_user

experiment_user
    -> zephyr_user
```

而不是：

```text
robot_a_user
    <-> robot_b_user
    <-> framework 内部任意目录
```

## 4.4 父级子树提供什么

父级 `zephyr_user` 向下提供稳定能力：

```text
驱动 API
算法 API
设备模块 API
topic 消息结构
启动阶段
注册宏
链接脚本
通用命令
公共板级资源
构建入口模式
```

这些能力的共同特点是：

```text
多个项目都可能需要
不应该绑定单一机器
需要统一维护
需要统一修复
需要经过评审后发布
```

父级子树不是为了限制创新。

它是为了让创新发生在正确的层。

普通用户可以在自己的工作区创新：

```text
新增 thread
组合设备模块
选择控制参数
适配自己的板卡
增加产品专用模式
```

如果这项能力具有多个项目的复用价值，再由维护者把它提升到：

```text
framework/modules
framework/algorithm
framework/drivers
framework/topic
```

## 4.5 使用者工作区的最小内容

普通使用者真正必要的内容并不多。

最小工作区通常只需要：

```text
CMakeLists.txt
prj.conf
Kconfig
boards/
thread/
```

其中：

```text
boards/
    -> 描述本项目使用的板卡、overlay、conf、runner

thread/
    -> 描述本项目如何组合公共模块

prj.conf
    -> 选择当前产品需要的能力

Kconfig
    -> 声明当前项目的业务开关

CMakeLists.txt
    -> 引入 zephyr_user 和当前应用源文件
```

使用者不需要把下面这些目录复制进自己的工作区：

```text
framework/drivers
framework/algorithm
framework/modules
framework/topic
framework/init
framework/cmd
platform/cmsis
```

这些目录由父级子树提供。

## 4.6 使用者工作区的典型结构

一个产品用户工作区可以写成：

```text
robot_a_user/
├── CMakeLists.txt
├── Kconfig
├── prj.conf
├── boards/
│   └── hpm/
│       └── robot_a/
│           ├── robot_a.overlay
│           ├── robot_a.conf
│           └── board.cmake
├── thread/
│   ├── trd_chassis.cpp
│   ├── trd_gimbal.cpp
│   ├── trd_remote.cpp
│   └── trd_can_tx.cpp
└── README.md
```

这里的 `thread/` 不意味着线程必须自己实现所有底层功能。

它只负责：

```text
选择使用哪些模块
决定线程优先级
定义产品周期
连接 topic
连接执行器
实现产品策略
```

## 4.7 用户工作区通过 CMake 引入父级子树

使用者工作区的核心动作是：

```cmake
set(ZEPHYR_USER_DIR
    "E:/Zephyr/zephyr_user"
)
```

然后把父级能力加入 Zephyr 的模块或源码装配路径：

```cmake
list(APPEND ZEPHYR_EXTRA_MODULES
    "${ZEPHYR_USER_DIR}/framework"
    "${ZEPHYR_USER_DIR}/platform/cmsis"
)
```

具体变量名称和装配方式应以当前工程的 CMake 入口为准。

当前仓库的真实入口是：

```text
project/CMakeLists.txt
```

它已经体现了以下思想：

```text
设置 ZEPHYR_EXTRA_MODULES
设置 BOARD_ROOT
设置 DTS_ROOT
设置 SOC_ROOT
显式 add_subdirectory framework 子树
显式 add_subdirectory project/thread
```

因此同级用户工作区的合理演进方向不是复制整个 `project/`。

而是：

```text
保留父级子树的模块接入逻辑
把用户自己的 board 和 thread 作为应用输入
让用户 CMake 成为当前产品的装配点
```

## 4.8 CMake 引入关系

完整的父子装配关系可以画成：

```text
robot_a_user/CMakeLists.txt
    -> find_package(Zephyr)
        -> 引入 zephyr_user/framework
            -> framework/zephyr/module.yml
            -> framework/drivers
            -> framework/algorithm
            -> framework/modules
            -> framework/topic
            -> framework/cmd
            -> framework/init
        -> 引入 zephyr_user/platform/cmsis
        -> 引入 robot_a_user/boards
        -> 引入 robot_a_user/thread
            -> app ELF
```

构建时真正发生的是：

```text
用户只拥有应用源文件
父级提供能力源文件
CMake 组合两者
Kconfig 决定哪些能力打开
DeviceTree 决定当前硬件实例
链接器生成最终注册表
```

这就是“父级能力，子级装配”。

## 4.9 用户工作区可以自由放置，但不能自由复制边界

“同级文件夹中随意放置”表达的是：

```text
用户可以按项目、成员、实验或产品自由组织自己的应用目录。
```

它不表达：

```text
用户可以随意复制或改写父级模块。
```

允许自由变化的是：

```text
工作区位置
项目名称
板卡配置
thread 组合
产品参数
产品专用命令
```

需要集中治理的是：

```text
公共驱动
公共算法
公共设备模块
公共 topic
启动注册器
链接段
公共 HAL 适配
```

这样可以同时获得：

```text
应用层灵活
基础层稳定
产品之间可复用
问题可以集中修复
```

## 4.10 用户工作区只保留 board 和 thread 的原因

板卡和线程正好对应普通使用者最常见的两类变化。

### 板卡变化

不同产品可能使用：

```text
不同 MCU
不同 CAN 控制器
不同 UART
不同 SPI
不同 IMU
不同 PWM 引脚
不同调试串口
```

这些变化应该优先落在：

```text
overlay
conf
board.cmake
board-specific DTS
```

而不是复制一份公共驱动。

### 线程变化

不同产品可能拥有不同的：

```text
控制周期
线程优先级
底盘策略
云台策略
测试任务
安全降级
```

这些变化应该落在：

```text
thread/
```

而不是修改 Remote、IMU 或电机模块的公共实现。

## 4.11 父子关系下的修改矩阵

可以用下面的矩阵描述职责：

| 内容 | 普通使用者 | 维护者/组长 | 典型位置 |
| --- | --- | --- | --- |
| 选择配置 | 可以 | 可以制定默认值 | `prj.conf` |
| 产品专用 Kconfig | 可以 | 评审公共化 | 用户 `Kconfig` |
| 板卡 overlay | 可以 | 维护公共板卡 | 用户 `boards/` |
| 产品 thread | 可以 | 提供模板和规范 | 用户 `thread/` |
| 公共驱动 | 不应直接改 | 负责维护 | `framework/drivers` |
| 公共算法 | 不应直接改 | 负责维护 | `framework/algorithm` |
| 公共设备模块 | 不应直接改 | 负责维护 | `framework/modules` |
| topic 契约 | 提交需求，评审修改 | 负责兼容性 | `framework/topic` |
| init/链接段 | 不应随意改 | 架构级维护 | `framework/init`、`framework/cmd` |
| 平台兼容层 | 不应随意改 | 负责平台演进 | `platform/` |
| 发布版本 | 使用固定版本 | 管理版本指针 | submodule/git |

“不应直接改”不等于不允许提出改动。

它表示：

```text
改动应进入维护者工作区
经过代码审查
形成公共版本
再由用户工作区消费
```

## 4.12 普通用户不能随意修改子模块的真实实现

需要把两个概念分开：

```text
CMake 引入关系
权限控制关系
```

CMake 可以做到：

```text
让用户使用父级模块
让用户不需要复制父级代码
让应用构建依赖一个固定路径
```

CMake 本身不能做到：

```text
阻止有文件系统写权限的人修改文件
阻止用户临时改本地 submodule
阻止用户绕过构建系统复制代码
```

真正要实现“普通用户不能随意修改子模块”，还需要：

```text
Git 仓库权限
protected branch
代码审查
版本发布权限
工作区文件系统 ACL
CI 检查
```

合理的组合是：

```text
架构上不把 framework 放进用户工作区
Git 上由维护者管理 framework 仓库
CMake 上由用户只读消费 framework
流程上由维护者合并公共修改
```

这才是可靠的父子关系。

## 4.13 submodule 在父子关系中的作用

当前 `framework/` 下已经挂载多个独立 submodule：

```text
drivers
algorithm
modules
topic
cmd
init
```

submodule 让父级仓库可以固定：

```text
每个公共模块的提交
不同模块之间的组合版本
用户工作区正在使用的发布状态
```

用户工作区只需要依赖：

```text
zephyr_user 的某个发布提交
```

而不需要自行选择：

```text
drivers 的任意分支
algorithm 的任意分支
modules 的任意本地修改
```

这可以避免“每个人都认为自己的版本才是正确版本”。

## 4.14 公共模块的发布流程

推荐流程是：

```text
1. 用户在工作区发现公共能力不足
2. 用户提交问题、复现路径或改进需求
3. 维护者在 zephyr_user/framework 内实现
4. 维护者补充测试、文档或验证记录
5. 公共 submodule 形成新提交
6. zephyr_user 更新 gitlink
7. 维护者发布一个可追踪的父级版本
8. 用户工作区更新父级版本
9. 用户只修改自己的 board/thread
```

这个流程把：

```text
需求发现
公共实现
版本发布
产品接线
```

分成了四个阶段。

## 4.15 为什么不能让用户直接修改公共模块

如果每个用户都直接修改：

```text
framework/modules/imu
framework/modules/remotes
framework/drivers/communication
```

最终会出现：

```text
同一个模块多个分叉
修复无法回收
接口无法稳定
文档无法对应
板卡问题和模块问题混在一起
```

更严重的是，用户可能只是为了让自己的板卡启动，而修改了公共生命周期。

这种修改可能会影响：

```text
其他板卡的启动阶段
其他产品的协议探测
其他线程的队列时序
其他用户的控制闭环
```

把公共模块收回维护者工作区，是对系统稳定性的保护。

## 4.16 “父级强、子级轻”的含义

父级强，不是说父级什么都替用户决定。

父级强表示：

```text
公共接口稳定
模块生命周期清楚
配置门禁完整
注册机制统一
错误策略明确
版本可追踪
```

子级轻表示：

```text
用户不需要理解所有底层实现
用户不需要复制所有驱动
用户只组合自己需要的功能
用户可以快速创建新产品
```

理想关系是：

```text
父级提供复杂度
子级提供产品语义
```

这正是当前架构重构投入的回报。

## 4.17 当前仓库与目标使用形态

当前仓库内已经存在：

```text
project/
```

它是一个真实的应用装配和架构验证工作区。

它包含：

```text
project/CMakeLists.txt
project/Kconfig
project/boards
project/thread
```

因此当前仓库可以直接验证：

```text
framework 是否能够被装配
板卡 alias 是否有效
thread 是否能够注册
启动阶段是否能够运行
```

但这不意味着所有用户都必须把自己的工作区放进：

```text
zephyr_user/project/
```

面向团队协作的目标形态是：

```text
zephyr_user/
    -> 父级公共子树和维护者验证工作区

robot_a_user/
    -> 产品 A 的 board 和 thread

robot_b_user/
    -> 产品 B 的 board 和 thread
```

当前 `project/` 可以视为：

```text
参考应用
集成测试工作区
维护者验证样例
```

同级用户工作区则是：

```text
真正的产品应用边界
```

## 4.18 维护者工作区的责任

维护者不仅是写公共代码的人。

还需要维护：

```text
模块边界
注册协议
版本兼容
板卡支持矩阵
构建入口
文档和示例
迁移说明
故障证据链
```

维护者每增加一个公共子模块，都应该回答：

```text
这个能力是否至少被两个工作区需要？
接口是否比当前业务代码更稳定？
是否有明确的生命周期？
是否有 Kconfig/CMake 门禁？
是否有板卡无关的核心实现？
是否能被用户 thread 组合？
```

只有回答清楚，才值得提升为公共子树。

## 4.19 使用者工作区的责任

普通使用者也不是只改两个文件就结束。

使用者需要负责：

```text
自己的板卡 overlay 正确
自己的 board conf 正确
自己的 thread 生命周期正确
自己的线程优先级合理
自己的 topic 使用符合契约
自己的产品故障处理清楚
```

使用者不需要负责：

```text
重写公共 UART DMA
重写公共 CAN dispatcher
重写 IMU source 管理
重写链接段遍历
复制一个新 Remote 状态机
```

这使成员的学习重点从“到处修底层”转向：

```text
理解接口
正确接线
正确验证
反馈公共缺口
```

## 4.20 这个父子关系带来的实际收益

当有多个电控成员并行工作时，父子关系可以带来：

```text
每个人都有独立工作区
每个项目可以独立构建
公共模块只维护一份
板卡差异不污染公共驱动
线程策略不污染设备协议
修复可以集中发布
版本可以回滚
```

架构图可以简化成：

```text
维护者子树
    ├── 公共驱动
    ├── 公共算法
    ├── 公共模块
    ├── 公共 topic
    ├── 公共 init
    └── 公共构建能力
          │
          ├── CMake 引入
          │
          ├── 用户工作区 A
          │     ├── board
          │     └── thread
          │
          ├── 用户工作区 B
          │     ├── board
          │     └── thread
          │
          └── 用户工作区 C
                ├── board
                └── thread
```

这不是简单的目录树。

它是一棵组织权限、代码复用和产品变化的树。

## 4.21 为什么这正是重构子树的原因

如果没有公共子树：

```text
每个成员都从零开始
每个项目都各自实现驱动
每个板卡都各自复制线程
错误不能集中修复
经验无法沉淀
```

如果只有公共代码，没有用户工作区边界：

```text
用户会直接改公共模块
临时需求会进入框架
公共模块变成某个产品的私有实现
维护者无法判断接口是否稳定
```

因此必须同时建立：

```text
强父级子树
轻量用户工作区
明确 CMake 引入
明确 board/thread 边界
明确公共修改流程
```

这就是为什么当前架构值得投入大量时间重写。

## 4.22 本部分结论

`zephyr_user` 的最终定位可以写成：

```text
维护者拥有的公共能力树
电控组长维护的产品基础设施
多个用户工作区的父级依赖
由 CMake 对外提供能力
由 Git/权限/评审保护公共模块
```

普通用户的最小动作是：

```text
准备自己的 board
准备自己的 thread
编写自己的 CMake
引入 zephyr_user
选择需要的 Kconfig
验证自己的产品
```

而不是：

```text
复制 framework
修改公共驱动
重写启动器
复制链接段
把所有内容塞回自己的工程
```

这条边界是整个“子树架构”能够长期运行的组织基础。

# 第五部分 四张架构地图

## 5.1 为什么需要四张地图

仅看源码目录，会漏掉运行时注册。

仅看 CMake，会漏掉设备树。

仅看设备树，会漏掉线程和 topic。

仅看启动日志，会漏掉编译期裁剪。

因此当前工程需要同时理解四张地图：

```text
地图一：源码子树
地图二：构建子树
地图三：链接注册树
地图四：运行时数据流树
```

---

## 5.2 地图一：源码子树

源码地图回答：

```text
代码按什么职责分组？
```

当前主干为：

```text
zephyr_user
├── framework
│   ├── drivers
│   ├── algorithm
│   ├── modules
│   ├── topic
│   ├── cmd
│   └── init
├── platform
├── boards
├── dts
└── project
```

源码地图体现的是责任边界。

它不直接表示构建顺序。

它也不直接表示线程启动顺序。

---

## 5.3 地图二：构建子树

构建地图回答：

```text
哪些配置会让哪些源文件进入 app？
```

当前构建主链为：

```text
project/prj.conf
    -> project/Kconfig
        -> framework/*/Kconfig
            -> CONFIG_*
                -> framework/*/CMakeLists.txt
                    -> target_sources(app ...)
                        -> app 镜像
```

构建地图是编译期地图。

它决定功能是否存在。

---

## 5.4 地图三：链接注册树

链接地图回答：

```text
已经编译进来的功能，如何被运行时自动找到？
```

当前注册主链为：

```text
REGISTER_INIT
    -> __section__(".user_init")
        -> dust_init.ld KEEP
            -> __user_init_start/end
                -> RunStage
```

其他注册链包括：

```text
REGISTER_THREAD -> .user_thread
REGISTER_REMOTE -> .remote
REGISTER_IMU    -> .imu
REGISTER_SHELL_VAR -> .shell_var
CAN_RX_HANDLER  -> .can_rx1/2/3
```

链接地图是编译器与链接器共同完成的组合地图。

它消灭了大量中央数组。

---

## 5.5 地图四：运行时数据流树

运行时地图回答：

```text
数据从哪里来？
在哪个上下文处理？
经过什么契约？
最终由谁消费？
```

一个典型控制数据流是：

```text
遥控器 UART
    -> UartDma
        -> Remote 协议状态机
            -> topic::remote_to::Message
                -> chassis thread
                    -> PID
                        -> PowerCtrl
                            -> CAN TX message queue
                                -> CAN thread
                                    -> Can
                                        -> CAN 控制器
```

这个数据流同时穿过：

```text
drivers
modules
topic
algorithm
project/thread
```

但每一层只处理自己的问题。

---

## 5.6 四张地图叠加后的价值

当一个功能出问题时，可以准确区分：

| 问题 | 应该看哪张地图 |
| --- | --- |
| 功能根本没编译 | 构建子树 |
| 注册项没有进入镜像 | 链接注册树 |
| 初始化顺序不对 | 运行时启动树 |
| 数据类型或单位不一致 | 运行时数据流树 |
| 板卡找不到设备 | 设备树与构建地图 |
| 线程没有被创建 | 配置、链接、启动三张图 |

这比“先去 main 里加日志”更快。

---

# 第六部分 顶层目录与责任边界

## 6.1 顶层责任表

| 顶层路径 | 所属边界 | 负责 | 不负责 |
| --- | --- | --- | --- |
| `framework/drivers` | 硬件访问 | 外设 API、缓冲、回调 | 业务策略 |
| `framework/algorithm` | 纯计算 | PID、滤波、辨识 | 硬件生命周期 |
| `framework/modules` | 设备能力 | IMU、Remote、电机 | 项目控制编排 |
| `framework/topic` | 数据契约 | zbus、msgq、消息结构 | 设备协议解析 |
| `framework/cmd` | 横切辅助 | shell、buzzer、flash、链接段 | 底盘业务 |
| `framework/init` | 生命周期 | main、阶段、注册表遍历 | 具体设备实现 |
| `platform` | 平台兼容 | CMSIS 等适配 | 机器人业务 |
| `boards` | 用户 board | board、DTS、runner | 线程逻辑 |
| `dts` | 用户 binding | compatible schema | C++ 协议 |
| `project` | 产品装配 | Kconfig、CMake、线程 | 通用框架机制 |

这张表看似简单，但它是整个工程可持续性的基础。

---

## 6.2 允许的依赖方向

当前建议依赖方向为：

```text
Zephyr
    <- platform
    <- boards/dts
    <- framework/drivers
    <- framework/algorithm
    <- framework/modules
    <- framework/topic
    <- framework/cmd
    <- framework/init
    <- project
```

更细一点：

```text
drivers
    -> Zephyr device / driver API

algorithm
    -> C/C++ 数学与 Eigen

modules
    -> drivers
    -> algorithm
    -> topic
    -> cmd 的稳定辅助入口

project/thread
    -> modules
    -> drivers
    -> algorithm
    -> topic
    -> init 注册接口
```

不应该出现：

```text
algorithm -> project/thread
drivers   -> chassis
topic     -> bmi088
init      -> specific motor
```

如果出现这些依赖，通常说明边界被倒置了。

---

## 6.3 “向下提供能力，向上组合语义”

当前架构可以用一句话概括：

```text
越靠下，越接近可复用能力。
越靠上，越接近当前产品语义。
```

例如：

```text
gpio_pin_set_dt
    -> Output::Set
        -> buzzer::Buzzer::On
            -> 启动提示
```

又例如：

```text
SPI 事务
    -> Bmi088::ReadRaw
        -> CalibSource::Read
            -> ImuManager::Task
                -> topic::imu_to::Message
```

每一层都把低层细节转换成更高层可理解的语义。

这就是分层的真正意义。

---

## 6.4 “业务层可以很懂业务”

分层不是要求业务层变得愚蠢。

底盘线程应该知道：

```text
舵轮位置
车体坐标
目标速度
电机组
功率预算
PID 参数
```

但它不应该知道：

```text
UART DMA 双缓冲如何切换
SPI CS 如何保持
DR16 帧如何位解包
USB qTD 如何回收
```

业务层的“懂业务”与底层的“懂硬件”应该并存。

而不是混在同一文件中。

---

## 6.5 “架构层可以很强，但不应该替业务决策”

当前 framework 已经包含：

```text
CAN
SPI
UART DMA
USB CDC
IMU
Remote
DJI 电机
DM 电机
功率计
PID
功率控制
四元数 EKF
Flash
Shell
启动注册
```

它已经不是一组零散工具。

但它仍然不应该替当前项目决定：

```text
底盘是否采用两轮舵轮
云台使用哪个电机
遥控器的某个开关对应什么业务
当前产品是否必须开启 IMU
功率预算如何按机器人策略分配
```

这些决定属于 `project/thread`。

---

# 第七部分 framework 六个独立子树

## 7.1 六个子树的总表

| 子树 | 主要回答的问题 |
| --- | --- |
| `drivers/` | 如何可靠访问一个硬件外设 |
| `algorithm/` | 如何在没有硬件的情况下完成计算 |
| `modules/` | 如何把设备变成可用能力 |
| `topic/` | 线程之间如何交换稳定数据 |
| `cmd/` | 横切辅助能力如何被无侵入调用 |
| `init/` | 已装配能力如何按阶段启动 |

六个子树相互配合，但不是一锅代码。

---

## 7.2 drivers 子树的位置

```text
framework/drivers/
```

当前包含：

```text
communication/can
communication/spi
communication/stream
device/gpio
device/pwm
```

驱动层的底线是：

```text
知道设备怎么访问。
不知道数据代表什么业务。
```

---

## 7.3 algorithm 子树的位置

```text
framework/algorithm/
```

当前包含：

```text
buffer
controller
filter
identify
math/eigen
```

算法层的底线是：

```text
知道输入输出和数学状态。
不持有 UART、CAN、SPI、PWM 设备句柄。
```

---

## 7.4 modules 子树的位置

```text
framework/modules/
```

当前包含：

```text
imu/
motors/
powermeter/
remotes/
```

模块层的底线是：

```text
知道一个设备能力如何工作。
不知道当前项目如何组合所有设备。
```

---

## 7.5 topic 子树的位置

```text
framework/topic/
```

当前包含：

```text
imu_to/
remote_to/
to_can_tx/
```

topic 层的底线是：

```text
知道消息形状、单位和传输语义。
不解析原始硬件协议。
```

---

## 7.6 cmd 子树的位置

```text
framework/cmd/
```

当前包含：

```text
shell/
buzzer/
flash/
linker/
build/
```

cmd 层的底线是：

```text
提供跨模块的小能力。
不成为第二个业务项目中心。
```

---

## 7.7 init 子树的位置

```text
framework/init/
```

当前包含：

```text
main.c
System_startup.h
Init_entry.hpp
Init_entry.cpp
Irq_handlers.h
Irq_handlers.cpp
CMakeLists.txt
```

init 层的底线是：

```text
知道如何遍历注册项。
不知道每个注册项的业务实现。
```

---

## 7.8 六个子树的组合关系

```text
boards + dts
       ↓
drivers
       ↓
modules
       ↓
topic
       ↑
algorithm
       ↑
project/thread
       ↑
init
       ↑
cmd
```

这张图不是严格的单向调用图。

它表示每一层主要提供的能力方向。

例如 `cmd` 的 flash 入口可以被 IMU 调用。

例如 `init` 的注册头文件可以被 project 和 cmd 包含。

但这些调用不会让 `cmd` 反向理解 IMU 业务。

---

# 第八部分 project 业务装配子树

## 8.1 project 的真正职责

`project/` 不是“剩下的代码都放这里”。

它是当前产品的装配层。

它回答：

```text
这个产品使用哪块板？
启用哪些能力？
创建哪些线程？
如何把能力组合成机器人行为？
```

它不回答：

```text
所有产品的通用驱动应该怎么写？
所有 IMU 的寄存器协议是什么？
所有遥控器帧格式如何解析？
```

---

## 8.2 project 的目录

```text
project/
├── .clangd
├── CMakeLists.txt
├── Kconfig
├── prj.conf
├── boards/
│   ├── hpm/
│   └── st/
└── thread/
    ├── CMakeLists.txt
    ├── thread.hpp
    ├── can/
    ├── chassis/
    ├── gimbal/
    ├── gpio/
    ├── imu/
    ├── pc/
    ├── remote/
    └── test/
```

这棵树是“产品装配树”。

它的每个分支都可能对应一个产品功能。

---

## 8.3 project/Kconfig 的业务门禁

当前业务符号包括：

```text
PRJ_MAIN
USE_CMD_SHELL
USE_CMD_BUZZER
USE_CMD_FLASH
TRD_GPIO
TRD_CHASSIS
TRD_GIMBAL
TRD_CAN_TX
TRD_REMOTE
TRD_IMU
TRD_PC
TRD_TEST
```

这些符号保留业务语义。

它们不使用 `DUST_` 前缀。

例如：

```kconfig
config TRD_REMOTE
    bool "Remote thread"
    select DUST_MOD_DEV_REMOTE
    select DUST_MOD_DEV_REMOTE_AUTO
```

这段配置表达了：

```text
产品要一个 Remote 线程。
Remote 线程需要 Remote 模块。
Remote 模块自动链接协议实现。
```

业务符号不需要知道所有底层源文件名。

---

## 8.4 project/thread 的线程目录

当前线程目录为：

```text
thread/
├── can/trd_can_tx.cpp
├── chassis/trd_chassis.cpp
├── chassis/trd_chassis.hpp
├── gimbal/trd_gimbal.cpp
├── gimbal/trd_gimbal.hpp
├── gpio/trd_gpio.cpp
├── imu/trd_imu.cpp
├── pc/trd_pc.cpp
├── remote/trd_remote.cpp
├── test/trd_test.cpp
├── CMakeLists.txt
└── thread.hpp
```

线程目录的每个叶子通常包含：

```text
一个 feature gate
一个 thread_init()
一个 thread_start()
一个 Task()
一组本地对象
两个注册宏
```

这使业务线程成为可以独立启停的装配单元。

---

## 8.5 project/thread 的边界价值

底盘线程可以直接拥有：

```text
四元运动学
轮子对象
控制器实例
功率控制器
CAN 发送组帧
```

因为这些是当前产品语义。

Remote 模块可以直接拥有：

```text
协议探测
UART 线参数
帧缓冲
锁定状态
超时行为
```

因为这些是设备能力语义。

两者不会互相吞并。

这正是 `project/thread` 与 `modules` 分开的意义。

---

# 第九部分 platform 平台兼容子树

## 9.1 platform 不是第二个 framework

`platform/` 当前很小。

小并不代表没有价值。

平台层的意义是：

```text
把特定架构、特定工具链、特定 SDK 兼容问题隔离起来。
```

如果这个问题放进业务代码：

```text
业务到处判断 CPU 架构
业务到处 include CMSIS
业务到处兼容旧符号
```

如果这个问题放进 platform：

```text
业务只看到稳定入口
平台层处理差异
```

---

## 9.2 platform/cmsis 的 module.yml

当前文件：

```text
platform/cmsis/zephyr/module.yml
```

内容声明：

```yaml
name: user_cmsis
build:
  kconfig: Kconfig
```

这使它可以被 Zephyr 的模块发现机制处理。

---

## 9.3 platform/cmsis 的 Kconfig

当前 `Kconfig` 预定义：

```kconfig
config HAS_CMSIS_CORE
    bool
```

这解决的是依赖解析问题。

它不是一个给用户选择的业务开关。

它更接近平台能力声明。

---

## 9.4 platform/cmsis 的头文件策略

`cmsis_core.h` 使用条件编译：

```c
#if defined(CONFIG_CPU_CORTEX_M)
#define SHPR SHP
#include <soc.h>
#include <core_cm4.h>
#endif
```

这种写法的关键点是：

```text
兼容逻辑集中在一个地方。
上层不需要重复判断 Cortex-M。
```

---

## 9.5 平台层的演进规则

平台层可以继续增加：

```text
芯片 ABI 适配
CMSIS 命名兼容
SDK 头文件桥接
缓存和内存属性入口
平台级链接布局适配
```

但不应增加：

```text
底盘线程
Remote 协议
IMU 业务策略
项目专属 CAN ID
```

平台是地基。

它不应该长成机器人。

---

# 第十部分 boards 与 dts 用户扩展子树

## 10.1 boards 与 project/boards 的区别

当前同时存在：

```text
boards/
project/boards/
```

两者职责不同。

| 路径 | 负责 |
| --- | --- |
| 根 `boards/` | 定义 Zephyr 可以识别的用户 board |
| `project/boards/` | 当前业务对某个 board_cfg 的 overlay、conf、runner |

根 `boards/` 描述：

```text
这块板卡是什么。
```

`project/boards/` 描述：

```text
当前产品在这块板卡上如何接线和启用功能。
```

这是一种非常漂亮的分离。

板卡身份可以复用。

产品接线可以变化。

---

## 10.2 board 身份与 board_cfg

当前构建系统中有两个相关名字：

```text
BOARD
BOARD_CFG
```

`BOARD` 是 Zephyr 的实际 board target。

`BOARD_CFG` 是项目侧配置目录选择器。

例如：

```text
BOARD_CFG=board_rm_c
BOARD=stm32f407igh6
```

构建脚本可以根据：

```text
project/boards/st/board_rm_c/stm32f407igh6.overlay
```

自动推导：

```text
west build -b stm32f407igh6
    -- -DBOARD_CFG=board_rm_c
```

这样：

```text
一块 board 可以对应多个产品配置。
一个产品配置可以清楚表达当前接线。
```

---

## 10.3 当前板级配置矩阵

当前 project 下的板级配置为：

| `BOARD_CFG` | `BOARD` | 主要平台 |
| --- | --- | --- |
| `hpm5361icb` | `hpm5361icb` | HPMicro |
| `hpm6e00evk` | `hpm6e00evk` | HPMicro |
| `board_rm_c` | `stm32f407igh6` | STM32F407 自定义板 |
| `puzhong` | `stm32f4_disco` | STM32F4 Disco |

这张表说明：

```text
BOARD_CFG 是产品/接线语义。
BOARD 是 Zephyr/board 语义。
```

两者没有被强行合并。

---

## 10.4 HPM5361ICB 的设备树别名

当前 HPM overlay 定义：

```dts
aliases {
    user-can1   = &mcan0;
    remote-uart = &uart4;
    imu-spi     = &icm42688p;
    imu-pwm     = &imu_pwm;
    buzzer-pwm  = &buzzer_pwm;
    flash-spi   = &w25q128;
    pc-usb      = &qingyuusb_usb0;
};
```

同时定义：

```dts
chosen {
    zephyr,console = &uart3;
};
```

这些 alias 是上层 C++ 的稳定硬件名字。

例如：

```cpp
DT_ALIAS(user_can1)
DT_ALIAS(remote_uart)
DT_ALIAS(imu_spi)
DT_ALIAS(imu_pwm)
DT_ALIAS(flash_spi)
```

上层不需要记住：

```text
具体 SOC 节点地址
具体 uart 编号
具体 spi 控制器编号
```

---

## 10.5 STM32F407IGH6 的设备树扩展

自定义 board 的 DTS 负责：

```text
芯片基础 dtsi
时钟
PLL
RAM/Flash chosen
LED alias
DMA 状态
```

业务 overlay 再负责：

```text
CAN1
CAN2
USART3 remote
USART6 console
BMI088 accel
BMI088 gyro
```

这使根 board 描述和当前机器人接线分离。

---

## 10.6 DeviceTree 是硬件契约

在当前工程中，设备树不是“附带配置”。

它定义：

```text
设备是否存在
设备是否 okay
设备连接哪条总线
设备使用哪个片选
设备使用哪个 DMA
设备使用哪个 pinctrl
设备用什么 alias 暴露
```

C++ 初始化只消费设备树结果：

```cpp
DEVICE_DT_GET(...)
SPI_DT_SPEC_GET(...)
PWM_DT_SPEC_GET(...)
GPIO_DT_SPEC_GET(...)
DT_REG_ADDR(...)
DT_IRQN(...)
```

因此硬件替换的第一入口通常是 overlay。

不是直接改驱动。

---

# 第十一部分 构建系统装配链

## 11.1 当前应用构建入口

当前应用入口：

```text
project/CMakeLists.txt
```

第一段路径设置：

```cmake
set(FW_ROOT         "${CMAKE_CURRENT_SOURCE_DIR}/../framework")
set(ZEPHYR_USER_DIR "${CMAKE_CURRENT_SOURCE_DIR}/..")
```

因此：

```text
FW_ROOT = E:/Zephyr/zephyr_user/framework
ZEPHYR_USER_DIR = E:/Zephyr/zephyr_user
```

这两条变量让业务工程不需要把六个 framework 路径写死。

---

## 11.2 SDK glue 选择

当前 CMake 先检查环境变量：

```cmake
if(DEFINED ENV{SDK_GLUE_DIR})
  set(SDK_GLUE_DIR "$ENV{SDK_GLUE_DIR}")
else()
  set(SDK_GLUE_DIR ".../Zephyr_HPMicro/sdk_glue")
endif()
```

这提供了两种工作方式：

```text
CI/脚本显式指定 SDK_GLUE_DIR
开发环境使用默认相对路径
```

当前还设置：

```cmake
set(SDK_GLUE_USER_DIR ".../Zephyr_HPMicro/sdk_glue_user")
```

这说明架构允许：

```text
官方 SDK glue
    +
用户 SDK glue 扩展
```

并列参与板卡和 SoC 发现。

---

## 11.3 BOARD_ROOT、SOC_ROOT、DTS_ROOT

当前 CMake 将用户树加入：

```cmake
list(APPEND BOARD_ROOT "${ZEPHYR_USER_DIR}")
list(APPEND DTS_ROOT   "${ZEPHYR_USER_DIR}")
list(APPEND DTS_ROOT   "${ZEPHYR_USER_DIR}/dts")
```

如果 SDK glue 存在，则继续加入：

```cmake
BOARD_ROOT
SOC_ROOT
DTS_ROOT
SDK glue dts root
SDK glue user root
```

这形成了一个多源发现路径：

```text
Zephyr 官方树
    +
zephyr_user
    +
sdk_glue
    +
sdk_glue_user
```

它是 HPM 平台能够在用户工程外部演进的重要原因。

---

## 11.4 ZEPHYR_EXTRA_MODULES

当前 CMake 设置：

```cmake
set(ZEPHYR_EXTRA_MODULES
  "${SDK_GLUE_DIR}"
  "${FW_ROOT}"
  "${ZEPHYR_USER_DIR}/platform/cmsis"
)
```

这三项分别代表：

| 模块 | 作用 |
| --- | --- |
| `SDK_GLUE_DIR` | HPM 官方适配能力 |
| `FW_ROOT` | framework 的 Zephyr module 声明 |
| `platform/cmsis` | 用户平台兼容能力 |

`framework/zephyr/module.yml` 当前声明：

```yaml
name: user_framework
build:
  settings:
    dts_root: drivers/communication/stream/usb
```

这使 USB binding 能进入 Zephyr 的设备树发现路径。

---

## 11.5 find_package 与 project

当前构建主流程：

```cmake
find_package(Zephyr REQUIRED HINTS $ENV{ZEPHYR_BASE})
project(my_project)
```

这表示：

```text
Zephyr 负责建立 app target。
project CMake 负责把用户代码装入 app。
```

因此，framework 子树不是一个独立的 ELF。

它们最终被组合为同一个 Zephyr app 镜像。

---

## 11.6 业务门禁控制装配

当前 CMake 设置：

```cmake
set(CONFIG_SYM PRJ_MAIN)
```

然后：

```cmake
if(CONFIG_${CONFIG_SYM})
  add_subdirectory(${FW_ROOT}/drivers ...)
  add_subdirectory(${FW_ROOT}/algorithm ...)
  add_subdirectory(${FW_ROOT}/modules ...)
  add_subdirectory(${FW_ROOT}/topic ...)
  add_subdirectory(${FW_ROOT}/cmd ...)
  add_subdirectory(${FW_ROOT}/init ...)
  add_subdirectory(thread)
endif()
```

这个门禁的意义是：

```text
PRJ_MAIN=n
    -> 当前维护者业务装配树不进入 app

PRJ_MAIN=y
    -> framework 六层 + project/thread 进入 app
```

它让业务项目有一个总开关。

同时，各层内部还有更细的能力开关。

---

## 11.7 为什么显式 add_subdirectory 很重要

当前采用：

```cmake
add_subdirectory(${FW_ROOT}/drivers ...)
```

而不是让所有代码自动扫描。

显式装配有三个好处：

```text
边界清楚
编译对象可追踪
不会因为目录里出现文件就意外进入产品
```

它特别适合嵌入式项目。

固件中每一个源文件都应该有明确的进入理由。

---

## 11.8 二进制目录隔离

当前 add_subdirectory 使用：

```cmake
${CMAKE_CURRENT_BINARY_DIR}/framework/drivers
${CMAKE_CURRENT_BINARY_DIR}/framework/algorithm
...
```

这会让不同子树的 CMake 中间产物分开。

它降低：

```text
同名目标冲突
同名生成文件冲突
构建目录难以阅读
```

这也是“源码树和构建树同时分层”的体现。

---

## 11.9 入口源码由 init 提供

当前 CMake 明确写出：

```cmake
# main() 由架构层 init 提供
```

并将：

```text
framework/init/main.c
```

加入：

```cmake
target_sources(app PRIVATE ...)
```

因此当前工程没有：

```text
project/src/main.c
```

业务项目不再拥有系统入口。

这正是架构层与业务层分开的关键证据。

---

## 11.10 CMake 装配图

完整构建图可以写成：

```text
west build
    -> project/CMakeLists.txt
        -> find_package(Zephyr)
        -> 设置 BOARD_ROOT/SOC_ROOT/DTS_ROOT
        -> 设置 ZEPHYR_EXTRA_MODULES
        -> 读取 BOARD_CFG
        -> 选择 overlay/conf/board.cmake
        -> 读取 project/Kconfig
        -> CONFIG_PRJ_MAIN
            -> framework/drivers
            -> framework/algorithm
            -> framework/modules
            -> framework/topic
            -> framework/cmd
            -> framework/init
            -> project/thread
        -> app ELF
```

这不是一个手工调用列表。

它是一棵由配置和构建共同决定的源代码子树。

---

# 第十二部分 Kconfig 配置门禁

## 12.1 Kconfig 在这棵树里的位置

Kconfig 不是“菜单文件”这么简单。

在当前架构中，Kconfig 是功能存在性的第一道门。

它负责把：

```text
产品选择
    -> 架构能力
        -> Zephyr 子系统
```

串成一个配置依赖图。

---

## 12.2 当前 Kconfig 入口

当前入口是：

```text
project/Kconfig
```

它的最后一行接入：

```kconfig
source "Kconfig.zephyr"
```

它的中间部分在 `if PRJ_MAIN` 内部使用：

```kconfig
rsource "../framework/drivers/Kconfig"
rsource "../framework/algorithm/Kconfig"
rsource "../framework/modules/Kconfig"
rsource "../framework/topic/Kconfig"
rsource "../framework/cmd/Kconfig"
rsource "../framework/drivers/communication/stream/usb/Kconfig"
```

这形成了：

```text
project/Kconfig
    -> framework capability Kconfig
        -> Zephyr Kconfig
```

---

## 12.3 为什么使用 rsource

`rsource` 使用相对于当前 Kconfig 文件的路径。

从：

```text
project/Kconfig
```

解析：

```text
../framework/drivers/Kconfig
```

得到：

```text
framework/drivers/Kconfig
```

这种写法有三个好处：

```text
不依赖调用者当前工作目录
不依赖绝对盘符
子树搬到同一相对位置后仍可解析
```

它把 Kconfig 的路径关系固定在源码树内部。

---

## 12.4 PRJ_MAIN 是业务总门禁

当前定义：

```kconfig
config PRJ_MAIN
    bool "Main business project"
    default n
    select DEBUG_LOG
```

并使用：

```kconfig
if PRJ_MAIN
    ...
endif
```

这意味着：

```text
PRJ_MAIN=n
    -> 维护者业务功能不进入配置空间

PRJ_MAIN=y
    -> 业务功能和 framework 能力进入配置空间
```

它是产品装配的顶层开关。

---

## 12.5 DEBUG_LOG 的能力拉取

当前定义：

```kconfig
config DEBUG_LOG
    bool "Enable Zephyr LOG output"
    default n
    select LOG
    select LOG_BACKEND_UART
```

`PRJ_MAIN` 通过 `select DEBUG_LOG` 拉起日志。

这体现了两层语义：

```text
PRJ_MAIN
    表达产品需要业务运行环境

DEBUG_LOG
    表达这个环境需要日志基础设施
```

业务不需要直接选择所有 Zephyr 日志底层符号。

---

## 12.6 业务符号与架构符号分离

当前命名约定：

```text
业务门禁：TRD_XXX / USE_XXX / PRJ_MAIN
架构能力：DUST_XXX
Zephyr 原生：CONFIG_XXX
```

例如：

```text
TRD_REMOTE
    -> DUST_MOD_DEV_REMOTE
        -> DUST_COM_UART_DMA
            -> SERIAL
            -> UART_ASYNC_API
            -> DUST_BUF_BIPBUF
```

这个名字链表达了：

```text
当前产品要什么
    -> 这个需求需要什么架构能力
        -> 架构能力需要什么底层资源
```

这比业务文件直接写 `CONFIG_UART_ASYNC_API=y` 更清晰。

---

## 12.7 当前业务开关总表

| 符号 | 默认值 | 主要选择 |
| --- | --- | --- |
| `USE_CMD_SHELL` | `n` | `DUST_CMD_SHELL_VAR` |
| `USE_CMD_BUZZER` | `n` | `DUST_CMD_BUZZER` |
| `USE_CMD_FLASH` | `y` | `DUST_CMD_FLASH` |
| `TRD_GPIO` | `y` | `DUST_DEV_GPIO_OUTPUT`、`DUST_CTL_TIMER` |
| `TRD_CHASSIS` | `n` | 电源、DJI 电机、CAN TX、Remote topic |
| `USE_POWERMETER` | `n` | `DUST_MOD_DEV_POWERMETER` |
| `TRD_GIMBAL` | `n` | PID、DM 电机、CAN TX、Remote topic |
| `TRD_CAN_TX` | `n` | CAN、CAN TX topic |
| `TRD_REMOTE` | `n` | Remote、自动协议 |
| `TRD_IMU` | `n` | IMU、IMU topic |
| `IMU_IDENTIFICATION` | `n` | IMU 辨识能力 |
| `TRD_PC` | `n` | USB CDC |
| `TRD_TEST` | `n` | PWM |

这张表是当前产品装配的开关面。

---

## 12.8 GPIO 线程的能力链

当前：

```kconfig
config TRD_GPIO
    default y
    select DUST_DEV_GPIO_OUTPUT
    select DUST_CTL_TIMER
```

它表达：

```text
GPIO 线程
    -> GPIO 输出驱动
    -> 软件定时器
```

`project/thread/gpio/trd_gpio.cpp` 中：

```cpp
#ifdef CONFIG_DUST_DEV_GPIO_OUTPUT
...
REGISTER_INIT(thread_init, PreInit, Low, "output_init");
REGISTER_THREAD(thread_start, PreThread, Low, "output_start");
#endif
```

因此存在三层裁剪：

```text
TRD_GPIO
    -> DUST_DEV_GPIO_OUTPUT
        -> trd_gpio.cpp 的输出分支
            -> .user_init / .user_thread
```

---

## 12.9 Remote 线程的能力链

当前：

```kconfig
config TRD_REMOTE
    select DUST_MOD_DEV_REMOTE
    select DUST_MOD_DEV_REMOTE_AUTO
```

模块层继续选择：

```kconfig
DUST_MOD_DEV_REMOTE
    select DUST_COM_UART_DMA
    select DUST_TPC_REMOTE_TO
```

这形成：

```text
TRD_REMOTE
    -> Remote 核心
        -> UART DMA
        -> Remote topic
        -> 自动协议注册表
```

业务线程只需要：

```cpp
remote_.Init(rx);
remote_.Start(ThreadPrio::High);
```

不需要自己选择 DR16、SBUS、VT12、VT13 的每个源文件。

---

## 12.10 IMU 线程的能力链

当前：

```kconfig
config TRD_IMU
    select DUST_MOD_DEV_IMU
    select DUST_TPC_IMU_TO
```

IMU 模块继续选择：

```kconfig
DUST_MOD_DEV_IMU
    select DUST_FLT_QUATERNION
    select DUST_DEV_PWM
    select DUST_CTL_PID
    select DUST_CTL_TIMER
    select DUST_ID_STABILITY
```

具体芯片再选择：

```text
DUST_MOD_DEV_IMU_BMI088
    -> DUST_COM_SPI

DUST_MOD_DEV_IMU_ICM42688P
    -> DUST_COM_SPI
```

于是：

```text
TRD_IMU
    -> IMU 管理
        -> SPI
        -> PWM 加热
        -> PID
        -> Timer
        -> 稳定判据
        -> 四元数 EKF
        -> zbus topic
```

这是一条完整的能力链。

---

## 12.11 CAN 线程的能力链

当前：

```kconfig
config TRD_CAN_TX
    select DUST_COM_CAN
    select DUST_TPC_TO_CAN_TX
```

它将：

```text
CAN 硬件访问
    +
CAN 发送消息队列
```

装配成一个发送线程。

CAN 接收回调分发能力位于 init 子树。

它不需要让 CAN 驱动知道电机协议。

---

## 12.12 底盘线程的能力链

当前：

```kconfig
config TRD_CHASSIS
    select DUST_MOD_CTL_POWER
    select DUST_MOD_DEV_MOTOR_DJI
    select DUST_TPC_TO_CAN_TX
    select DUST_TPC_REMOTE_TO
```

这代表：

```text
底盘业务
    -> 功率控制算法
    -> DJI 电机模块
    -> CAN 发送队列
    -> Remote 输入 topic
```

这正是“业务开关只描述业务需要”的例子。

底盘不需要直接 select：

```text
RLS
Eigen
LPF
CAN API
UART API
```

这些由能力链自动拉起。

---

## 12.13 配置的强大之处

Kconfig 的强大之处不是选项多。

而是它让依赖关系成为可读的图：

```text
业务语义
    -> 模块能力
        -> 驱动能力
            -> Zephyr 子系统
```

这个图还可以被构建系统和配置工具消费。

它比散落在 `.cpp` 里的 `#ifdef` 更接近系统设计。

---

## 12.14 `select` 的使用边界

当前工程使用 `select` 拉起硬件能力。

例如：

```kconfig
select DUST_COM_CAN
select DUST_DEV_PWM
select DUST_TPC_REMOTE_TO
```

使用时要保持语义单向：

```text
上层功能选择下层必需能力。
下层能力不反向选择具体业务。
```

不应出现：

```text
DUST_COM_CAN select TRD_CHASSIS
```

否则架构层会反向拥有业务。

---

## 12.15 prj.conf 的作用

当前 `project/prj.conf` 设置：

```text
CONFIG_CPP=y
CONFIG_STD_CPP17=y
CONFIG_STATIC_INIT_GNU=y
CONFIG_THREAD_NAME=y
CONFIG_SYS_CLOCK_TICKS_PER_SEC=2000
CONFIG_TICKLESS_KERNEL=n
CONFIG_FPU=y
CONFIG_FPU_SHARING=y
CONFIG_HW_STACK_PROTECTION=y
CONFIG_SERIAL=y
CONFIG_CONSOLE=y
CONFIG_UART_CONSOLE=y
CONFIG_LOG_MODE_IMMEDIATE=y
CONFIG_SPEED_OPTIMIZATIONS=y
CONFIG_PRJ_MAIN=y
```

这些是产品运行环境基础设置。

它们与具体功能开关不同。

可以把它们分成：

| 类别 | 例子 |
| --- | --- |
| 语言环境 | `CONFIG_CPP`、`CONFIG_STD_CPP17` |
| 调度环境 | `CONFIG_SYS_CLOCK_TICKS_PER_SEC` |
| 硬件环境 | `CONFIG_FPU`、`CONFIG_SERIAL` |
| 调试环境 | `CONFIG_CONSOLE`、`CONFIG_LOG_MODE_IMMEDIATE` |
| 产品门禁 | `CONFIG_PRJ_MAIN` |

---

## 12.16 Kconfig 的三种结果

一个配置项最终产生三类结果：

```text
结果一：符号状态
结果二：依赖符号状态
结果三：CMake 条件
```

例如：

```text
TRD_PC=y
    -> DUST_COM_USB=y
        -> DUST_USB_DEVICE_STACK=y
            -> CONFIG_USB=y
```

然后：

```cmake
if(CONFIG_DUST_COM_USB)
    add_subdirectory(communication/stream/usb)
endif()
```

最终才有：

```text
USB core
USB HAL
USB Stream adapter
PC thread
```

---

# 第十三部分 CMake 源码装配

## 13.1 Kconfig 和 CMake 是一对门

Kconfig 说：

```text
功能是否有资格存在？
```

CMake 说：

```text
这个功能的哪些源码真正进入编译？
```

两者缺一不可。

只有 Kconfig 没有 CMake：

```text
符号可能为 y
但实现没有进入 app
```

只有 CMake 没有 Kconfig：

```text
源码可能被无条件编译
产品无法裁剪
```

---

## 13.2 drivers CMake 映射

当前 drivers CMake 的主要映射：

| 配置 | 源文件 |
| --- | --- |
| `DUST_COM_CAN` | `communication/can/can.cpp` |
| `DUST_COM_RS485` | `communication/stream/rs485/rs485.cpp` |
| `DUST_COM_SPI` | `communication/spi/spi.cpp` |
| `DUST_COM_UART_DMA` | `communication/stream/uart/uart.cpp` |
| `DUST_DEV_GPIO_INPUT` | `device/gpio/input.cpp` |
| `DUST_DEV_GPIO_OUTPUT` | `device/gpio/output.cpp` |
| `DUST_DEV_PWM` | `device/pwm/pwm.cpp` |
| `DUST_COM_USB` | `communication/stream/usb` 子目录 |

每个分支同时提供 include directory。

---

## 13.3 algorithm CMake 映射

算法层中：

```text
header-only 能力
    -> 主要添加 include 目录

有 cpp 的能力
    -> target_sources(app ...)
```

当前例子：

```text
PID
    -> pid.cpp

Quaternion
    -> quaternion.cpp

MotorPlant
    -> motorplant.cpp

Kalman check
    -> kalman_check.cpp
```

这使模板算法和编译单元算法可以并存。

---

## 13.4 modules CMake 映射

modules CMake 先装配公共模块：

```cmake
if(CONFIG_DUST_MOD_DEV_IMU)
    target_sources(app PRIVATE
        imu/drivers/imu.cpp
        imu/drivers/heater.cpp
        imu/drivers/processor.cpp
    )
endif()
```

再根据具体源选择：

```cmake
if(CONFIG_DUST_MOD_DEV_IMU_BMI088)
    target_sources(app PRIVATE imu/devices/bmi088/bmi088.cpp)
endif()
```

这形成：

```text
公共流程
    +
具体设备实现
```

---

## 13.5 Remote CMake 映射

当前 Remote CMake 先加入核心：

```text
remotes/remote.cpp
```

然后按协议开关加入：

```text
dr16/dr16.cpp
sbus/sbus.cpp
vt12/vt12.cpp
vt13/vt13.cpp
```

自动协议模式可以一次性链接多个协议叶子。

这正是注册式架构的用武之地。

---

## 13.6 topic CMake 映射

当前 topic CMake：

```text
DUST_TPC_TO_CAN_TX
    -> to_can_tx.cpp

DUST_TPC_REMOTE_TO
    -> remote_to.cpp

DUST_TPC_IMU_TO
    -> imu_to.cpp
```

topic 的头文件目录也由同一个开关控制。

这样未选择的 topic 不会进入应用。

---

## 13.7 cmd CMake 映射

当前 cmd CMake 让头文件目录始终可见：

```cmake
target_include_directories(app PRIVATE shell)
target_include_directories(app PRIVATE buzzer)
target_include_directories(app PRIVATE flash)
```

但实现文件按配置选择：

```cmake
if(CONFIG_DUST_CMD_SHELL_VAR)
    target_sources(app PRIVATE shell/shell.cpp)
endif()
```

这和头文件宏降级配合：

```text
调用方可以稳定 include。
实现文件仍然可以完全裁剪。
```

---

## 13.8 init CMake 映射

当前 init CMake：

```cmake
target_sources(app PRIVATE
    ${CMAKE_CURRENT_SOURCE_DIR}/Irq_handlers.cpp
    ${CMAKE_CURRENT_SOURCE_DIR}/Init_entry.cpp
    ${CMAKE_CURRENT_SOURCE_DIR}/main.c
)

zephyr_linker_sources(ROM_SECTIONS ../cmd/linker/dust_init.ld)
```

这段代码完成两个动作：

```text
把启动实现放进 app。
把自定义注册 section 放进链接流程。
```

这是“源码入口”和“链接入口”同时集中在 init 子树的证据。

---

## 13.9 project/thread CMake 映射

当前业务线程 CMake 对每个 `TRD_XXX` 分支使用：

```cmake
target_sources(app PRIVATE ...)
target_include_directories(app PRIVATE ...)
```

例如：

```cmake
if(CONFIG_TRD_REMOTE)
    target_sources(app PRIVATE remote/trd_remote.cpp)
    target_include_directories(app PRIVATE
        ${CMAKE_CURRENT_SOURCE_DIR}
        ${CMAKE_CURRENT_SOURCE_DIR}/remote
    )
endif()
```

这让一个线程叶子具备：

```text
配置存在
    -> CMake 编译
        -> 注册项存在
            -> 运行时启动
```

---

## 13.10 源文件进入镜像的三道门

一个业务线程能否出现在最终镜像中，至少经过：

```text
第一道门：Kconfig
第二道门：CMake
第三道门：链接
```

如果代码定义了：

```cpp
REGISTER_THREAD(thread_start, MidThread, Mid, "foo_start");
```

但 `TRD_FOO=n`：

```text
trd_foo.cpp 不编译
注册项不存在
```

如果 CMake 漏了：

```text
Kconfig 可能为 y
但源文件不进入 app
```

如果链接脚本漏了 KEEP：

```text
对象可能被链接器裁掉
start/end 范围可能为空
```

---

# 第十四部分 设备树硬件契约

## 14.1 alias 是上层硬件命名空间

当前业务代码大量使用：

```cpp
DT_ALIAS(user_can1)
DT_ALIAS(remote_uart)
DT_ALIAS(imu_spi)
DT_ALIAS(imu_pwm)
DT_ALIAS(flash_spi)
```

这些 alias 不是随手起的变量名。

它们是“硬件资源语义名”。

例如：

```text
remote_uart
    表达当前产品的遥控器输入串口

imu_spi
    表达当前产品的 IMU 设备 SPI 节点

flash_spi
    表达当前产品的参数存储 SPI 节点
```

---

## 14.2 alias 把 board 差异留在 overlay

HPM 版本：

```dts
remote-uart = &uart4;
```

STM32 版本：

```dts
remote-uart = &usart3;
```

业务代码都可以保持：

```cpp
DT_ALIAS(remote_uart)
```

这就是设备树带来的替换能力。

---

## 14.3 chosen 与 alias 的区别

`chosen` 表达：

```text
系统约定的默认资源。
```

当前：

```dts
chosen {
    zephyr,console = &uart3;
};
```

`alias` 表达：

```text
应用语义上的资源名。
```

当前：

```dts
aliases {
    user-can1 = &mcan0;
    remote-uart = &uart4;
};
```

shell 使用 chosen console。

Remote 使用 alias remote-uart。

这两个命名空间没有混为一谈。

---

## 14.4 HPM5361ICB 的硬件组合

当前 HPM overlay 连接：

```text
mcan0     -> user-can1
uart4     -> remote-uart
spi2/imu  -> imu-spi
pwm0/7    -> imu-pwm
pwm0/5    -> buzzer-pwm
spi1/flash -> flash-spi
usb0      -> pc-usb
uart3     -> zephyr,console
```

这是一张产品级硬件地图。

上层模块不需要直接记住这些控制器编号。

---

## 14.5 STM32F407IGH6 的硬件组合

当前 `board_rm_c` overlay 连接：

```text
can1     -> user-can1
usart3   -> remote-uart
usart6   -> zephyr,console
bmi088_accel -> bmi088-accel
bmi088_gyro  -> bmi088-gyro
```

同时配置：

```text
can1 bitrate = 1000000
can2 bitrate = 1000000
usart3 baudrate = 100000
usart6 baudrate = 115200
```

这展示了同一套框架如何换用另一种传感器与 MCU。

---

## 14.6 设备树与 C++ 初始化的边界

设备树负责：

```text
节点存在性
总线关系
引脚和 DMA
设备参数
alias 和 chosen
```

C++ 负责：

```text
读取设备规格
检查 device_is_ready
调用驱动 API
执行寄存器序列
处理运行时失败
```

设备树不应该承载：

```text
业务状态机
线程周期
PID 算法
Remote 协议语义
```

C++ 也不应该把所有硬件连接重新写死。

---

# 第十五部分 init 启动子树

## 15.1 当前真实入口

当前真实入口文件：

```text
framework/init/main.c
```

当前实现：

```c
int main(void)
{
    System_Startup();
    while (1) {
        k_msleep(1000);
    }
}
```

因此当前入口链是：

```text
Zephyr kernel
    -> framework/init/main.c
        -> System_Startup()
            -> 各阶段注册项
```

业务工程没有另一个 `main()`。

---

## 15.2 init 子树的职责

init 负责：

```text
系统入口
初始化阶段
线程阶段
注册项结构
注册宏
失败等级
CAN section 分发入口
链接脚本接入
```

init 不负责：

```text
IMU 寄存器
Remote 协议
底盘运动学
电机反馈解释
USB CDC 业务
```

---

## 15.3 InitStage

当前初始化阶段：

```cpp
enum class InitStage : uint8_t
{
    PreInit   = 0,
    EarlyInit = 1,
    MidInit   = 2,
    LateInit  = 3,
    AppInit   = 4,
};
```

它解决的问题是：

```text
一次性资源准备在什么时候发生？
```

---

## 15.4 ThreadStage

当前线程阶段：

```cpp
enum class ThreadStage : uint8_t
{
    PreThread   = 0,
    EarlyThread = 1,
    MidThread   = 2,
    LateThread  = 3,
    AppThread   = 4,
};
```

它解决的问题是：

```text
线程启动动作在什么时候发生？
```

---

## 15.5 InitLevel

当前失败等级：

```cpp
enum class InitLevel : uint8_t
{
    High = 0,
    Mid  = 1,
    Low  = 2,
};
```

它解决的问题不是“什么时候运行”。

它解决的是：

```text
失败之后系统如何处理？
```

---

## 15.6 阶段与等级是两个维度

例如：

```cpp
REGISTER_INIT(thread_init, EarlyInit, High, "imu_init");
```

其中：

```text
EarlyInit
    -> 什么时候执行

High
    -> 失败后是否停机
```

这两个维度不能混为一谈。

一个很晚的应用功能也可以是 High。

一个很早的 LED 初始化也可以是 Low。

---

## 15.7 InitEntry

当前注册项结构：

```cpp
struct InitEntry
{
    InitFunc     func;
    InitStage    stage;
    InitLevel    level;
    const char*  name;
};
```

它是一个非常小的运行时描述对象。

它不保存：

```text
设备句柄
线程栈
业务状态
动态容器
```

它只保存：

```text
要调用谁
属于哪一阶段
失败如何处理
日志叫什么
```

这使注册表可以放进 ROMABLE_REGION。

---

## 15.8 REGISTER_INIT 展开逻辑

当前宏核心：

```cpp
#define REGISTER_INIT(fn, stage_, level_, name_) \
    static const InitEntry kInitEntry_##fn \
    __attribute__((used, __section__(".user_init"))) = { \
        fn, InitStage::stage_, InitLevel::level_, name_ \
    }
```

一个本地函数调用这个宏后：

```text
产生一个静态 const InitEntry
    -> 放到 .user_init
        -> 链接脚本保留
```

这个条目不需要被某个 C++ 中心数组引用。

---

## 15.9 REGISTER_THREAD 展开逻辑

当前宏核心：

```cpp
#define REGISTER_THREAD(fn, stage_, level_, name_) \
    static const InitEntry kThreadEntry_##fn \
    __attribute__((used, __section__(".user_thread"))) = { \
        fn, static_cast<InitStage>(ThreadStage::stage_), \
        InitLevel::level_, name_ \
    }
```

这里有一个当前实现细节：

```text
InitEntry 的 stage 字段统一使用 InitStage。
ThreadStage 在宏中转换成相同数值。
```

线程段独立存放。

线程遍历时再把值转换回 `ThreadStage`。

这是一种复用同一个条目布局的简化实现。

---

## 15.10 本地注册的意义

在 `trd_imu.cpp` 中：

```cpp
REGISTER_INIT  (thread_init,  EarlyInit,  High, "imu_init");
REGISTER_THREAD(thread_start, LateThread, High, "imu_start");
```

IMU 线程拥有自己的启动声明。

启动器不需要增加：

```cpp
InitImu();
StartImuThread();
```

这让中心启动器保持稳定。

---

# 第十六部分 链接段注册系统

## 16.1 链接段是这套架构的核心魔法

在普通 C++ 工程里，分散的实现要集中管理，常见方式是：

```cpp
Registry registry[] = {
    ...
};
```

当前工程使用另一种方式：

```text
每个实现自己声明条目。
编译器把条目放进 section。
链接器把 section 连接起来。
运行时遍历 section 边界。
```

这就是编译期注册表。

---

## 16.2 dust_init.ld 当前段

当前链接脚本：

```text
framework/cmd/linker/dust_init.ld
```

保留的 section：

```text
.user_init
.user_thread
.can_rx1
.can_rx2
.can_rx3
.remote
.imu
.shell_var
```

每一段都使用：

```text
SECTION_PROLOGUE
KEEP
start symbol
end symbol
GROUP_LINK_IN(ROMABLE_REGION)
```

---

## 16.3 .user_init

```text
注册宏：REGISTER_INIT
对象类型：InitEntry
消费者：RunStage(InitStage)
边界：__user_init_start / __user_init_end
```

它承载一次性初始化。

---

## 16.4 .user_thread

```text
注册宏：REGISTER_THREAD
对象类型：InitEntry
消费者：RunStage(ThreadStage)
边界：__user_thread_start / __user_thread_end
```

它承载线程启动动作。

---

## 16.5 .remote

```text
注册宏：REGISTER_REMOTE
对象类型：remote::RemoteEntry
消费者：Remote::InitRange / HandleDetecting
边界：__remote_start / __remote_end
```

它让协议实现像插件一样被 Remote 核心发现。

---

## 16.6 .imu

```text
注册宏：REGISTER_IMU
对象类型：imu::ImuEntry
消费者：ImuManager::InitSource
边界：__imu_start / __imu_end
```

当前 `ImuManager` 明确要求：

```text
0 个 -> 失败
1 个 -> 使用
多于 1 个 -> 失败
```

这是一种“编译期选择、运行时唯一性检查”的设计。

---

## 16.7 .shell_var

```text
注册宏：REGISTER_SHELL_VAR
对象类型：debug::Entry
消费者：DbgConsole::FindVar / CmdList / CmdGet / CmdSet
边界：__shell_var_start / __shell_var_end
```

业务代码只需要：

```cpp
REGISTER_SHELL_VAR("gain", gain);
```

shell 不需要认识变量所在模块。

---

## 16.8 .can_rx1/.can_rx2/.can_rx3

```text
注册宏：CAN_RX_HANDLER
对象类型：CanRxEntry
消费者：user_canN_rx_callback
边界：__can_rxN_start / __can_rxN_end
```

分发器只做：

```text
根据 CAN ID 线性查找
调用匹配 handler
```

分发器不做：

```text
CAN 设备初始化
过滤器配置
总线启动
IRQ 使能
物理层判断
```

这把“收到帧之后怎么分发”和“总线能不能收到帧”分成了两件事。

---

## 16.9 KEEP 的意义

如果注册条目没有被普通符号引用，链接器可能认为它是无用数据。

当前链接脚本使用：

```text
KEEP(*(.user_init))
```

以及其他 section 的 `KEEP`。

这确保：

```text
即使只有运行时通过 start/end 遍历，
条目也不会因为没有 C++ 直接引用而被裁掉。
```

---

## 16.10 start/end 符号的意义

运行时消费者不需要知道条目数量常量。

它只需要：

```cpp
extern const InitEntry __user_init_start[];
extern const InitEntry __user_init_end[];
```

然后：

```cpp
for (const InitEntry* e = __user_init_start;
     e < __user_init_end;
     ++e)
{
    ...
}
```

这是一种天然适合静态固件的容器。

它没有：

```text
动态分配
注册时锁
运行时扩容
中心数组长度维护
```

---

## 16.11 section 注册的统一公式

当前所有注册机制都可以抽象为：

```text
本地实现
    -> 静态描述对象
        -> 指定 section
            -> linker KEEP
                -> start/end
                    -> 专用消费者
```

这个公式已经贯穿：

```text
启动
线程
Remote
IMU
Shell
CAN
```

所以它是架构能力，而不是一次性技巧。

---

# 第十七部分 当前启动阶段

## 17.1 完整阶段序列

当前 `System_Startup()` 的顺序：

```text
PreInit
    -> 500 ms
PreThread
    -> 500 ms
    -> short buzzer

EarlyInit
    -> 500 ms
EarlyThread
    -> 500 ms
    -> short buzzer

MidInit
    -> 500 ms
MidThread
    -> 500 ms
    -> short buzzer

LateInit
    -> 500 ms
LateThread
    -> 500 ms
    -> short buzzer

AppInit
    -> 500 ms
AppThread
```

---

## 17.2 当前启动器不是依赖解析器

`RunStage()` 做的是：

```text
遍历当前 section
过滤 stage
按条目顺序调用
检查 bool 返回值
处理 InitLevel
```

它不做：

```text
拓扑排序
动态依赖解析
按名字排序
自动等待设备 ready
分析 topic 数据是否有效
```

因此复杂依赖不能只靠 stage 名字。

应当结合：

```text
Kconfig 依赖
模块 ready 状态
显式 Init 检查
topic 有效性
```

---

## 17.3 当前项目注册矩阵

| 注册项 | Init stage | Thread stage | Level |
| --- | --- | --- | --- |
| `output_init` | `PreInit` | `PreThread` | `Low` |
| `input_init` | `EarlyInit` | `EarlyThread` | `Low` |
| `remote_init` | `PreInit` | `PreThread` | `High` |
| `can_init` | `PreInit` | `PreThread` | `High` |
| `imu_init` | `EarlyInit` | `LateThread` | `High` |
| `chassis_init` | `MidInit` | `MidThread` | `Mid` |
| `gimbal_init` | `MidInit` | `MidThread` | `Mid` |
| `pc_init` | `PreInit` | `LateThread` | `High` |
| `test_init` | `LateInit` | `LateThread` | `High` |
| `dbg_init` | `PreInit` | `LateThread` | `High` |
| `buzzer` | `PreInit` | none | `Mid` |
| `w25q128` | `PreInit` | none | `Mid` |

这张表是当前源码中实际能搜索到的注册布局。

---

## 17.4 为什么 init 和 thread 分开

以 Remote 为例：

```text
remote_init
    -> 创建/配置 UartDma
    -> Remote::Init

remote_start
    -> Remote::Start
```

初始化阶段完成资源准备。

线程阶段只做线程启动。

这样可以避免：

```text
线程创建后发现 UART 尚未准备
线程循环抢先读取未初始化对象
初始化失败仍然启动线程
```

---

## 17.5 IMU 的特殊阶段跨度

当前 IMU：

```text
imu_init  -> EarlyInit
imu_start -> LateThread
```

这不是笔误式的相邻阶段。

它反映了 IMU 初始化可能包含：

```text
SPI 设备初始化
寄存器配置
加热器初始化
预热
自动校准
Flash 参数加载
姿态解算器初始化
```

这些工作可能在 `thread_init()` 返回前完成。

而真正的周期采样线程被延迟到 `LateThread`。

这是“资源准备”和“高频循环”之间的明确分隔。

---

## 17.6 InitLevel 的当前行为

当前 `HandleInitFail()`：

```text
High
    -> LOG_ERR
    -> EXEC_BUZZER_ERR
    -> while(1)

Mid
    -> LOG_WRN
    -> 继续

Low
    -> LOG_WRN
    -> 继续
```

它把失败处理策略写在注册项旁边。

例如：

```cpp
REGISTER_INIT(thread_init, PreInit, High, "can_init");
```

读者不需要去另一个中心文件猜 CAN 失败怎么办。

---

## 17.7 启动延时的正确理解

当前阶段之间有 `500 ms` 延时。

它的作用可以包括：

```text
让启动日志可读
让蜂鸣提示有节奏
给某些硬件上电后的可观察时间
```

但它不应该被当成精确依赖机制。

不应写成：

```text
sleep 500 ms 后 IMU 一定 ready
```

应该写成：

```text
IMU::Init 返回 true 才代表初始化成功。
```

---

# 第十八部分 注册表为什么强大

## 18.1 中央数组的传统问题

传统写法：

```cpp
InitEntry entries[] = {
    {"gpio", gpio_init},
    {"remote", remote_init},
    {"imu", imu_init},
};
```

它的问题：

```text
新增组件必须修改中央文件
删除组件必须修改中央文件
合并冲突集中在中央文件
层内子仓库无法独立注册
```

---

## 18.2 本地注册的改进

当前写法：

```cpp
static bool thread_init()
{
    return true;
}

REGISTER_INIT(thread_init, MidInit, Mid, "foo_init");
```

组件拥有：

```text
自己的实现
自己的初始化函数
自己的 stage
自己的失败等级
自己的诊断名称
```

中心启动器只负责通用流程。

---

## 18.3 注册表对 submodule 的价值

六个 framework 子仓库可以独立维护。

如果每个新能力都要修改 root 的中心注册数组：

```text
submodule 独立性会被破坏。
```

当前通过 section：

```text
子仓库内部注册
    -> 根工程只接入子仓库
```

这使 submodule 真正成为可组合单元。

---

## 18.4 注册表对产品线的价值

产品 A：

```text
Remote
IMU
CAN
底盘
```

产品 B：

```text
USB
GPIO
测试
```

产品 C：

```text
Remote
IMU
云台
```

三者可以共享：

```text
drivers
algorithm
modules
topic
cmd
init
```

只改变：

```text
project/Kconfig
project/thread
project/boards
```

---

## 18.5 注册表的一个重要限制

注册表解决的是：

```text
发现和遍历
```

它不自动解决：

```text
数据有效性
硬件 IRQ
协议正确性
线程实时性
多实例语义
```

所以当前文档强调：

```text
section 有条目
    !=
硬件已经工作
```

这是嵌入式排错时非常重要的边界。

---

# 第十九部分 当前架构的关键优势

## 19.1 优势总览

当前架构的优势不是单点优势。

它是多种机制叠加后的系统性优势：

```text
目录分层
    +
submodule 版本隔离
    +
Kconfig 裁剪
    +
CMake 装配
    +
DeviceTree 硬件抽象
    +
链接段注册
    +
阶段化启动
    +
topic 数据契约
    +
模块线程模型
```

---

## 19.2 优势一：新增功能不必扩大 main

新增线程时，主要改：

```text
project/Kconfig
project/thread/CMakeLists.txt
project/thread/foo/trd_foo.cpp
```

通常不需要改：

```text
framework/init/main.c
framework/init/Init_entry.cpp
```

这意味着启动骨架可以长期稳定。

---

## 19.3 优势二：新增协议不必修改核心状态机

新增 Remote 协议时：

```text
新建协议实现
实现 Validate
实现 Decode
设置 UART line_cfg
REGISTER_REMOTE
```

Remote 核心仍然使用：

```text
__remote_start
__remote_end
```

它不需要知道新协议的类名。

---

## 19.4 优势三：换板不等于重写业务

换板时主要变化：

```text
board target
overlay
conf
board.cmake
```

业务代码继续使用：

```cpp
DT_ALIAS(remote_uart)
DT_ALIAS(user_can1)
```

这使硬件差异停留在硬件配置层。

---

## 19.5 优势四：换 IMU 不等于重写姿态管理

当前 IMU 设备都继承：

```text
CalibSource
```

公共流程统一处理：

```text
原始样本
单位转换
偏置校准
比例修正
Sample 输出
```

具体设备只实现：

```text
Init
LateInit
ReadRaw
ConvertAccel
ConvertGyro
ConvertTemperature
```

`ImuManager` 不需要知道 BMI088 与 ICM42688P 的寄存器差异。

---

## 19.6 优势五：主动设备与被动设备不强行同构

当前模块区分：

```text
主动数据源：
    IMU
    Remote

被动回调设备：
    DJI motor
    DM motor
    PowerMeter
```

主动数据源需要线程。

被动 CAN 设备只更新快照。

这避免“每个设备一个线程”的资源浪费。

---

## 19.7 优势六：算法可以脱离硬件复用

PID 不知道 PWM。

PowerCtrl 不知道 CAN。

QuaternionEkf 不知道 SPI。

RLS 不知道电机型号。

这些算法可以被：

```text
底盘线程
云台线程
离线测试
仿真程序
参数辨识工具
```

复用。

---

## 19.8 优势七：数据契约不是裸全局变量

Remote 通过：

```text
topic::remote_to::Message
```

IMU 通过：

```text
topic::imu_to::Message
```

CAN TX 通过：

```text
topic::to_can_tx::Message
```

数据结构集中定义：

```text
字段名称明确
单位可以写在注释中
生产者和消费者容易检索
```

这比让每个线程直接访问另一线程的全局变量更稳。

---

## 19.9 优势八：调试能力是架构的一部分

`cmd/shell` 提供：

```text
l
g <name>
s <name> <value>
? / h
```

它通过链接段发现调试变量。

调试能力不是把 `printf` 散落到业务代码。

它成为一个可裁剪、可独立启动的横切子树。

---

## 19.10 优势九：错误策略可读

注册项写：

```cpp
REGISTER_INIT(thread_init, PreInit, High, "remote_init");
```

读者可以直接看到：

```text
Remote 很早初始化。
Remote 失败属于高等级。
日志名称是 remote_init。
```

这比所有错误都返回 `false`、再由中心文件决定严重程度更清楚。

---

## 19.11 优势十：注册机制可以横向复制

同样的机制已经复用于：

```text
启动项
线程项
Remote 协议
IMU 数据源
Shell 变量
CAN 接收回调
```

未来还可以复用于：

```text
诊断项
命令项
传感器校准项
日志过滤器
测试用例
协议编解码器
```

这说明架构具有扩展语言。

---

## 19.12 优势十一：变化被按原因分流

换硬件：

```text
boards/dts
```

换总线访问方式：

```text
drivers
```

换芯片寄存器：

```text
modules/<device>
```

换控制策略：

```text
project/thread
algorithm
```

换消息字段：

```text
topic
```

换启动规则：

```text
init
```

这就是高内聚、低耦合在工程中的实际表现。

---

## 19.13 优势十二：编译期和运行期职责清楚

编译期决定：

```text
功能是否存在
源文件是否编译
设备树节点是否生成
注册项是否产生
```

运行期决定：

```text
设备是否 ready
初始化是否成功
线程是否启动
数据是否超时
硬件是否真的收到帧
```

这两种问题不会混为一个“启动失败”。

---

## 19.14 优势十三：实时路径有上下文意识

UART、CAN、USB 回调可能在中断或驱动回调上下文。

当前设计倾向于：

```text
回调快速搬运数据
线程负责复杂解析
算法在线程中运行
topic 负责跨线程交换
```

例如：

```text
UART RX callback
    -> BipBuffer
    -> semaphore
    -> Remote thread
```

这比在 ISR 中直接执行完整协议和控制闭环更安全。

---

## 19.15 优势十四：硬件复杂度可以被封装

USB 子树当前处理：

```text
EP0
标准请求
CDC 类请求
端点生命周期
USB reset
DMA buffer
cache flush
IRQ
HPM EHCI
```

但 PC 线程只需要：

```cpp
usb_.Init(cfg);
usb_.Read(...);
usb_.Send(...);
```

这就是复杂底层封装后的价值。

---

## 19.16 优势十五：架构层可以独立演进

因为六个 framework 层是 submodule：

```text
drivers 可以单独升级
algorithm 可以单独升级
modules 可以单独升级
topic 可以单独升级
cmd 可以单独升级
init 可以单独升级
```

根仓库通过提交指针固定组合版本。

这比所有代码永远绑定在一个巨大提交历史里更灵活。

---

## 19.17 优势十六：应用接线是显式的

虽然架构使用自动注册，但构建装配仍然是显式的：

```text
project/CMakeLists.txt
    -> add_subdirectory(framework/...)
    -> add_subdirectory(thread)
```

这避免了：

```text
目录里出现一个临时 cpp
整个产品突然多出一条线程
```

自动发现只用于运行时注册。

源码是否参与构建仍由 CMake 明确决定。

这是“可扩展”和“可控”之间的平衡。

---

# 第二十部分 驱动子树

## 20.1 驱动子树的职责

当前驱动子树位于：

```text
framework/drivers/
```

它不是所有硬件代码的垃圾桶。

它的职责可以概括为：

```text
把 Zephyr 外设 API 和板级设备树实例，
整理成 framework 其他子树可以稳定使用的 C++ 能力。
```

驱动层关心：

```text
设备是否 ready
设备如何初始化
数据如何收发
中断或异步事件如何转换
缓冲区如何管理
并发边界在哪里
```

驱动层不应该关心：

```text
底盘如何计算轮速
云台如何进行角度控制
遥控器通道如何映射到机器人策略
某一款产品的战术状态
```

这条边界非常重要。

如果 UART 驱动知道“第三通道是右拨杆”，那么 UART 驱动已经开始承担业务语义。

如果 CAN 驱动知道“某个 ID 是底盘左轮”，那么 CAN 驱动已经开始绑定产品。

当前架构把这些事情分开：

```text
UART 只提供字节流
CAN 只提供帧收发
SPI 只提供总线访问
GPIO 只提供输入输出
PWM 只提供占空比控制
```

更高层的模块再把这些原始能力解释成：

```text
Remote
IMU
Motor
PowerMeter
USB
```

## 20.2 当前驱动子树的组织

当前可以把驱动目录理解成下面几类：

```text
framework/drivers/
├── CMakeLists.txt
├── Kconfig
├── communication/
│   ├── can/
│   ├── spi/
│   └── stream/
├── gpio/
├── pwm/
└── ...
```

实际目录还会随着 submodule 的演进继续细分。

但是职责不会因为目录变化而改变。

驱动子树的最小组织单位通常包括：

```text
一个头文件
一个实现文件
一个 Kconfig 符号
一个 CMake 源码门禁
一个或多个 Zephyr 设备树引用
```

完整接线可以写成：

```text
project/prj.conf
    -> CONFIG_DUST_COM_UART_DMA=y
        -> framework/drivers/Kconfig
            -> framework/drivers/CMakeLists.txt
                -> UartDma.cpp
                    -> Zephyr UART async API
                        -> DeviceTree chosen/alias
                            -> SoC 外设
```

这里的每一层都可以独立检查。

功能没有工作时，可以回答：

```text
配置有没有打开？
源文件有没有参与构建？
设备树节点有没有 ready？
驱动 Init 有没有返回成功？
回调有没有注册？
线程有没有启动？
```

这就是子树架构的诊断价值。

## 20.3 驱动层与设备模块层的区别

驱动层处理“怎么访问”。

模块层处理“这个设备是什么”。

以 IMU 为例：

```text
SPI 驱动
    -> 只负责 SPI 读写

BMI088 source
    -> 负责 BMI088 寄存器和量程

CalibSource
    -> 负责单位、偏置和标定

ImuManager
    -> 负责加热、校准、姿态和运行任务

project/thread/trd_imu.cpp
    -> 负责把 IMU 接入当前产品生命周期
```

以遥控器为例：

```text
UartDma
    -> 提供异步字节流

RemoteProtocol
    -> 判断某一帧是否属于某种协议

Remote
    -> 探测、锁定、超时、解码

project/thread/trd_remote.cpp
    -> 选择 DT_ALIAS(remote_uart) 并启动线程
```

这种分层让“同一设备的协议实现”和“不同设备之间的总线实现”不会互相污染。

## 20.4 驱动层的四个稳定接口

当前驱动代码反复体现四种接口：

```text
初始化接口
运行接口
异步回调接口
资源状态接口
```

初始化接口解决：

```text
设备句柄是否存在
设备是否 ready
参数是否有效
回调是否挂载
硬件是否启动
```

运行接口解决：

```text
读取
发送
停止
重启
重新配置
```

异步回调接口解决：

```text
DMA 接收完成
CAN 接收帧到达
USB 端点事件
错误状态变化
```

资源状态接口解决：

```text
当前是否 busy
当前是否 configured
当前是否 running
当前是否有可读数据
当前是否允许改线
```

这四类接口共同构成了比“几个裸函数”更完整的设备边界。

## 20.5 驱动子树的强大之处

驱动子树最强的地方，不是把 Zephyr API 换成了 C++ 名字。

真正的价值是把易错的细节集中起来：

```text
设备 ready 检查集中
异步回调转同步等待集中
缓冲区生命周期集中
并发保护集中
错误返回语义集中
设备树取值集中
```

上层模块因此不必每次重新发明：

```text
DMA 双缓冲
信号量同步
CAN filter
USB reset recovery
SPI 配置
GPIO 去抖
PWM 占空比
```

当多个产品共享同一驱动子树时，这种集中治理会产生复利。

## 20.6 驱动层的检查问题

阅读一个新驱动时，可以按以下顺序：

```text
1. Kconfig 符号是什么？
2. CMake 是否用该符号控制源文件？
3. 头文件暴露的是哪一层接口？
4. Init 是否检查 device_is_ready？
5. 异步回调运行在哪个上下文？
6. 回调是否只搬运数据，不做重业务？
7. Send 是否可能阻塞？
8. Read 是否有超时语义？
9. Stop 后能否安全重配？
10. reset 或 error 后能否恢复？
```

这个顺序会把“代码能编译”和“驱动能长期运行”区分开。

---

# 第二十一部分 Stream 通信抽象

## 21.1 Stream 解决的问题

当前通信流抽象位于：

```text
framework/drivers/communication/stream/
```

它把不同物理接口抽象成相近的使用方式：

```text
UART
USB CDC
RS485
```

它们的物理实现不同。

但是对上层协议来说，常见需求高度相似：

```text
启动
读取
发送
等待数据
查询状态
停止
```

`Stream` 的意义不是抹平所有差异。

它只抽象出真正稳定的共性。

这是一种克制的抽象。

## 21.2 Stream 的核心形态

当前 `Stream` 提供了类似下面的职责：

```cpp
class Stream
{
public:
    virtual bool Init(const BaseConfig& cfg) = 0;
    virtual bool Start() = 0;
    virtual bool Stop() = 0;
    virtual int Read(uint8_t* data, size_t len, k_timeout_t timeout) = 0;
    virtual bool Send(const uint8_t* data, size_t len) = 0;
};
```

实际接口以当前头文件为准。

这里最重要的不是函数名字。

最重要的是：

```text
上层协议依赖 Stream 能力
上层协议不依赖 UART 寄存器
上层协议不依赖 USB 端点编号
```

## 21.3 BaseConfig 的价值

通信流通常需要一组运行参数：

```text
波特率
数据位
停止位
校验位
回调
接收超时
```

如果这些参数散落在每个调用点，应用代码很快会变成：

```text
remote uart 这里配置一次
测试线程再配置一次
协议切换又配置一次
恢复流程再复制一份
```

统一配置结构的价值是：

```text
配置集中表达
生命周期可以复用
换底层实现时上层参数不必重写
```

它也给协议切换提供了基础。

当前 Remote 在锁定协议后可能需要改变：

```text
波特率
帧格式
接收长度
```

这些变化应该通过 Stream 的配置边界完成，而不是让 Remote 直接触碰 UART。

## 21.4 Read 的三个语义

一个成熟的流式读取接口至少需要说清楚三件事：

```text
有没有数据
读到了多少
等待多久
```

这三件事不能混为一谈。

例如：

```text
返回 0
```

可能表示：

```text
超时
当前没有完整帧
设备已经停止
```

如果没有明确约定，上层状态机会把不同故障误认为同一种情况。

当前 Stream 体系通过信号量、缓冲区和超时参数组合出等待语义。

因此调用方应该记录：

```text
调用前状态
返回值
等待时长
当前缓冲余量
```

在调试阶段，原始接收状态比“协议解析失败”更有价值。

## 21.5 Send 的非阻塞边界

实时控制线程不能假设每次发送都可以无限等待。

CAN 的发送通常使用：

```text
K_NO_WAIT
```

UART DMA 的发送也有 busy 状态。

USB 发送则需要考虑：

```text
端点是否 configured
上一次传输是否完成
是否需要 ZLP
```

所以 Stream 的 `Send` 不是“调用就一定发送完成”。

正确理解是：

```text
Send 接受数据
驱动根据当前资源状态安排发送
调用方根据返回值决定是否丢弃、重试或记录
```

如果上层忽略返回值，架构仍然可以工作，但可靠性信息会被吞掉。

这也是当前代码审查时需要单独关注的点。

## 21.6 Stream 与协议层的组合

组合关系如下：

```text
Stream
    -> 提供字节搬运

协议解析器
    -> 解释帧格式

设备模块
    -> 解释设备语义

业务线程
    -> 解释产品策略
```

同一个协议解析器可以在：

```text
UART
USB
虚拟串口
测试回放器
```

上复用。

前提是解析器只依赖它真正需要的输入。

## 21.7 Stream 抽象的边界

Stream 不应该负责：

```text
识别 DR16
识别 SBUS
识别 VT12
解算四元数
决定底盘模式
```

Stream 应该负责：

```text
字节可靠到达
缓冲区不越界
生命周期可控
忙状态可观察
错误能够返回
```

抽象强大的前提，是不把不属于它的知识塞进去。

---

# 第二十二部分 UART DMA 子树

## 22.1 UART DMA 的实际定位

当前 UART DMA 实现位于通信流驱动目录下。

它服务的是高频、连续、异步的字节流场景：

```text
遥控器
调试串口
Shell
传感器协议
RS485 上层协议
```

普通轮询 UART 和 DMA UART 的共同点是：

```text
都是字节流
```

不同点是：

```text
DMA UART 必须管理缓冲区、事件和传输状态
```

## 22.2 两块接收缓冲区

当前实现使用两块 DMA 接收缓冲区：

```text
rx_buffer_a
rx_buffer_b
```

典型工作方式是：

```text
DMA 正在写 A
CPU 处理 B
DMA 切换到 B
CPU 处理 A
```

这种结构的意义是：

```text
接收不会因为 CPU 短暂处理而立刻覆盖
驱动可以把硬件事件转换成连续字节流
上层不必知道 DMA 当前写哪一块
```

双缓冲不能解决所有实时性问题。

如果上层长期不消费，最终仍然会溢出。

但是它把短暂调度抖动从“立刻丢数据”变成了“有缓冲的延迟”。

## 22.3 BipBuffer 的价值

DMA 缓冲区和上层协议需要不同的数据形态。

DMA 产生的是：

```text
一段一段的接收块
```

协议需要的是：

```text
连续可读的字节序列
```

`BipBuffer` 的作用是把块状输入组织成可消费的双区域缓冲。

它帮助解决：

```text
环回边界
连续读取
写入位置
可读长度
```

上层协议不应该直接处理：

```text
DMA 半传输事件
DMA 完成事件
当前缓冲块编号
```

这些细节应该留在 UART DMA 驱动内部。

## 22.4 接收事件到信号量

当前驱动的重要路径可以抽象成：

```text
UART async callback
    -> 识别接收事件
        -> 把数据写入 BipBuffer
            -> give sem_
                -> Read/协议线程唤醒
```

这条路径体现了上下文意识：

```text
回调做轻量搬运
线程做协议解析
```

如果在 UART 回调里直接完成复杂协议解析，容易造成：

```text
中断或回调执行时间不可控
高优先级上下文被拖长
错误处理和重入复杂
```

当前结构把“数据到达”和“业务解释”拆开，是实时系统中非常重要的一刀。

## 22.5 发送状态

当前 UART DMA 发送使用单独的发送缓冲和 busy 状态。

典型状态是：

```text
idle
    -> Send 接受
        -> DMA active
            -> callback complete
                -> idle
```

如果发送尚未完成，新的 `Send` 可能返回失败。

这不是偶然错误。

这是驱动对资源所有权的诚实表达。

调用方必须决定：

```text
丢弃这一帧
放入上层队列
等待稍后重试
降低发送频率
```

驱动不能替业务自动选择策略。

## 22.6 Stop 与线配置

Remote 协议锁定时可能要切换 UART 线配置。

但是正在运行的 DMA 不能随意改参数。

所以正确流程是：

```text
停止接收
等待或确认 DMA 状态
修改 line_cfg
重新启动接收
```

这也是 `Stream` 生命周期抽象存在的原因。

如果协议模块直接修改底层 UART 配置，会把“切换时机”散落到多处。

集中由驱动处理，可以把非法状态提前拒绝。

## 22.7 遥控器实际链路

当前遥控器业务线程使用：

```cpp
DT_ALIAS(remote_uart)
```

因此板级差异是：

```text
HPM5361ICB -> uart4
STM32F407 -> usart3 或对应 overlay 定义
```

而 Remote 模块不需要知道具体外设编号。

完整链路是：

```text
project/boards/*/*.overlay
    -> remote_uart alias
        -> UartDma
            -> Remote
                -> zbus remote_to
                    -> chassis/gimbal thread
```

这就是“换板不等于重写遥控器业务”的直接证据。

## 22.8 UART DMA 的检查清单

遇到“串口没有数据”时，按层检查：

```text
1. remote_uart alias 是否存在？
2. UART 节点是否 status = "okay"？
3. DMA 配置是否打开？
4. DUST_COM_UART_DMA 是否为 y？
5. UartDma.cpp 是否进入 CMake？
6. device_is_ready 是否成功？
7. Init 是否返回成功？
8. Start 是否被调用？
9. async callback 是否进入？
10. BipBuffer 是否增长？
11. sem_ 是否唤醒？
12. Remote 是否仍处于 detecting？
13. 原始字节是否符合协议长度？
14. 解码后 zbus 是否发布？
```

这个检查顺序比直接修改解析器更可靠。

---

# 第二十三部分 CAN 子树

## 23.1 CAN 在架构中的位置

CAN 不是单纯的“发送函数”。

它在当前系统中同时承担：

```text
底层总线初始化
硬件过滤
接收回调
发送请求
队列边界
设备模块输入
电机控制输出
```

因此 CAN 链路跨越多个子树：

```text
boards/overlay
    -> framework/drivers CAN
        -> framework/modules motors/powermeter
            -> framework/topic to_can_tx
                -> project/thread/trd_can_tx.cpp
```

这种跨树不是耦合失控。

只要每个边界的所有权清楚，它就是完整系统的自然连接。

## 23.2 Can::Init 的核心流程

当前 CAN 驱动初始化可以按下面理解：

```text
获取 device
    -> 检查 device_is_ready
        -> 添加接收 filter
            -> 设置工作模式
                -> can_start
```

其中任何一步失败，都应该影响初始化结果。

`can_start` 成功并不自动证明：

```text
业务回调已经挂载
所有 filter 都正确
上层发送队列已经运行
硬件 IRQ 一定按照预期到达
```

所以 CAN bring-up 必须继续向上追踪。

## 23.3 板级 CAN 实例

当前板级 overlay 通过 alias 统一应用命名：

```dts
aliases {
    user-can1 = &mcan0;
};
```

或：

```dts
aliases {
    user-can1 = &can1;
};
```

业务线程只取：

```cpp
DT_ALIAS(user_can1)
```

这层别名就是硬件和业务之间的翻译表。

它把：

```text
mcan0
can1
mcan4
```

翻译成：

```text
当前产品的第一路用户 CAN
```

## 23.4 接收过滤和分派

CAN 接收至少有两种架构选择：

```text
所有帧进入一个统一回调
硬件或驱动先过滤，再由注册表分派
```

当前框架提供：

```text
CAN_RX_HANDLER
.can_rx1
.can_rx2
.can_rx3
user_can1_rx_callback
```

注册项包含：

```text
CAN ID
处理函数
```

链接器把注册项保留到指定 section。

回调再按当前帧 ID 查找处理函数。

这意味着新增设备处理器的主要动作可以是：

```text
在设备模块旁边注册自己的 CAN ID
```

而不是改动一个越来越长的中央 `switch-case`。

## 23.5 重要限制：注册项不等于硬件 IRQ

这一点必须单独强调。

出现下面这条链：

```text
CAN_RX_HANDLER
    -> .can_rx1
        -> __can_rx1_start/end
```

只能证明：

```text
链接器知道这些处理器
```

不能直接证明：

```text
CAN 设备已经 ready
can_start 已执行
filter 已安装
IRQ 已打开
硬件确实产生接收中断
```

因此 CAN 问题要分成两条证据链：

```text
注册和分派链
硬件和驱动链
```

两条都成立，业务回调才可能运行。

## 23.6 CAN 发送队列

当前 topic 子树定义了 CAN 发送队列：

```text
user_can1_msgq
```

消息包含：

```text
tx_id
data[8]
```

底盘和云台线程可以把控制意图写入队列：

```text
chassis thread
    -> enqueue CAN message

gimbal thread
    -> enqueue CAN message

CAN TX thread
    -> dequeue
        -> Can::Send
```

这比业务线程直接调用底层发送更容易控制：

```text
发送节拍
队列深度
总线拥塞
失败计数
统一日志
```

但是队列满时的处理策略仍然需要产品决定。

控制系统中，旧的电机控制帧和新的控制帧并不总是具有相同价值。

因此队列不是“放进去就完事”，而是一个需要定义丢弃语义的实时边界。

## 23.7 电机反馈链

DJI 电机和 DM 电机模块都需要处理：

```text
CAN RX frame
    -> ID 匹配
        -> 解析角度、速度、电流、温度
            -> 写入快照
                -> 控制线程读取
```

当前电机模块使用 seqlock 风格的快照保护读写。

它避免控制线程读到：

```text
角度已经更新
速度还没更新
温度仍是上一帧
```

这种“半新半旧”的组合。

快照的优势是：

```text
读取无需长时间持锁
写入可保持短路径
控制线程可以检测版本一致性
```

## 23.8 CAN 子树的厉害之处

CAN 子树把总线上的三种变化拆开了：

```text
换控制器实例
    -> overlay/驱动装配变化

换设备协议
    -> modules 变化

换产品控制策略
    -> project/thread 变化
```

这三种变化的原因不同。

架构没有强行把它们放到同一文件里。

这就是可维护性的来源。

---

# 第二十四部分 SPI、GPIO、PWM 子树

## 24.1 SPI 的边界

SPI 驱动位于通信驱动目录中。

当前 SPI 封装的价值主要在于：

```text
从设备树取得 spi_dt_spec
统一设备 ready 检查
统一同步读写入口
减少业务代码接触 Zephyr 结构体
```

SPI 本身不解释：

```text
BMI088 的寄存器含义
ICM42688P 的温度单位
W25Q128 的文件布局
```

它只提供总线层能力。

上层设备驱动再负责：

```text
片选
寄存器地址
读写位
校验
初始化序列
```

## 24.2 SPI 与设备树别名

当前板级配置会为 IMU 和 flash 提供别名或节点引用。

例如：

```dts
aliases {
    imu-spi = &icm42688p;
    flash-spi = &flash0;
};
```

不同板卡可以把相同功能放在不同 SPI 控制器上。

上层设备模块只依赖：

```text
imu-spi
flash-spi
```

而不是：

```text
spi1
spi2
具体 SoC 的控制器编号
```

## 24.3 GPIO Input 的去抖

当前 GPIO 输入驱动使用 16 位移位历史实现简单去抖。

可以把它理解成：

```text
每次采样把新电平移入历史寄存器
根据连续历史判断稳定高或稳定低
```

这种方法的优点是：

```text
内存小
时间复杂度固定
不需要动态分配
容易放入周期线程
```

它不是所有按键场景的万能滤波器。

但对于嵌入式输入信号，它提供了一个清楚、可测试的最小实现。

## 24.4 GPIO Output 的职责

GPIO 输出驱动处理：

```text
引脚配置
初始电平
写入电平
```

蜂鸣器、指示灯、使能脚等功能可以通过设备树节点绑定。

上层只表达：

```text
打开
关闭
翻转
```

而不是直接调用：

```text
GPIO port
pin number
flags
```

这使得同一功能可以换引脚而不改业务代码。

## 24.5 PWM 的职责

PWM 驱动负责：

```text
设备 ready
周期配置
脉宽配置
占空比更新
```

典型使用者包括：

```text
IMU 加热器
蜂鸣器
电机测试
舵机或执行器测试
```

IMU 加热器之所以可以放在模块层，是因为它把：

```text
PWM 输出
    +
温度采样
    +
PID
```

组合成了设备能力。

PWM 驱动本身不需要知道温度控制。

## 24.6 小外设的架构价值

GPIO、PWM、SPI 看起来简单。

但是它们是最容易被业务代码直接绕过的地方。

一旦业务线程开始直接访问 Zephyr device：

```text
硬件名会泄漏
初始化顺序会泄漏
错误处理会重复
测试替身变难
```

把小外设也放入子树边界，能保持整个工程的语言一致性：

```text
业务描述意图
驱动描述操作
设备树描述连接
```

---

# 第二十五部分 USB 分层子树

## 25.1 USB 为什么需要多层

USB 的复杂度明显高于普通 GPIO。

当前 USB 子树至少涉及：

```text
USB device core
EP0 标准请求
CDC ACM 类请求
配置和端点
RX/TX 队列
USB reset
HPM HAL
PHY
IRQ
cache
```

如果这些内容全部写进 `trd_pc.cpp`，业务线程就会被硬件细节淹没。

当前结构将它们拆成：

```text
USB Stream adapter
    -> USB device port
        -> USB HAL
            -> SoC USB controller/PHY
```

## 25.2 USB 核心层

USB 核心层处理与具体 SoC 相对无关的内容：

```text
设备请求
配置状态
端点对象
CDC 描述符
类请求
控制传输
```

它应该尽量保持：

```text
知道 USB 规范
不知道某个 SoC 的寄存器布局
```

这为未来更换 HAL 留出了空间。

## 25.3 UsbDevPort 的职责

`UsbDevPort` 负责端口级生命周期：

```text
EP0 处理
标准请求
配置变化
CDC endpoint
接收重新排队
reset recovery
```

USB 设备不是初始化一次就永远稳定。

主机可能会：

```text
枚举
重新配置
reset
断开
重新连接
```

所以端口层必须处理动态状态。

## 25.4 Usb Stream adapter

USB Stream 把端点队列转换成 Stream 接口：

```text
USB RX packet
    -> UsbRxQueue
        -> Read

Send
    -> tx buffer
        -> endpoint transfer
```

上层 PC 线程因此可以使用：

```cpp
usb_.Init(cfg);
usb_.Read(...);
usb_.Send(...);
```

而不必知道：

```text
端点编号
USB 包大小
ZLP
控制请求
```

## 25.5 USB reset 与队列清理

USB reset 时，旧的接收数据可能已经失去上下文。

因此当前 USB 适配层会关注：

```text
清理接收队列
清理 configured 状态
清理 tx_busy
重新打开端点
重新投递接收
```

如果 reset 后仍然使用旧状态，常见表现是：

```text
主机看到了设备
设备却不再接收数据
发送一直 busy
重新插拔后暂时恢复
```

这类问题必须在 USB 子树内解决，不能让业务线程到处补丁式重启。

## 25.6 HPM HAL 的职责

HPM HAL 处理：

```text
时钟
PHY
控制器初始化
动态 IRQ
cache flush/invalidate
non-cache buffer
endpoint open/close
ISR
```

这些是芯片平台细节。

如果未来换成 STM32 USB HAL，理想情况下变化应集中在：

```text
USB HAL 子树
板级配置
对应 Kconfig/CMake
```

核心 USB 协议和 Stream 使用者不应该大面积改动。

## 25.7 当前 USB 配置的正确理解

不同板卡的 `.conf` 可能关闭某个 HAL 或设备栈能力。

这不表示 USB 设计不存在。

应该区分：

```text
USB 子树已经提供了什么能力
当前产品配置选择了什么能力
当前硬件是否完成了验证
```

架构能力、配置选择、硬件验证是三个不同问题。

## 25.8 USB 子树的优势

USB 是当前架构“复杂硬件被封装”的最好例子。

业务层面对的是：

```text
一个可以读写的通信流
```

底层面对的是：

```text
一整套设备枚举、端点、缓存和中断问题
```

复杂度没有消失。

它被放到了正确的边界里。

---

# 第二十六部分 algorithm 算法子树

## 26.1 algorithm 的基本定位

当前算法子树位于：

```text
framework/algorithm/
```

它主要承载：

```text
滤波
控制
估计
辨识
四元数
计时
稳定性判断
功率分配
```

算法子树和设备模块的分界是：

```text
algorithm 负责可复用的数学和控制机制
modules 负责把机制放进具体设备生命周期
project/thread 负责选择产品策略
```

例如 PID 算法不应该知道：

```text
它控制的是哪个电机
反馈来自哪个 CAN ID
底盘当前处于什么比赛模式
```

它只应该知道：

```text
目标值
测量值
采样时间
限幅
控制参数
```

## 26.2 算法子树的配置裁剪

算法目录提供多个 Kconfig 能力开关。

这些开关可以控制：

```text
PID
低通滤波
高通滤波
Kalman/EKF
Quaternion
RLS
PowerCtrl
Timer
Eigen
MotorPlant
Stability
```

配置关闭某个算法后，理想效果是：

```text
对应源码不编译
对应模板不实例化
对应依赖不被拉入
固件尺寸不被无意义扩大
```

这使算法库可以同时服务：

```text
资源很小的测试板
带 IMU 的姿态系统
带功率限制的底盘
需要在线辨识的实验产品
```

## 26.3 PID 的工程化程度

当前 PID 不是只有一条：

```text
output = kp * error
```

它还考虑：

```text
D-first
可变积分
积分分离
D 项低通
前馈
死区
输出限幅
角度环绕
```

这些功能的价值在于：

```text
同一控制结构可以覆盖更多执行器
业务线程只组合参数和反馈
控制细节不会散落成大量手写公式
```

例如串级控制可以表达为：

```text
角度误差
    -> 角度环 PID
        -> 速度目标
            -> 速度环 PID
                -> 电流/力矩目标
```

算法层提供的是每一级控制器。

底盘线程决定：

```text
哪一级是外环
哪一级是内环
目标如何映射
何时启用或禁用
```

## 26.4 变量采样时间

控制算法不应该默认所有循环都严格等周期。

当前 IMU 运行路径会使用 cycle counter 计算实际 `dt`。

这比固定写死：

```text
dt = 0.001f
```

更诚实。

固定周期仍然是设计目标。

实际周期测量则是运行事实。

二者结合可以区分：

```text
控制器设计频率
线程调度频率
传感器到达频率
当前一次计算的真实间隔
```

这也是算法子树和实时操作系统之间的连接点。

## 26.5 滤波器的组合

滤波器并不是越多越好。

当前算法库提供：

```text
LPF
HPF
Kalman/EKF
```

使用时需要先问：

```text
噪声来自传感器还是执行器？
需要降低高频噪声还是估计不可测状态？
滤波延迟是否会破坏控制相位？
是否有足够的采样频率？
```

如果只是想消掉高频测量噪声，低通可能足够。

如果需要估计姿态和陀螺零偏，才有必要进入 EKF。

把所有问题都交给复杂滤波器，反而会降低系统可解释性。

## 26.6 Quaternion EKF 的边界

Quaternion EKF 当前使用固定尺寸 Eigen 类型。

它可以处理：

```text
姿态四元数
陀螺零偏
重力观测
创新检验
卡方门限
偏航处理
```

它不应该负责：

```text
SPI 读寄存器
加热 IMU
选择哪个 IMU source
决定是否自动标定
向电机发送控制帧
```

这些职责分别属于：

```text
驱动
heater
ImuManager
Calibration
project/thread
```

算法边界越清晰，姿态估计才越容易在离线数据、仿真数据和真实传感器之间复用。

## 26.7 PowerCtrl 的模型价值

当前功率控制算法不是简单的静态限流。

它可以使用类似下面的模型预测：

```text
P_hat = K1 * tau^2
      + K2 * omega^2
      + tau * omega
      + K3
```

并支持：

```text
RLS 参数更新
成员函数分配
约束电流计算
```

这意味着控制系统可以从：

```text
发现电流超限 -> 直接硬削减
```

升级为：

```text
估计负载
    -> 预测功率
        -> 分配执行器预算
            -> 在约束内保持尽可能多的控制能力
```

这类能力必须放在 algorithm，而不是底盘线程的巨大公式块中。

## 26.8 algorithm 子树的复用方式

算法子树的复用通常有三种形式：

```text
直接实例化
    -> PID<float>

组合对象
    -> PowerCtrl + Motor feedback

模块内部封装
    -> ImuManager 使用 QuaternionEKF
```

这三种复用方式都保持一个原则：

```text
算法对象拥有算法状态
业务层拥有业务策略
```

这样可以避免把积分项、滤波器状态和产品状态混在一个全局结构体里。

## 26.9 算法子树的厉害之处

算法子树把“数学复杂度”从“产品复杂度”中分离出来。

产品线程可以说：

```text
用这个 PID 控角度
用这个 PID 控速度
用这个 PowerCtrl 做功率分配
```

算法库内部负责：

```text
积分防 windup
角度 wrap
导数滤波
矩阵更新
创新门限
参数辨识
```

如果以后需要替换算法，只要保持输入输出契约，业务线程仍然可以保持稳定。

---

# 第二十七部分 modules 设备模块子树

## 27.1 modules 解决什么问题

当前设备模块子树位于：

```text
framework/modules/
```

它位于驱动和项目线程之间。

可以用一句话概括：

```text
modules 把底层设备访问组织成可复用的设备能力。
```

驱动知道：

```text
怎么收字节
怎么收 CAN 帧
怎么读 SPI
怎么写 PWM
```

模块知道：

```text
这一串字节是不是遥控器
这个 CAN 帧是不是电机反馈
这个 SPI 设备是不是 BMI088
这个温度闭环是不是 IMU heater
```

## 27.2 modules 的典型目录

当前模块子树包含多类设备：

```text
framework/modules/
├── imu/
├── remotes/
├── motors/
│   ├── dji/
│   └── dm/
└── powermeter/
```

未来还可以继续增加：

```text
编码器
电源管理
视觉协议
执行器组
状态监控设备
```

只要新能力的生命周期和协议边界清楚，就可以成为新的模块叶子。

## 27.3 模块不等于线程

这是当前架构非常重要的分离：

```text
模块是能力
线程是调度方式
```

一个模块可以：

```text
由一个线程调用
由多个阶段调用
被测试代码直接驱动
只通过回调被使用
```

一个线程也可以：

```text
组合多个模块
读取多个 topic
产生多个输出
```

如果每个模块都自己创建线程，系统容易出现：

```text
线程数量失控
优先级难以统筹
生命周期重复
测试需要启动完整系统
```

当前架构更倾向于：

```text
模块提供 Init/Start/Read/Process
project/thread 决定线程和优先级
```

## 27.4 模块的四类状态

设备模块通常需要管理：

```text
未初始化
已初始化但未运行
运行中
故障或超时
```

以 Remote 为例，它还细分为：

```text
detecting
locked
timeout
unlocking
```

以 IMU 为例，它还会经历：

```text
source init
heater init
preheat
auto calibration
attitude init
ready
```

把这些状态集中在模块内部，业务线程只需要观察稳定结果。

## 27.5 模块的输入和输出

模块输入可能来自：

```text
Stream
CAN RX
SPI
GPIO
PWM feedback
```

模块输出可能进入：

```text
zbus
快照
msgq
回调
状态接口
```

这形成了一个可追踪的数据边界：

```text
物理输入 -> 模块解释 -> 稳定输出
```

模块内部可以隐藏协议细节。

但模块对外的输出结构必须稳定、命名明确、单位清楚。

## 27.6 模块的生命周期

一个设备模块如果被很多项目复用，至少需要说明：

```text
Init 做什么
LateInit 做什么
Start 做什么
线程是否由自己创建
Read 是否阻塞
失败后能否重试
Stop 是否存在
```

当前 IMU 的生命周期尤其具有代表性：

```text
InitSource
    -> heater Init
        -> Preheat
            -> AutoCalib
                -> source LateInit
                    -> attitude Init
                        -> ready
```

这比“构造对象就算初始化完成”更适合真实硬件。

## 27.7 模块子树的复用价值

模块层把产品线程从以下细节中解放出来：

```text
协议帧长度
设备寄存器
标定偏置
超时恢复
协议探测
快照一致性
```

产品线程可以集中做：

```text
模式选择
控制目标
故障降级
执行器组合
```

这就是“业务代码更懂业务”的前提。

---

# 第二十八部分 IMU 组合子树

## 28.1 IMU 是架构能力的综合样板

当前 IMU 路径同时连接：

```text
SPI 驱动
BMI088/ICM42688P source
标定
加热器
PID
温度控制
QuaternionEKF
线程
topic
```

它不是一个简单的传感器读取例子。

它是当前子树架构如何组合能力的完整样板。

## 28.2 IMU source 接口

当前设备层抽象出 source：

```text
Init
LateInit
Read
Calibrate
```

BMI088 和 ICM42688P 都可以实现这组能力。

上层 `ImuManager` 不需要把每个芯片的寄存器初始化写成：

```cpp
if (BMI088) { ... }
else if (ICM42688P) { ... }
```

它通过 `.imu` section 得到注册的 source。

这就是设备插件化。

## 28.3 单 source 约束

当前 `ImuManager::InitSource()` 要求：

```text
注册 source 数量必须恰好为一个
```

状态可以写成：

```text
0 个 source
    -> 没有可用 IMU

1 个 source
    -> 正常选择

多个 source
    -> 配置存在歧义，拒绝启动
```

这个约束很有价值。

它没有让运行时偷偷选择“第一个能用的设备”。

它把配置错误尽早暴露出来。

## 28.4 IMU 初始化阶段

当前 IMU 初始化可以展开为：

```text
EarlyInit
    -> imu_.Init(AutoCalib)
        -> 选择 source
        -> 初始化硬件
        -> 初始化 heater
        -> 预热
        -> 可选自动标定
        -> source LateInit
        -> 姿态估计器初始化

LateThread
    -> imu_.Start(High)
        -> 建立周期任务
```

这里有一个关键设计：

```text
初始化对象
和
启动周期运行
```

不是同一个动作。

传感器可以先完成：

```text
设备配置
预热
静态标定
姿态初值
```

再进入稳定周期。

## 28.5 加热和姿态不是两个孤立功能

IMU 温度变化会影响：

```text
陀螺零偏
加速度偏置
噪声水平
姿态估计稳定性
```

所以 heater 并不是一个完全无关的附件。

当前模块把：

```text
PWM
温度控制
PID
预热
可选辨识
```

放在 IMU 运行路径中。

这使启动流程可以明确表达：

```text
先把传感器带到可用温度
再做需要稳定条件的标定
再进入姿态输出
```

## 28.6 标定数据路径

公共 `CalibSource` 负责：

```text
原始采样
偏置
比例
单位转换
Sample 输出
```

因此设备 source 可以专注于：

```text
芯片寄存器
原始数据读取
硬件自检
芯片特有标定
```

公共层负责：

```text
系统统一格式
轴向约定
单位约定
运行时校正
```

这种拆分避免 BMI088 和 ICM42688P 各自复制一套完全相同的校正逻辑。

## 28.7 IMU 运行循环

当前运行循环可以抽象为：

```text
读取实际 dt
    -> source.Read
        -> heater.Update
            -> EKF.Process
                -> 发布或提供姿态结果
```

每一步都具有不同的时间特性：

```text
Read 受硬件采样影响
heater 受温度控制带宽影响
EKF 受 dt 和观测质量影响
topic 受消费者调度影响
```

把它们放在一个模块内部统一编排，能够保持传感器样本的时间顺序。

## 28.8 当前 IMU 输出状态的阅读方式

当前源码中，某些 `zbus_chan_pub` 路径可能仍处于注释或未完成接线状态。

因此不能只看到：

```text
ImuManager 存在
EKF 存在
topic 定义存在
```

就断言：

```text
姿态已经稳定发布给所有消费者
```

正确的证据链是：

```text
source.Read
    -> Sample 有效
        -> EKF.Process 执行
            -> 输出变量更新
                -> zbus_chan_pub 成功调用
                    -> 消费线程订阅或读取
```

这正体现了本文坚持“能力”和“当前接线”分开。

## 28.9 IMU 子树的厉害之处

换 IMU 时，理论上的变化路径是：

```text
新增 source
    -> REGISTER_IMU
        -> 选择一个 Kconfig
            -> 选择一个 overlay 节点
                -> ImuManager 继续使用统一接口
                    -> project/thread/trd_imu.cpp 基本不变
```

这就是设备可替换性。

它不是没有成本。

芯片驱动、轴向、时序和标定都需要验证。

但变化被收束在正确的叶子节点，而不是扩散到整个系统。

---

# 第二十九部分 Remote 协议子树

## 29.1 Remote 的双层结构

当前 Remote 同时包含：

```text
传输层
    -> UartDma

协议层
    -> DR16
    -> SBUS
    -> VT12
    -> VT13
```

Remote 核心负责：

```text
探测
锁定
切换
解码
超时
失锁
发布
```

协议插件负责：

```text
帧长度
帧校验
协议解码
线路配置
```

## 29.2 RemoteEntry

当前注册项包含类似信息：

```text
name
frame_size
protocol
priority
need_hits
```

这些字段把协议差异数据化了。

Remote 状态机不需要知道：

```text
DR16 的通道在第几个字节
SBUS 的反相由谁处理
VT13 的校验怎么算
```

它只调用统一的：

```text
Validate
Decode
line_cfg
```

## 29.3 探测和锁定

Remote 初始处于 detecting。

可以把它理解为：

```text
从所有已注册协议中寻找候选
```

只有一次校验成功不一定足够。

当前注册项提供 `need_hits`，意味着：

```text
连续多次验证通过
    -> 才锁定协议
```

这能降低随机字节刚好通过一次校验造成的误锁定。

锁定之后：

```text
只使用当前协议解码
统计连续失败
```

达到失锁阈值才重新探测。

## 29.4 协议切换

切换协议不能只改一个指针。

正确动作至少包括：

```text
停止当前接收
切换 line config
清理或确认旧缓冲状态
启动新的接收配置
更新当前 entry
重置命中和失败计数
```

当前 `SwitchProto` 体现了这种生命周期意识。

如果不清理旧缓冲，旧协议残留字节可能会被新协议误解。

这也是协议切换中最容易被忽略的工程细节之一。

## 29.5 超时和安全输出

遥控器失联时，最危险的行为是：

```text
保持上一帧控制量不变
```

当前 Remote 会在超时路径发布零值或安全状态。

因此控制线程收到的不是：

```text
“上一帧还能继续用”
```

而是：

```text
“当前输入已经失效”
```

这使失联处理变成数据契约的一部分，而不是每个业务线程各自猜测。

## 29.6 新增协议的成本

新增一个协议主要需要：

```text
实现 RemoteProtocol
定义 Validate
定义 Decode
提供 frame_size
提供 line_cfg
使用 REGISTER_REMOTE
打开对应 Kconfig
```

不需要修改：

```text
Remote 主状态机
Remote 线程
底盘线程
云台线程
```

这就是插件注册机制在协议领域的直接收益。

## 29.7 Remote 子树的厉害之处

它把“多协议兼容”从一堆条件编译，变成：

```text
统一接口
本地插件
数据化注册
状态机调度
安全超时
```

这套结构既方便扩展，也方便诊断：

```text
当前锁定了哪个协议？
已命中几次？
连续失败多少次？
当前线路参数是什么？
最后一次有效帧何时到达？
```

这些状态都可以成为后续调试输出。

---

# 第三十部分 电机与功率计子树

## 30.1 电机模块的边界

当前电机模块位于：

```text
framework/modules/motors/
```

其中包括：

```text
DjiC610
DjiC620
DM motor
```

电机模块负责：

```text
反馈帧解析
状态快照
控制帧打包
单位转换
错误状态
```

它不负责：

```text
底盘运动学
云台模式
比赛策略
功率预算的最终决策
```

## 30.2 电机反馈不是裸全局变量

电机反馈通常包含：

```text
角度
速度
电流
温度
在线状态
```

这些数据来自异步 CAN 接收。

控制线程则以周期方式读取。

如果直接共享多个独立变量，就可能读到：

```text
新角度 + 旧速度 + 新电流 + 旧温度
```

当前模块使用 seqlock 风格快照来降低这种风险。

读取者可以：

```text
读取版本
复制数据
再次读取版本
版本一致则接受
版本变化则重试
```

这是低开销实时数据交换的一种实用方式。

## 30.3 DJI 电机差异

DJI C610 和 C620 的共同点是：

```text
CAN 反馈帧
目标电流控制
状态字段
```

差异可能出现在：

```text
控制器能力
电流范围
参数语义
设备编号
```

将它们放在同一模块族中，可以复用公共解析结构。

产品层只需要组合：

```text
电机实例
CAN ID
方向
减速比
控制参数
```

## 30.4 DM 电机差异

DM 电机通常还需要：

```text
特殊位置编码
特殊帧格式
模式控制
使能/停止
```

这些属于设备协议。

它们应该留在 `dm` 模块，而不是出现在：

```text
trd_chassis.cpp
trd_gimbal.cpp
```

业务线程只表达：

```text
目标位置
目标速度
目标力矩
控制模式
```

## 30.5 功率计模块

功率计模块负责从 CAN 或其他输入得到：

```text
电压
电流
功率
能量
状态
```

它与 `PowerCtrl` 的关系是：

```text
powermeter
    -> 提供实测功率

PowerCtrl
    -> 根据模型和预算分配控制输出
```

实测数据和预测模型是两个不同来源。

架构将它们分开，便于判断：

```text
模型错了
还是测量错了
还是执行器没有按要求输出
```

## 30.6 电机输出链

一个典型链路是：

```text
底盘控制线程
    -> 计算各轮目标
        -> 电机模块打包
            -> to_can_tx 队列
                -> CAN TX 线程
                    -> 驱动 Send
```

反馈方向则相反：

```text
CAN RX
    -> 电机模块解析
        -> seqlock snapshot
            -> 底盘/云台控制线程读取
```

输入和输出分别有明确的所有权。

## 30.7 电机子树的厉害之处

电机模块把最容易扩散的协议细节收住了。

底盘线程可以专注于：

```text
运动学
闭环控制
功率限制
故障降级
```

而不是同时处理：

```text
CAN 字节偏移
端序
ID 编号
饱和编码
反馈解码
```

这使同一套控制策略可以在不同电机族之间演进。

---

# 第三十一部分 topic 数据契约子树

## 31.1 topic 的定位

当前 topic 子树位于：

```text
framework/topic/
```

它不是“全局变量集中地”。

它的职责是定义跨模块、跨线程的数据契约：

```text
数据类型是什么
谁发布
谁消费
是否需要队列
是否需要 zbus
消息是否允许丢失
```

## 31.2 zbus 适合什么

当前 `imu_to`、`remote_to` 等主题使用 zbus。

zbus 适合表达：

```text
最新状态
广播消息
多个消费者
不希望消费者直接知道发布者
```

例如遥控器状态：

```text
Remote 发布一次
底盘订阅
云台订阅
其他状态模块也可以订阅
```

发布者不需要逐个调用消费者。

这减少了模块之间的直接依赖。

## 31.3 msgq 适合什么

当前 CAN TX 使用：

```text
k_msgq
```

消息队列更适合：

```text
有顺序的离散消息
生产者和消费者速度不同
需要显式队列深度
发送动作由专门线程完成
```

zbus 和 msgq 不是互相替代。

可以这样理解：

```text
zbus -> “现在最新状态是什么”
msgq -> “接下来要处理哪些消息”
```

## 31.4 topic 的所有权

每个 topic 都应该明确：

```text
谁发布
谁订阅
谁负责默认值
超时如何表示
单位是什么
线程上下文是什么
```

如果一个消息结构没有所有权说明，使用者很容易出现：

```text
多个线程同时写
消费者把旧值当新值
超时被误认为零值
单位被重复转换
```

数据契约是架构边界，不是变量命名问题。

## 31.5 Remote topic

Remote 输出应该包含：

```text
通道值
开关值
鼠标/键盘状态
连接状态
时间戳或有效性
```

即使当前结构较小，也要保持：

```text
安全状态可表达
无数据状态可表达
解码成功和链路在线可区分
```

这会直接影响底盘和云台的失联策略。

## 31.6 IMU topic

IMU 输出可能包含：

```text
加速度
角速度
姿态四元数
欧拉角
温度
校准状态
有效性
```

需要特别注意：

```text
样本时间
坐标系
单位
偏置是否已经扣除
姿态是否已经归一化
```

如果这些信息没有进入契约，消费者只能依赖隐含约定。

## 31.7 CAN TX topic

当前 CAN TX 消息包含：

```text
tx_id
data[8]
```

它是一个低层消息契约。

电机模块可以把设备协议编码为帧。

业务线程也可以直接组织某些产品帧。

但是长期维护时，应逐渐明确：

```text
哪些帧属于设备模块
哪些帧属于产品控制
谁负责 ID 分配
队列满时如何处理
```

架构允许演进，不代表契约天然完善。

## 31.8 topic 的厉害之处

topic 把线程连接从：

```text
直接调用
直接引用
共享全局变量
```

变成：

```text
通过命名契约传递状态或消息
```

这让系统具备：

```text
替换发布者
增加消费者
离线回放
单元测试
故障注入
```

的空间。

---

# 第三十二部分 cmd 横切能力子树

## 32.1 cmd 的位置

当前 cmd 子树位于：

```text
framework/cmd/
```

它的特点是：

```text
不属于单个设备
但会影响多个架构层
```

当前可见能力包括：

```text
build
linker
shell
buzzer
flash
```

## 32.2 Shell 的价值

Shell 是运行时观察架构的窗口。

它可以用于：

```text
查看状态
修改参数
执行测试
读取设备信息
验证通信
```

如果没有 Shell，很多故障只能依赖重新编译和重新烧录。

Shell 不是业务逻辑的替代品。

它更像是：

```text
运行时检查点
诊断入口
实验工具
```

## 32.3 Buzzer 的价值

蜂鸣器看起来是输出设备。

但在启动架构中，它可以承担：

```text
阶段到达提示
高等级初始化失败提示
设备 ready 提示
调试节拍提示
```

当前 `HandleInitFail` 对高等级失败使用错误提示并停机。

这意味着没有串口日志时，系统仍可能提供最小故障信号。

横切能力的价值就是让多个模块共享这种行为。

## 32.4 Flash 能力

Flash 子树可以承载：

```text
参数保存
标定数据
设备信息
运行记录
```

它应该与具体业务参数结构保持边界。

更好的演进方式是：

```text
flash 驱动负责存储介质
参数模块负责版本和校验
业务模块负责参数语义
```

这样升级参数格式时，不需要让 SPI 或底层 flash 了解业务。

## 32.5 linker 子树

链接脚本位于：

```text
framework/cmd/linker/dust_init.ld
```

它不是普通工具脚本。

它是运行时注册机制的编译期一半。

没有它：

```text
REGISTER_INIT 只能生成孤立对象
section 起止符号不会可靠存在
启动器无法遍历注册表
```

因此 linker 属于架构基础设施。

## 32.6 build 脚本的现实边界

当前 build 脚本提供：

```text
工作目录切换
board_cfg 选择
overlay/conf 扫描
west build 调用
```

脚本属于开发装配工具。

它不应该被误认为：

```text
运行时启动器
Kconfig 本身
CMake 本身
```

脚本、CMake、Kconfig 的职责要分开。

## 32.7 cmd 的厉害之处

cmd 子树把横切能力集中起来，避免：

```text
每个业务线程自己写一套蜂鸣器提示
每个模块自己写一套 Flash 访问
每个项目自己复制链接脚本
每个开发者自己猜构建命令
```

它是“基础设施的基础设施”。

---

# 第三十三部分 project/thread 业务线程子树

## 33.1 project/thread 的真正定位

用户工作区最重要的应用内容是：

```text
boards
thread
```

其中 `thread` 是产品语义最集中的地方。

当前维护者验证工作区的线程位于：

```text
project/thread/
```

它负责把公共能力组合成一个具体产品：

```text
遥控器
底盘
云台
IMU
CAN
PC/USB
GPIO
PWM
测试
```

线程层不应该重新实现：

```text
UART DMA
CAN 驱动
IMU 寄存器
Remote 协议探测
电机帧解析
链接段遍历
```

它应该调用已经存在的公共接口。

## 33.2 Thread 模板

当前 `thread.hpp` 提供线程模板，封装：

```text
k_thread_create
线程栈
线程入口
线程优先级
线程对象生命周期
```

这使业务线程的表达更接近：

```cpp
Thread<StackSize, Priority> chassis_thread;
```

而不是每个线程都重复：

```cpp
K_THREAD_STACK_DEFINE(...)
struct k_thread ...
k_thread_create(...)
```

线程模板不能替业务选择优先级。

它只把底层创建流程统一起来。

## 33.3 当前线程的组合关系

当前项目线程可以按功能理解为：

```text
trd_gpio
    -> GPIO 输出/输入测试

trd_remote
    -> UartDma + Remote

trd_can_tx
    -> Can + TX 队列 + RX callback

trd_imu
    -> ImuManager + heater + attitude

trd_chassis
    -> remote topic + 运动学 + PID + PowerCtrl + CAN TX

trd_gimbal
    -> remote topic + DM motor + CAN TX

trd_pc
    -> USB Stream + echo/PC 通信

trd_test
    -> PWM 测试
```

每一条线程都是一个产品接线点。

公共模块则是被接线的能力。

## 33.4 初始化和启动分离

以 IMU 线程为例：

```text
EarlyInit
    -> imu_.Init(...)

LateThread
    -> imu_.Start(...)
```

这种结构比在线程入口里做所有工作更清楚。

初始化阶段负责：

```text
设备检查
资源准备
预热
标定
初始状态
```

线程启动阶段负责：

```text
建立周期
进入循环
开始产生实时输出
```

业务线程可以依赖：

```text
Init 已经完成
Start 才进入周期执行
```

## 33.5 底盘线程的真实职责

当前底盘线程的核心链路是：

```text
读取 remote topic
    -> 判断控制输入
        -> 运动学计算
            -> 级联 PID
                -> PowerCtrl
                    -> 生成电机目标
                        -> 写入 CAN TX 队列
```

这正是 thread 层应该做的事情：

```text
把多个公共能力组合成当前产品策略
```

它不应该把：

```text
遥控器帧解析
电机 CAN 偏移
功率模型矩阵
```

全部塞成一段无法复用的巨大函数。

## 33.6 云台线程的真实职责

云台线程通常需要：

```text
读取控制输入
读取姿态或电机反馈
选择模式
计算目标
发送执行命令
```

它可以组合：

```text
Remote topic
DM motor module
PID
Quaternion/IMU output
CAN TX queue
```

但公共模块仍然拥有自己的边界。

云台线程只拥有：

```text
当前产品的目标和模式
```

## 33.7 线程优先级的含义

线程优先级不是装饰信息。

它体现：

```text
数据新鲜度
控制闭环紧急程度
调试任务可延迟程度
```

常见考虑是：

```text
传感器采集和控制闭环较高
通信转发居中
Shell、echo、测试较低
```

实际优先级仍需要结合：

```text
采样频率
最坏执行时间
阻塞行为
中断负载
总线负载
```

不能只凭线程名字判断。

## 33.8 用户线程的最低契约

一个新的用户线程至少应当说明：

```text
它消费什么
它发布什么
它使用什么硬件
它的周期是多少
它的优先级是多少
它如何处理初始化失败
它是否允许阻塞
它如何退出或降级
```

如果这些问题没有答案，线程虽然可能能跑，但还没有成为可维护的架构叶子。

---

# 第三十四部分 端到端数据流

## 34.1 为什么要画端到端数据流

目录树只能回答：

```text
代码放在哪里
```

端到端数据流回答：

```text
数据从哪里来
经过谁解释
通过什么契约传递
由谁消费
最后影响什么输出
```

控制系统的问题通常不发生在一个文件里。

它们发生在：

```text
生产者和消费者之间
```

因此必须画完整链路。

## 34.2 启动数据流

启动阶段的控制流是：

```text
framework/init/main.c
    -> System_Startup()
        -> RunStage(PreInit)
        -> RunStage(PreThread)
        -> RunStage(EarlyInit)
        -> RunStage(EarlyThread)
        -> RunStage(MidInit)
        -> RunStage(MidThread)
        -> RunStage(LateInit)
        -> RunStage(LateThread)
        -> RunStage(AppInit)
        -> RunStage(AppThread)
```

每个 `RunStage` 都遍历链接器提供的注册范围。

因此启动数据流同时依赖：

```text
CMake 把对象编进来
宏把对象放进 section
链接器保留 section
启动器找到 start/end
阶段匹配函数
```

## 34.3 遥控器到电机的链路

完整链路是：

```text
遥控器物理信号
    -> remote_uart alias
        -> UartDma
            -> BipBuffer
                -> Remote protocol Validate
                    -> Remote Decode
                        -> Remote state
                            -> remote_to
                                -> chassis/gimbal thread
                                    -> PID/模式/运动学
                                        -> motor command
                                            -> to_can_tx
                                                -> Can::Send
                                                    -> 电机
```

任何一层都可能造成“电机没动作”。

因此不能只修改最后的发送函数。

## 34.4 电机反馈到控制闭环

反向链路是：

```text
电机
    -> CAN controller
        -> CAN RX callback
            -> motor ID dispatch
                -> Dji/DM parser
                    -> seqlock Snapshot
                        -> chassis/gimbal thread
                            -> feedback PID
                                -> new command
```

这条链路体现：

```text
异步硬件输入
    -> 稳定快照
        -> 周期控制读取
```

快照是中间的时间一致性边界。

## 34.5 IMU 到姿态消费者

IMU 链路可以写成：

```text
SPI device tree
    -> SPI driver
        -> BMI088/ICM42688P source
            -> CalibSource
                -> heater/temperature control
                    -> QuaternionEKF
                        -> imu output
                            -> imu topic or module API
                                -> gimbal/control consumer
```

这里的关键是：

```text
原始采样
和
姿态结果
```

不是同一层数据。

消费者必须知道自己拿到的是：

```text
raw sample
calibrated sample
attitude estimate
```

## 34.6 PC/USB 链路

PC 通信链路是：

```text
主机
    -> USB enumeration
        -> CDC ACM endpoints
            -> UsbDevPort
                -> Usb Stream adapter
                    -> trd_pc
                        -> echo/command/protocol
```

底层 reset 时：

```text
USB reset
    -> 清理队列
        -> 重新配置
            -> 重新投递 RX
```

上层线程不应该自己重建端点。

## 34.7 数据类型和所有权表

| 数据 | 生产者 | 中间契约 | 消费者 | 主要风险 |
| --- | --- | --- | --- | --- |
| 遥控器状态 | Remote | zbus `remote_to` | 底盘、云台 | 超时和旧值 |
| IMU 状态 | ImuManager | zbus或模块输出 | 姿态、控制 | 单位和时间 |
| 电机反馈 | CAN RX/电机模块 | Snapshot | 控制线程 | 半更新 |
| CAN 发送 | 底盘、云台 | `k_msgq` | CAN TX 线程 | 队列满 |
| USB 字节 | USB port | RxQueue/Stream | PC 线程 | reset和busy |
| GPIO输入 | GPIO driver | 状态接口 | 测试/业务 | 去抖 |

这张表比“全局变量名字”更能说明系统如何工作。

## 34.8 数据流诊断的基本方法

沿着数据流逐层打点：

```text
raw received
    -> parsed
        -> state updated
            -> topic published
                -> consumer woke
                    -> control computed
                        -> output queued
                            -> driver accepted
```

每一层都应该能回答：

```text
最后一次成功时间
当前数据长度
当前状态
失败原因
```

这样“没动作”可以被拆成具体问题。

---

# 第三十五部分 运行时顺序与开发顺序

## 35.1 运行时顺序

当前运行时启动顺序来自 `System_Startup()`：

```text
PreInit
    -> 等待
PreThread
    -> 等待
EarlyInit
    -> 等待
EarlyThread
    -> 等待
MidInit
    -> 等待
MidThread
    -> 等待
LateInit
    -> 等待
LateThread
    -> 等待
AppInit
    -> 等待
AppThread
```

这里的阶段顺序是运行事实。

不是文档作者喜欢的理论顺序。

## 35.2 开发顺序

开发一个新功能时，通常不应该从 `main()` 开始。

更合理的开发顺序是：

```text
硬件契约
    -> 驱动
        -> 设备模块
            -> topic/快照
                -> 算法
                    -> 业务线程
                        -> 产品验证
```

这是依赖关系决定的。

## 35.3 两种顺序为什么不同

运行时先执行什么，取决于：

```text
启动阶段
资源可用性
实时需求
故障策略
```

开发时先写什么，取决于：

```text
接口依赖
验证成本
数据是否可观测
硬件是否已经稳定
```

因此：

```text
运行时 EarlyInit 先做 IMU
不代表开发时必须先写 IMU
```

也不代表：

```text
AppThread 最后运行
所以必须最后才设计
```

架构阅读和架构开发要分别建模。

## 35.4 依赖驱动的开发顺序

例如新增一个 DM 云台：

```text
1. 确认 CAN 帧格式
2. 确认板卡 CAN alias
3. 确认 Can::Init 和 filter
4. 实现 DM parser
5. 实现 Snapshot
6. 实现发送打包
7. 定义或复用 topic
8. 实现云台 thread
9. 注册 Init/Thread
10. 做端到端验证
```

这样每一步都有可观察结果。

## 35.5 架构成熟度的判断

不能只看：

```text
目录是否齐全
类是否存在
宏是否存在
```

还要看：

```text
构建是否接入
注册是否保留
硬件是否 ready
线程是否启动
输出是否可观测
故障是否可定位
```

当前文档因此区分：

```text
架构能力已经提供
当前产品已经接线
当前硬件已经验证
```

---

# 第三十六部分 如何新增一个子树叶子

## 36.1 先判断它属于哪一层

新增代码前先问：

```text
它是硬件访问吗？
它是纯数学吗？
它是设备协议吗？
它是跨线程数据吗？
它是启动机制吗？
它是产品策略吗？
```

对应关系：

```text
硬件访问 -> drivers
纯数学 -> algorithm
设备协议 -> modules
跨线程数据 -> topic
启动机制 -> init/cmd linker
产品策略 -> 用户 workspace/thread
```

如果答案是“只是当前产品的组合”，不要把它放进公共模块。

## 36.2 叶子节点最小契约

一个新的公共叶子至少需要：

```text
目录边界
头文件接口
实现文件
Kconfig 门禁
CMake 门禁
错误返回
生命周期说明
使用示例
```

如果使用注册机制，还需要：

```text
注册结构
注册宏
section
start/end 符号
启动遍历或分派
```

## 36.3 先写契约再写实现

先写一页接口说明：

```text
输入是什么
输出是什么
谁拥有状态
是否阻塞
线程上下文是什么
初始化失败如何处理
是否允许多个实例
```

这会提前暴露：

```text
模块边界不清
生命周期缺失
数据所有权冲突
```

## 36.4 叶子进入系统的四道门

公共叶子要进入产品，通常经过：

```text
Kconfig
    -> 功能是否可选

CMake
    -> 源文件是否编译

链接器
    -> 注册对象是否保留

启动/线程
    -> 注册对象是否执行
```

任何一道门没有接上，功能都可能“看起来存在，实际没有运行”。

## 36.5 叶子文档要求

每个公共叶子最好说明：

```text
源码路径
配置符号
构建入口
依赖设备树
运行阶段
输出契约
失败策略
验证命令或日志
```

这能让下一个维护者不必从零追踪。

---

# 第三十七部分 如何新增一块板卡

## 37.1 板卡工作的最小范围

普通用户新增板卡时，优先处理：

```text
board 身份
设备树节点
alias
chosen
conf
runner
```

不应该因为换了 UART 就复制一份 UART 驱动。

不应该因为换了 IMU SPI 就复制一份 IMU 管理器。

## 37.2 板卡接入顺序

推荐顺序：

```text
1. 确认 Zephyr board/soc 名称
2. 创建用户 boards 目录
3. 添加 overlay
4. 添加 conf
5. 添加必要的 board.cmake
6. 定义 alias
7. 定义 chosen
8. 让 CMake 指向 BOARD_ROOT
9. 检查 DT_ALIAS 是否能被编译
10. 再接入 thread
```

## 37.3 alias 命名规则

业务 alias 应该表达功能：

```text
user-can1
remote-uart
imu-spi
imu-pwm
buzzer-pwm
flash-spi
pc-usb
```

不要让业务线程直接依赖：

```text
mcan0
uart4
spi2
pwm0
```

功能名比控制器编号更稳定。

## 37.4 board 与 thread 的配合

板卡只描述：

```text
硬件在哪里
硬件叫什么
硬件是否启用
硬件参数是什么
```

线程描述：

```text
什么时候使用
如何初始化
谁消费它
如何形成产品行为
```

如果 overlay 开了设备但没有 thread，设备可能只是可用。

如果 thread 引用了 alias 但 overlay 没有定义，构建或运行会失败。

## 37.5 换板验证清单

```text
alias 是否存在
节点 status 是否 okay
chosen 是否正确
设备是否 ready
时钟和 pinctrl 是否正确
DMA 是否可用
中断是否到达
线程 Init 是否返回成功
```

这比只看“编译通过”完整得多。

---

# 第三十八部分 如何新增一个驱动

## 38.1 驱动新增的边界

新增驱动时，先确定它只负责：

```text
设备访问
缓冲管理
事件转换
状态返回
```

不要在驱动里加入：

```text
产品控制策略
设备业务模式
底盘或云台语义
```

## 38.2 驱动实现步骤

```text
1. 定义上层最小接口
2. 明确阻塞和非阻塞语义
3. 加入 Kconfig
4. 加入 CMake
5. 取得 device/DT spec
6. 检查 ready
7. 实现 Init/Start/Stop
8. 实现回调或轮询
9. 处理错误和重启
10. 补充使用者工作区接线示例
```

## 38.3 驱动回调原则

回调路径应尽量：

```text
短
确定
不阻塞
不做大计算
只搬运或记录必要状态
```

协议解析和控制计算放到线程中。

## 38.4 驱动失败语义

至少区分：

```text
设备不存在
设备未 ready
参数非法
资源忙
超时
硬件错误
队列满
```

调用方才能决定：

```text
重试
降级
报警
停机
```

## 38.5 驱动新增后的接线检查

```text
Kconfig 是否能关闭
CMake 是否随配置裁剪
DT alias 是否有来源
Init 是否被 thread 调用
错误是否进入日志
停止后是否可重新启动
```

---

# 第三十九部分 如何新增一个设备模块

## 39.1 设备模块与驱动的分界

新增设备模块时，模块负责：

```text
协议
寄存器语义
数据单位
状态机
设备特有恢复
```

它调用：

```text
Stream
Can
Spi
Gpio
Pwm
```

但不绕过这些边界。

## 39.2 模块接口

模块接口应优先表达设备语义：

```text
Init
Start
Read
GetState
GetSnapshot
SetTarget
Calibrate
```

而不是把底层帧字节直接暴露给业务线程。

## 39.3 是否需要注册

如果系统需要：

```text
多个设备实现可替换
运行时遍历候选
按配置选择唯一实例
```

可以使用：

```text
REGISTER_IMU
REGISTER_REMOTE
```

如果只有一个明确产品实例，也可以由用户 thread 显式构造。

注册机制不是越多越好。

关键是选择与变化类型匹配。

## 39.4 模块的故障状态

模块至少要能表达：

```text
ready
not ready
timeout
invalid data
calibrating
degraded
```

不要让 thread 从一个模糊的 `false` 猜测所有故障。

---

# 第四十部分 如何新增一个业务线程

## 40.1 线程属于使用者工作区

只要功能是产品组合，就优先放在：

```text
用户工作区/thread/
```

例如：

```text
底盘控制
云台控制
测试线程
日志转发
产品专用状态机
```

## 40.2 线程新增步骤

```text
1. 明确输入 topic/模块
2. 明确输出 topic/队列/设备
3. 确定周期
4. 确定优先级
5. 确定栈大小
6. 写 Init 函数
7. 写 Start 函数
8. 使用 REGISTER_INIT
9. 使用 REGISTER_THREAD
10. 加入用户 CMake
11. 验证启动日志
```

## 40.3 线程不应拥有的东西

线程不应拥有：

```text
公共设备协议实现
多个产品共享的寄存器表
公共链接器注册逻辑
重复的底层缓冲管理
```

否则下一位用户会复制线程，而不是复用能力。

## 40.4 线程的实时性检查

```text
循环周期是否可测
是否存在无限阻塞
是否在高优先级线程打印大量日志
是否会访问未初始化 topic
是否能处理输入超时
是否会让队列持续满
```

线程是业务层，但仍然是实时系统的一部分。

---

# 第四十一部分 如何新增一个协议插件

## 41.1 以 Remote 为例

新协议优先实现：

```text
RemoteProtocol
```

然后提供：

```text
Validate
Decode
line_cfg
frame_size
priority
need_hits
```

最后使用：

```text
REGISTER_REMOTE
```

## 41.2 协议插件不修改主状态机

协议插件不应修改：

```text
Remote detecting/locked 状态机
超时策略
公共 zbus 发布逻辑
用户底盘线程
```

它只提供：

```text
自己的帧知识
```

这样新协议的风险被限制在插件叶子。

## 41.3 协议验证样本

至少准备：

```text
合法帧
错误校验帧
短帧
长帧
随机噪声
连续帧
协议切换后的首帧
```

尤其要验证：

```text
旧协议残留字节不会误锁定新协议
```

## 41.4 协议状态输出

调试时应能观察：

```text
当前候选
当前锁定协议
连续命中数
连续失败数
最后有效帧时间
当前线路配置
```

协议插件越多，状态可见性越重要。

---

# 第四十二部分 如何新增一个算法

## 42.1 算法先保持硬件无关

新增算法时先把输入限制在：

```text
标量
向量
矩阵
时间
参数
状态
```

不要直接引用：

```text
device
CAN
UART
具体板卡 alias
```

如果算法必须接触硬件，它可能属于模块或控制线程组合层。

## 42.2 算法状态的所有权

算法对象应该拥有：

```text
积分项
滤波状态
协方差
估计偏置
模型参数
```

业务线程拥有：

```text
何时 reset
何时启用
何时限幅
何时降级
```

## 42.3 算法验证

算法验证可分为：

```text
纯输入输出测试
边界参数测试
随机噪声测试
时间步长变化测试
饱和测试
真实回放测试
```

先验证数学行为，再接入真实硬件。

## 42.4 算法的可替换接口

如果两个算法都能实现：

```text
Reset
Update
Output
```

业务层就有机会在不改变线程结构的情况下更换实现。

这比把公式直接嵌入 thread 更容易维护。

---

# 第四十三部分 如何新增一个 topic

## 43.1 先判断数据类型

需要先选择：

```text
最新状态 -> zbus
离散消息 -> msgq
高频一致快照 -> Snapshot/seqlock
单调用结果 -> 接口返回值
```

不要所有数据都做成 zbus。

## 43.2 topic 契约内容

新 topic 应说明：

```text
消息结构
单位
坐标系
时间戳
有效位
发布者
消费者
默认值
超时语义
```

## 43.3 topic 兼容性

修改公共 topic 时要考虑：

```text
新增字段是否保持旧字段语义
字段单位是否变化
消息大小是否影响队列
发布者是否仍能初始化默认值
消费者是否会读取未初始化字段
```

公共 topic 是父级子树最需要审查的接口之一。

## 43.4 topic 的测试

可以验证：

```text
发布者不存在时消费者行为
超时时的安全值
多个消费者并发读取
队列满时的返回值
快照读写一致性
```

---

# 第四十四部分 如何新增一个 cmd 能力

## 44.1 cmd 能力的判断

如果能力被多个模块或项目使用，并且不属于单一产品，可以考虑放入：

```text
framework/cmd/
```

例如：

```text
Shell 命令
蜂鸣器错误码
Flash 参数工具
构建辅助
链接段基础设施
```

如果只是一个产品的临时测试命令，优先留在用户工作区。

## 44.2 Shell 命令的边界

Shell 命令应该：

```text
读取状态
设置参数
触发测试
输出诊断
```

不应该在命令处理函数里长期阻塞整个系统。

长操作可以：

```text
投递消息
启动任务
返回进度
```

## 44.3 cmd 能力的可裁剪性

每个公共 cmd 能力都应有：

```text
Kconfig 符号
CMake 门禁
依赖说明
资源开销说明
```

调试功能不应无条件进入所有产品镜像。

## 44.4 cmd 和用户工作区

用户可以在自己的 `thread/` 或 app 目录添加产品命令。

当命令具有：

```text
多个产品复用
稳定接口
通用诊断价值
```

再提升到维护者的 `framework/cmd`。

---

# 第四十五部分 启动问题证据链

## 45.1 启动问题的第一原则

看到“没有启动日志”时，不要直接猜某个模块。

先验证：

```text
入口
构建
配置
链接
注册
阶段
硬件
```

## 45.2 入口检查

当前真实入口是：

```text
framework/init/main.c
```

它调用：

```text
System_Startup()
```

因此首先检查：

```text
该文件是否进入 app
target_sources 是否包含它
是否有其他 main 冲突
```

## 45.3 构建检查

检查：

```text
project/CMakeLists.txt
ZEPHYR_EXTRA_MODULES
BOARD_ROOT
DTS_ROOT
SOC_ROOT
add_subdirectory
```

如果用户工作区是同级目录，则检查：

```text
父级 zephyr_user 路径是否正确
用户 thread 是否被 add_subdirectory
用户 board 是否进入 BOARD_ROOT
```

## 45.4 配置检查

检查：

```text
PRJ_MAIN
目标线程开关
驱动开关
模块开关
底层 Zephyr 依赖
```

配置关闭时，后面的注册和线程都不会出现。

## 45.5 链接和注册检查

检查：

```text
.user_init
.user_thread
.remote
.imu
.can_rx1/.can_rx2/.can_rx3
KEEP
start/end symbols
```

有宏不代表有 section。

有 section 不代表启动阶段执行。

## 45.6 阶段检查

确认：

```text
注册项 stage 是否正确
Init 和 Thread 是否混淆
Start 是否在预期阶段
前置依赖是否已经完成
```

特别注意：

```text
初始化成功
不等于线程已经启动
```

## 45.7 失败等级检查

当前 `InitLevel` 需要结合实际行为理解：

```text
High
    -> 错误日志
    -> 蜂鸣器错误
    -> 停机

Mid/Low
    -> 警告
    -> 继续
```

因此“系统还在运行”不一定表示所有初始化都成功。

---

# 第四十六部分 数据不通问题证据链

## 46.1 数据问题的分层

“数据不通”至少可能是：

```text
物理层没有信号
驱动没有收到
缓冲没有增长
协议没有通过
模块没有更新
topic 没有发布
消费者没有唤醒
输出队列没有发送
```

每一层都要单独证明。

## 46.2 UART 数据链检查

```text
UART pin
    -> UART device ready
        -> async callback
            -> DMA buffer
                -> BipBuffer
                    -> sem_
                        -> Read
                            -> protocol Validate
```

调试时保留：

```text
原始字节长度
原始帧摘要
解析状态
超时计数
锁定协议
```

不要只输出：

```text
parse failed
```

## 46.3 CAN 数据链检查

```text
CAN pin/transceiver
    -> device ready
        -> filter
            -> can_start
                -> IRQ
                    -> callback
                        -> ID dispatcher
                            -> module parser
```

注册 section 只能证明最后一段的静态能力。

必须继续验证：

```text
硬件是否真的收帧
```

## 46.4 topic 数据链检查

```text
module state update
    -> zbus publish or queue put
        -> consumer subscription/read
            -> consumer local state
```

检查：

```text
发布返回值
topic 名称
消息结构
连接/订阅
消费者线程状态
```

## 46.5 输出链检查

```text
control result
    -> frame pack
        -> k_msgq_put
            -> TX thread
                -> Can::Send
                    -> hardware mailbox
```

要记录：

```text
队列深度
put 返回值
send 返回值
busy 状态
总线错误
```

## 46.6 数据问题的停止条件

当用户确认某一层已经解决时，后续分析应把它视为工作真相。

例如：

```text
PC 原始串口输出已确认
```

就不应反复回到：

```text
怀疑 MCU 完全没有发送
```

除非出现新的相反证据。

这也是证据链调试比猜测更高效的原因。

---

# 第四十七部分 当前源码中的接线状态

## 47.1 已确认的入口和装配

当前源码已经确认：

```text
真实 main 在 framework/init/main.c
业务构建入口在 project/CMakeLists.txt
公共代码位于 framework/
平台兼容代码位于 platform/
用户板卡扩展位于 boards/ 和 project/boards/
```

根目录没有当前应用用的 `CMakeLists.txt`。

直接在仓库根目录猜测构建入口会得到错误结论。

## 47.2 已确认的公共子树

当前 `framework/` 挂载：

```text
drivers
algorithm
modules
topic
cmd
init
```

这些子树已经体现了：

```text
驱动
算法
设备模块
数据契约
横切工具
启动和注册
```

## 47.3 已确认的注册能力

当前源码提供：

```text
REGISTER_INIT
REGISTER_THREAD
REGISTER_REMOTE
REGISTER_IMU
CAN_RX_HANDLER
```

对应链接段包括：

```text
.user_init
.user_thread
.remote
.imu
.can_rx1
.can_rx2
.can_rx3
```

这说明自动注册不是文档概念，而是实际编译链接能力。

## 47.4 当前仍需关注的接线点

以下内容需要区分“能力存在”和“当前产品是否已完全接通”：

```text
CAN_RX_HANDLER 的业务调用点需要持续核对
IMU 输出 topic 的发布路径需要确认
CAN TX Init 返回值应由调用方检查
不同板卡的 USB HAL 配置需要分别验证
用户同级工作区的 CMake 入口需要按项目落地
```

这些不是对架构能力的否定。

它们是当前产品接线的验证清单。

## 47.5 当前用户工作区形态的理解

当前仓库已经有：

```text
project/
```

它承担集成验证和示例产品的职责。

用户提出的同级工作区形态是进一步的组织边界：

```text
zephyr_user/
    -> 维护者公共子树

member_workspace/
    -> 用户 board/thread
```

这两种形态可以并存。

前者用于维护者验证。

后者用于多人并行开发。

## 47.6 当前文档的使用边界

本文描述的是：

```text
当前源码树已经存在的机制
加上推荐的父子工作区组织方式
```

它不应被误读为：

```text
所有同级用户工作区已经自动创建
所有公共修改已经由权限系统阻断
所有硬件路径已经完成实测
```

真正的权限和发布流程还需要：

```text
Git 仓库设置
团队协作规范
CI
板卡实测
```

## 47.7 这组状态为什么要写进架构文档

架构文档如果只写“未来应该怎样”，使用者会误以为已经完成。

如果只写“当前哪里能跑”，维护者又无法表达长期组织目标。

所以当前文档同时保留：

```text
当前源码证据
目标父子工作区
已知接线缺口
验证顺序
```

这才是一份能够指导团队工作的文档。

---

# 第四十八部分 维护边界与演进方向

## 48.1 公共模块进入维护区的条件

一段代码满足下面条件时，才适合提升到公共子树：

```text
至少两个工作区需要
接口可以稳定表达
生命周期可以解释
硬件依赖可以隔离
Kconfig/CMake 可以裁剪
有明确的所有权
```

如果只是一个产品临时需求，先放在用户工作区。

## 48.2 公共模块的版本策略

维护者需要记录：

```text
模块提交
父级 gitlink
兼容的 Zephyr 版本
兼容的 SDK glue
支持的板卡
已验证的配置
```

用户工作区应尽量使用固定版本，而不是随时追踪公共分支。

## 48.3 接口优先于实现

公共模块演进时优先保证：

```text
输入输出不随意变化
错误语义可追踪
单位和坐标系稳定
启动阶段不随意改变
注册结构有兼容策略
```

实现可以重写。

接口变化需要更高等级的评审。

## 48.4 维护者与用户的协作方式

推荐把问题分成三类：

```text
用户接线问题
公共模块缺陷
架构能力缺口
```

用户接线问题留在用户工作区解决。

公共模块缺陷进入维护者子树修复。

架构能力缺口需要先设计边界，再决定放在哪棵树。

## 48.5 防止子树重新膨胀

公共子树也可能重新膨胀。

需要持续检查：

```text
是否出现产品名变量
是否出现某台机器人专用分支
是否出现大量业务 ifdef
是否出现 thread 反向依赖产品细节
是否出现公共模块直接引用用户目录
```

一旦出现这些信号，应重新划分边界。

## 48.6 防止用户工作区过重

用户工作区也可能变成新的“大一统工程”。

需要避免：

```text
用户 thread 直接访问寄存器
用户 board 复制公共设备驱动
用户项目复制 init/linker
用户项目重复实现协议状态机
用户项目把所有模块重新打包
```

用户工作区应该保持：

```text
板卡配置
线程组合
产品策略
少量产品专用代码
```

## 48.7 文档和代码同步

每次公共边界变化，都应该同步：

```text
目录文档
配置说明
CMake 接入
设备树示例
注册说明
启动阶段
用户工作区迁移方式
```

尤其是下面这些变化：

```text
main 入口变化
framework 子树位置变化
board alias 变化
topic 结构变化
注册 section 变化
```

## 48.8 最终演进目标

这棵架构最终应当让新用户做到：

```text
拿到一个同级工作区模板
选择自己的板卡
写自己的 thread
通过 CMake 引入 zephyr_user
打开需要的 Kconfig
快速得到可运行产品
```

让维护者做到：

```text
集中维护公共模块
控制版本发布
修复一次，多个工作区受益
持续增加板卡和模块支持
保护启动、注册和数据契约
```

## 48.9 本部分结论

成熟的子树架构不是让所有人都能改所有代码。

它是让每个人：

```text
在自己的边界内拥有足够自由
在公共边界上遵守稳定契约
在需要公共能力时通过正式流程贡献
```

这正是维护者工作区和使用者工作区必须分开的原因。

---

# 附录 A 关键文件索引

## A.1 启动和注册

| 文件 | 作用 |
| --- | --- |
| `framework/init/main.c` | 当前真实 `main()` |
| `framework/init/Init_entry.hpp` | 阶段、注册结构、注册宏 |
| `framework/init/Init_entry.cpp` | 启动遍历和失败处理 |
| `framework/init/Irq_handlers.h` | CAN 接收注册宏 |
| `framework/init/Irq_handlers.cpp` | CAN 接收分派 |
| `framework/cmd/linker/dust_init.ld` | 注册 section 和起止符号 |

## A.2 构建和配置

| 文件 | 作用 |
| --- | --- |
| `project/CMakeLists.txt` | 当前应用装配入口 |
| `project/Kconfig` | 当前业务 Kconfig 入口 |
| `project/prj.conf` | 当前项目配置结果 |
| `framework/*/CMakeLists.txt` | 子树源码门禁 |
| `framework/*/Kconfig` | 子树能力门禁 |
| `framework/zephyr/module.yml` | framework module 元数据 |
| `platform/cmsis/zephyr/module.yml` | platform module 元数据 |

## A.3 板卡和设备树

| 文件或目录 | 作用 |
| --- | --- |
| `boards/` | 根级 Zephyr board 扩展 |
| `dts/bindings/` | 自定义设备树 binding |
| `project/boards/` | 当前项目板卡 overlay/conf |
| `project/boards/hpm/` | HPM 板卡配置 |
| `project/boards/st/` | STM32 板卡配置 |
| `project/boards/*/*.overlay` | alias、chosen、外设连接 |
| `project/boards/*/*.conf` | 板卡功能配置 |

## A.4 公共子树

| 目录 | 主要职责 |
| --- | --- |
| `framework/drivers` | 外设和通信访问 |
| `framework/algorithm` | 控制、滤波、估计、辨识 |
| `framework/modules` | IMU、Remote、电机、功率计 |
| `framework/topic` | zbus、msgq、跨线程契约 |
| `framework/cmd` | shell、buzzer、flash、构建、链接 |
| `framework/init` | main、阶段、注册表、IRQ |
| `platform` | 平台兼容和用户平台扩展 |

## A.5 用户工作区

推荐最小结构：

```text
<user_workspace>/
├── CMakeLists.txt
├── Kconfig
├── prj.conf
├── boards/
└── thread/
```

公共子树不复制进来。

---

# 附录 B 符号、配置与链接段映射

## B.1 配置到源码

```text
CONFIG_PRJ_MAIN
    -> framework + project/thread 总装配

CONFIG_DUST_COM_UART_DMA
    -> UartDma 源码

CONFIG_DUST_COM_CAN
    -> CAN 驱动源码

CONFIG_DUST_MOD_DEV_IMU_*
    -> 对应 IMU source

CONFIG_DUST_MOD_DEV_REMOTE
    -> Remote 模块和协议

CONFIG_DUST_TPC_IMU_TO
    -> IMU zbus topic

CONFIG_DUST_TPC_REMOTE_TO
    -> Remote zbus topic

CONFIG_DUST_TPC_TO_CAN_TX
    -> CAN TX msgq
```

## B.2 注册到链接段

```text
REGISTER_INIT
    -> .user_init

REGISTER_THREAD
    -> .user_thread

REGISTER_REMOTE
    -> .remote

REGISTER_IMU
    -> .imu

CAN_RX_HANDLER(..., 1)
    -> .can_rx1

CAN_RX_HANDLER(..., 2)
    -> .can_rx2

CAN_RX_HANDLER(..., 3)
    -> .can_rx3
```

## B.3 链接段到运行时

```text
section
    -> __section_start
    -> __section_end
        -> RunStage/dispatcher
            -> function call
```

这条链是当前自动注册系统的核心。

---

# 附录 C 架构检查清单

## C.1 新增公共模块

```text
[ ] 是否真的需要跨多个工作区复用
[ ] 是否选择了正确的 framework 子树
[ ] 是否有稳定头文件接口
[ ] 是否定义生命周期
[ ] 是否有 Kconfig
[ ] 是否有 CMake
[ ] 是否有错误返回
[ ] 是否需要注册
[ ] 是否需要链接段
[ ] 是否有文档和示例
[ ] 是否说明当前已验证和未验证部分
```

## C.2 新增用户工作区

```text
[ ] CMakeLists.txt 已创建
[ ] 正确引入 zephyr_user
[ ] 用户 board 目录已创建
[ ] 用户 thread 目录已创建
[ ] overlay 定义功能 alias
[ ] conf 打开必要能力
[ ] prj.conf 选择产品功能
[ ] thread 不直接访问公共寄存器
[ ] thread 使用公共 topic/模块
[ ] 构建入口路径已确认
[ ] 版本固定且可追踪
```

## C.3 新增板卡

```text
[ ] board 身份确认
[ ] DTS 节点 status 正确
[ ] alias 正确
[ ] chosen 正确
[ ] pinctrl 正确
[ ] DMA 正确
[ ] 时钟正确
[ ] 设备 ready
[ ] 驱动 Init 成功
[ ] thread 能够启动
[ ] 真实硬件路径已验证
```

## C.4 启动问题

```text
[ ] 是否进入 framework/init/main.c
[ ] System_Startup 是否执行
[ ] CMake 是否加入 init
[ ] PRJ_MAIN 是否打开
[ ] 注册 section 是否生成
[ ] KEEP 是否生效
[ ] start/end 是否存在
[ ] stage 是否匹配
[ ] InitLevel 是否导致停机
[ ] thread Start 是否执行
```

## C.5 数据不通

```text
[ ] 物理信号存在
[ ] device ready
[ ] callback 到达
[ ] raw buffer 增长
[ ] parser 收到完整帧
[ ] module state 更新
[ ] topic publish/queue put 成功
[ ] consumer 被唤醒
[ ] control 结果生成
[ ] output queue 未满
[ ] driver Send 接受
[ ] 硬件最终发出
```

## C.6 父子工作区边界

```text
[ ] 公共模块只在维护者工作区修改
[ ] 用户工作区只保留 board/thread 为主
[ ] 用户通过 CMake 引入公共子树
[ ] 公共版本由 git/submodule 固定
[ ] 公共修改经过评审
[ ] CMake 引入不被误认为权限控制
[ ] 文件系统/Git/CI 权限已经配置
[ ] 用户需求可以回流到维护者工作区
```

---

## 总结

这棵子树最值得保留的核心关系是：

```text
维护者维护公共能力
    -> CMake 对外提供能力
        -> 用户工作区选择板卡
            -> 用户工作区编写 thread
                -> framework 完成驱动、模块、算法和启动
                    -> 产品形成自己的行为
```

它让团队不必在“所有代码集中维护”和“所有项目各自复制”之间二选一。

公共能力集中。

产品组合分散。

版本可以追踪。

板卡可以替换。

线程可以自由演进。

而真正需要跨项目共享的能力，始终有一条清晰的回流路径：

```text
用户问题
    -> 维护者设计
        -> 公共子树实现
            -> 版本发布
                -> 多个用户工作区复用
```

这就是 `zephyr_user` 作为子树、维护者工作区和团队基础设施的真正价值。
