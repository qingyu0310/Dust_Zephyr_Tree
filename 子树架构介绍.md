# zephyr_user 子树架构介绍

> 文档版本：简洁版（2026-08-02）
> 适用路径：`E:\Zephyr\zephyr_user`
> 定位：先讲清楚每一层是干什么的，再讲层与层怎么配合。

---

## 1. 总览

`zephyr_user` 是一棵用户自研子树，与官方 `zephyr` 同级，用 `framework/`（架构层，6 个独立 submodule）+
`platform/`（平台兼容）+ `project/`（业务装配）把硬件、算法、设备、线程组合成可裁剪的产品。

```text
zephyr_user/
├── framework/              架构层（6 个独立 submodule）
│   ├── drivers/            硬件驱动层
│   ├── algorithm/          算法层
│   ├── modules/            设备模块层
│   ├── topic/              数据契约层
│   ├── cmd/                横切辅助层
│   └── init/               启动层
├── platform/cmsis/         平台兼容层
├── boards/                 用户自研板卡
├── dts/bindings/           用户自研设备树 binding
├── drivers/                子树驱动层（板卡专属底层驱动，Zephyr 模块式）
└── project/                业务装配层（顶层应用工程）
```

核心思想一句话：**每个功能在自己的叶子节点声明一次，Kconfig 决定它是否存在，CMake 决定它是否进镜像，
链接器决定它如何进注册表，启动器决定它何时运行，上层只依赖稳定接口。**

---

## 2. 各层是干什么的

### 2.1 `framework/drivers` —— 硬件驱动层

**干什么**：把 Zephyr 的设备树对象和外设 API 封装成项目自己的 C++ 驱动对象，供上层持有和调用。

**两种形态**：
- **轻量外设封装**：GPIO（Input/Output）、PWM、SPI、CAN——纯 Zephyr API 包装，不建线程、不解析协议。
- **异步通信驱动**：UART（UartDma）、RS485、USB——处理 DMA 双缓冲、回调、软件队列、发送忙状态、信号量。

**提供**：`Input`/`Output`、`Pwm`、`Spi`、`Can`、`UartDma`、`Rs485`、`usb::Usb`，统一抽象接口 `Stream`
（`Read`/`Send` + 接收信号量）。

**边界**：只做设备 ready 检查、外设读写、DMA/回调/缓冲管理、错误返回。**不负责**协议解析、topic 发布、
控制算法、机器人业务。

**与子树 `drivers/` 的分工**：`framework/drivers` 封装**通用外设对象**，跨板卡共用；**板卡专属
底层驱动放子树 `drivers/`**（`zephyr_user/drivers/`，Zephyr 模块式，见 §2.8）。例如 USB：协议栈
中间层（`UsbHal` 接口 + `UsbDevPort`/`UsbCdcAcm` + `Usb` Stream 适配）留在 framework，HPM 板卡
底层（`UsbHalHpm`）下沉到 `zephyr_user/drivers/usb/hal/hpm/`。换板卡只动子树，架构层稳定。

### 2.2 `framework/algorithm` —— 算法层（纯计算）

**干什么**：提供纯数学/控制/估计能力，**零硬件依赖**，可在仿真或离线环境独立测试。

**提供**：
- 控制器：`alg::pid::Pid`（位置式 PID）、`PowerCtrl<N>`（功率分配）、软件定时器
- 滤波/估计：LPF、HPF、模板化 `Kalman<N,M>` / `ExtendedKalman<N,M>`、`QuaternionEKF`（四元数姿态）
- 辨识：`RLS<n>`（递归最小二乘）、`MotorPlant`（电机本体辨识）、稳定判据
- 基础：`BipBuffer`/`RingBuf` 缓冲、内置 Eigen 线性代数库

**边界**：只做数学计算和状态更新。**不创建线程、不持有硬件句柄**。

### 2.3 `framework/modules` —— 设备模块层

**干什么**：把一个设备所需的驱动访问 + 协议解析 + 状态管理 + 必要算法，组合成可被项目直接使用的
**设备能力对象**。位于 `drivers` 和 `project` 之间。

**提供**：
- `imu/`：IMU 采样、加热、姿态（`ImuManager`），发布 `topic::imu_to`
- `remotes/`：遥控器接收机，自动探测锁定协议（DR16/SBUS/VT12/VT13），发布 `topic::remote_to`
- `motors/`：DJI C6xx / DM 电机（CAN 反馈解析 + 状态快照 + 发送）
- `powermeter/`：功率计

**边界**：把设备封装成能力，但**不负责项目线程如何编排**。

### 2.4 `framework/topic` —— 数据契约层

**干什么**：定义跨模块、跨线程的数据结构（消息形状、单位、发布/消费关系），不含业务逻辑。

**提供**：
- `imu_to`：姿态/角速度/温度（zbus）
- `remote_to`：遥控通道/开关（zbus）
- `to_can_tx`：CAN 发送帧（k_msgq）

### 2.5 `framework/cmd` —— 横切辅助层

**干什么**：提供与具体业务无关、但需要运行时交互的工具，业务用一行宏注册、编译期链接段收集。

**提供**：
- `shell`：调试变量 shell（`REGISTER_SHELL_VAR` + `l/g/s/?` 命令）
- `buzzer`：PWM 蜂鸣器（`EXEC_BUZZER_*` 宏）
- `flash`：W25Q128 SPI NOR Flash + 分区表
- `linker`：`dust_init.ld` 链接段（注册表机制的另一半）
- `build`：`dust build` 构建脚本

### 2.6 `framework/init` —— 启动层

**干什么**：把 `main()`、注册表、初始化阶段、线程阶段、公共中断分发串起来，回答
"已编译进来的组件按什么顺序初始化和启动"。

**提供**：
- `main.c` → `System_Startup()`
- `REGISTER_INIT` / `REGISTER_THREAD`（`.user_init` / `.user_thread` 链接段）
- `InitStage` / `ThreadStage` / `InitLevel`（阶段 + 失败等级）
- `CAN_RX_HANDLER`（CAN 接收分发）

### 2.7 `platform/cmsis` —— 平台兼容层

**干什么**：隔离特定架构/工具链/SDK 的兼容问题。

**提供**：`HAS_CMSIS_CORE` Kconfig 符号、`cmsis_core.h`（遮蔽 Zephyr 空桩 + `SHPR→SHP` 兼容），
让 ARM Cortex-M 构建拿到真正的 CMSIS core 头。业务不直接触碰。

### 2.8 `boards` / `dts` —— 板卡与设备树层

**干什么**：给 Zephyr 提供官方没有的用户板卡和 devicetree binding。

**提供**：
- `boards/st/stm32f407igh6/`：自定义 STM32F407IGH6 板卡（dts、pinctrl、defconfig、board.cmake）
- `dts/bindings/mtd/winbond,w25q128.yaml`：W25Q128 SPI NOR Flash binding
- `dts/bindings/fsmc/st,fsmc-lcd.yaml`：STM32 FSMC LCD binding
- `dts/bindings/usb/hpmicro,hpm-dustusb.yaml`：HPM 自研 USB 控制器 binding

**子树驱动层 `drivers/`**：板卡专属的底层驱动代码放 `zephyr_user/drivers/`（Zephyr 模块式：
`Kconfig`/`CMakeLists.txt`/`zephyr/module.yml`），与 Zephyr 树 `zephyr/drivers/` 同构。当前含
`drivers/fsmc/`（FSMC LCD 驱动）、`drivers/usb/hal/hpm/`（USB 板卡 HAL）。与 `framework/drivers`
（通用 C++ 封装）的分工见 §2.1。

### 2.9 `project` —— 维护者验证工作区

**干什么**：主干 `project/` 是**维护者工作区**——**只专注验证 framework 六模块是否能够使用**
（六模块的编译/装配/运行），**不专注任何业务线程**。业务线程全部在用户工作区编写。
- CMakeLists 把 `framework` 各层 + `platform/cmsis`（+ HPM 时加 sdk_glue）挂进 Zephyr 构建
- Kconfig 门禁（`PRJ_MAIN` / `TRD_XXX` select 架构层 `DUST_*` 符号）决定编译哪些模块
- `thread/*.cpp`：主干只保留 **gpio 心跳 + test 测试**两个线程，作为**验证六模块的工具**，
  不是项目线程；真正的业务线程（底盘/云台/遥控/IMU/PC 等）在 `projects/<user>/project/thread/` 编写
- 设备树 overlay 每次只添加验证六模块当前需要的外设，用完即删

---

## 3. 配置规范（约定）

### 3.1 子模块默认拉取都为 n

架构层（framework 各层）的 `DUST_*` 符号默认值一律为 `n`——产品不显式启用就不进镜像，
不做运行时 `if(false)` 裁剪。

### 3.2 业务层在 Kconfig 里拉取子模块，不写在 prj.conf

业务层（project）要启用某个架构能力，在 `project/Kconfig` 里用业务开关 `select` 对应的
`DUST_*` 符号，而不是在 `prj.conf` 里写 `CONFIG_DUST_XXX=y`：

```kconfig
# project/Kconfig —— 正确：业务开关 select 架构能力
config TRD_REMOTE
    bool "Remote thread"
    select DUST_MOD_DEV_REMOTE
```

### 3.3 project 根 CMakeLists / prj.conf 只放通用配置

`project/CMakeLists.txt` 放通用构建装配（framework 挂载、BOARD_ROOT/DTS_ROOT、总门禁）；
`project/prj.conf` 放通用运行配置（C++ 环境、调度、FPU、日志）。不放具体功能开关。

### 3.4 板卡单一配置放板卡的 board.cmake 和 conf

一块板卡专属的东西（外设使能、引脚、时钟、烧录器）放对应 `boards/<vendor>/<board_cfg>/` 的
`board.cmake` 和 `<board>.conf`。架构能力默认是 n，板卡 conf **不需要**再写
`CONFIG_DUST_XXX=n`（默认就是 n）。

---

## 4. 层与层怎么配合

依赖方向（只允许向下依赖能力，向上组合语义）：

```text
Zephyr ← platform/cmsis ← boards/dts
       ← framework/drivers    （硬件访问）
       ← framework/algorithm  （纯计算）
       ← framework/modules    （设备能力）
       ← framework/topic      （数据契约）
       ← framework/cmd        （横切辅助）
       ← framework/init       （启动遍历）
       ← project              （业务装配）
```

一句话：**越靠下越接近可复用能力，越靠上越接近当前产品语义。**

一个典型数据流穿过所有层：

```text
遥控器 UART → drivers(UartDma) → modules(Remote) → topic(remote_to)
    → project(chassis 线程) → algorithm(PID/PowerCtrl) → topic(to_can_tx)
    → modules(电机) → drivers(CAN) → 电机
```

每一层只处理自己的问题。

---

## 5. 一个功能如何走完全程（以 GPIO 线程为例）

```text
1. project/Kconfig:  TRD_GPIO=y  →  select DUST_DEV_GPIO_OUTPUT + DUST_CTL_TIMER
2. project/prj.conf: CONFIG_PRJ_MAIN=y（总门禁）
3. framework/drivers 的 CMake 按 DUST_DEV_GPIO_OUTPUT 编译 output.cpp
4. project/thread/gpio/trd_gpio.cpp: REGISTER_INIT/REGISTER_THREAD
5. 链接器 dust_init.ld KEEP .user_init/.user_thread，生成 __start/__end
6. framework/init 的 RunStage 按阶段遍历并调用 thread_init/thread_start
7. 线程循环运行
```

每一步都可单独检查：配置 → 编译 → 链接 → 注册 → 启动。这就是子树架构的诊断价值。

---

## 6. 维护者层与使用者层

### 6.1 两层是谁

- **维护者层** = `zephyr_user` 本身（父级能力树），由维护者/电控组长维护。**只专注 framework 六模块
  是否能够使用**（验证编译/装配/运行），**不专注任何业务线程**——业务线程全在用户区。
- **使用者层** = 与 `zephyr_user` 同级的用户应用工作区（`projects/<user>`，如 `qingyu`），
  由电控成员维护，编写自己的业务线程。

### 6.2 各负责什么

| | 维护者层（zephyr_user） | 使用者层（用户工作区） |
| --- | --- | --- |
| 负责 | framework 六模块可用性（验证编译/装配/运行）、platform、公共板卡、构建工具、架构文档 | 自己的业务线程（`project/thread/`）、产品策略 |
| 不负责 | 具体产品业务线程、某台机器人的编排 | 改公共驱动/模块/启动器；公共能力由维护者集中演进 |

### 6.3 依赖方向（单向）

```text
用户工作区
    -> CMake 引入 zephyr_user/framework + platform/cmsis
        -> 消费父级能力
```

使用者**不复制** framework，**不改**公共模块。公共能力由维护者集中演进，用户只在自己的
`boards/` 和 `thread/` 里组合。

### 6.4 用户工作区（新模板，2026-08-13）

用户工作区放在 `E:\Zephyr\projects\<user>\`，与 `zephyr_user` 同级。**新模板 = 用户区根平铺
framework 六模块（用户层自己的层）+ `project/`**，与架构层六模块（`zephyr_user/framework/`）
**共存不冲突**（不同路径）。范本：**`qingyu` 是新模板**，`template` 已对齐它；新建/迁移用户区
先看 qingyu/template 的 CMakeLists，不翻 dust/ai_imu/detected（过时保留文件）。

```text
E:\Zephyr\projects\qingyu\          ← 用户工作区（新模板范本）
├── algorithm\ cmd\ drivers\        ← 用户层六模块（跟架构对齐，空目录跳过装配）
│   init\ modules\ topic\
└── project\                        ← 业务工程
    ├── CMakeLists.txt              ← 装配：FW_ROOT(架构层六模块) + 用户层六模块 + project
    ├── Kconfig                     ← 业务开关（TRD_XXX select DUST_*）
    ├── prj.conf                    ← 通用配置（C++/调度/总门禁）
    ├── boards\                     ← 板卡单一配置（board.cmake / conf / overlay）
    └── thread\                     ← 业务线程（can/chassis/gimbal/imu/pc/remote/gpio/test）
        ├── CMakeLists.txt          ← 按 CONFIG_TRD_XXX 装配线程
        └── trd_*.cpp               ← 业务线程实现（REGISTER_INIT/REGISTER_THREAD）
```

> 用户区 `project/CMakeLists.txt` 用 `ZEPHYR_EXTRA_MODULES` 追加用户层六模块（`if(EXISTS
> .../CMakeLists.txt)` 保护空目录，跳过），架构层六模块照常走 `FW_ROOT` 引入。`thread.hpp` 在
> 架构层 `framework/init/`，用户区不复制。

### 6.5 公共修改流程

```text
用户发现问题/需求
    -> 提交给维护者
        -> 维护者在 framework 内实现
            -> 评审 + 发布（submodule 指针更新）
                -> 用户工作区更新父级版本
```

这就是"父级提供能力，子级组合语义"——公共代码只维护一份，产品差异留在用户工作区。

### 6.6 上下级关系：使用者不允许修改子模块，问题在自己工作区单独维护

上下级关系是**单向的**：`zephyr_user`（父级）在上，使用者工作区（子级）在下，使用者**不允许
直接修改 framework 子模块的内容**。

遇到子模块问题，只有两种方式，**绝不允许修改子模块**：
1. **向维护者提 issue**（正式反馈，走 §6.5 公共修改流程）；
2. **本地修改**——在自己的工作区建独立文件夹覆盖/补丁，不碰子模块文件。

| 情况 | 处理方式 |
| --- | --- |
| 子模块有 bug | 向维护者提 issue；或在自己工作区建覆盖/补丁目录 |
| 子模块未包含的功能 | 在自己工作区单独实现 |
| 自己想填的新内容 | 在自己工作区单独建文件夹维护 |

例如：使用者发现 `framework/drivers` 某驱动有问题，不直接改它，而是在自己的工作区建
`<user_workspace>/drivers_patch/` 或 `vendor/` 之类目录存放自己的版本，构建时优先使用自己的。

单独维护之后，如果该内容有**多个项目的复用价值**，再提交给维护者，评审后提升到 framework——
这就回到了 §6.5 的公共修改流程。
