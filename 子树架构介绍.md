# zephyr_user 子树架构介绍

> 文档版本：2026-08-21 重新整理
> 适用路径：`E:\Zephyr\zephyr_user`
> 说明：本文按当前源码、Kconfig、CMakeLists 和板卡配置更新。

---

## 1. 总览

`zephyr_user` 是一棵面向 Zephyr 的用户自研子树。它把公共能力拆到 `framework/`，把板卡专属底层驱动放到 `drivers/`，把板卡和设备树放到 `boards/` / `dts/`，再用 `project/` 把这些能力装配进具体应用。

```text
zephyr_user/
├── boards/                 用户自研板卡定义
├── dts/                    用户自研 devicetree binding
├── doc/                    架构与搭建文档
├── drivers/                子树板卡底层驱动与 HAL
├── framework/              公共架构层
│   ├── drivers/            通用外设封装
│   ├── algorithm/          纯计算 / 滤波 / 辨识
│   ├── modules/            设备能力层
│   ├── topic/              数据契约层
│   ├── cmd/                横切辅助层
│   ├── init/               启动层
│   └── zephyr/             Zephyr 模块入口
├── platform/               平台兼容层
│   └── cmsis/
└── project/                当前维护者验证工程
```

核心主线是：

```text
Kconfig -> CMake -> DTS / BOARD_ROOT -> 模块编译 -> 链接段注册 -> 启动遍历
```

---

## 2. 各层职责

### 2.1 `boards/` 和 `dts/`

这两层只放板卡和设备树相关内容。

- `boards/st/stm32f407igh6/` 是当前仓库里的自定义板卡。
- `dts/bindings/` 放自定义 binding，例如 `winbond,w25q128.yaml`、`st,st-dustusb.yaml`、`hpmicro,hpm-dustusb.yaml`。

它们解决的是 "Zephyr 树本身没有这块板/这个外设" 的问题，不负责业务逻辑。

### 2.2 `drivers/`

这是子树的板卡底层驱动层，按 Zephyr 模块方式组织。

当前能看到的内容有：

- `drivers/fsmc/` - STM32 FSMC LCD 底层驱动
- `drivers/usb/hal/hpm/` - HPM EHCI USB HAL
- `drivers/usb/hal/stm32/` - STM32 OTG FS USB HAL

对应 Kconfig 里当前有 `DUST_USB_DEVICE_HAL_HPM` 和 `DUST_USB_DEVICE_HAL_STM32`。这些符号属于板卡/SoC 底层，不是业务线程开关。

### 2.3 `framework/drivers`

这一层做的是通用外设封装。

它把 Zephyr 的设备树对象、外设 API 和通信接口，封装成项目自己的 C++ 对象，典型包括：

- `Input` / `Output`
- `Pwm`
- `Spi`
- `Can`
- `UartDma`
- `Rs485`
- `usb::Usb`

这里还包含 `DUST_USB_DEVICE_STACK`，会把 USB 设备栈、CDC ACM 和板卡 HAL 串起来。它的边界是设备访问、DMA、缓冲和回调，不管协议解析和业务编排。

### 2.4 `framework/algorithm`

这一层只做纯计算。

当前源码包括：

- `buffer/` - `BipBuffer`、`RingBuf`
- `controller/` - `Pid`、`PowerCtrl<N>`、协作式 `Timer`
- `filter/` - LPF、HPF、Kalman、EKF、四元数姿态滤波
- `identify/` - RLS、电机本体辨识、稳定判据
- `math/eigen/` - 内置 Eigen

需要特别记住的一点是：`DUST_CTL_EXECTIMER` 现在还只是预留开关，当前源码里没有单独的 `ExecTimer` 类实现。

### 2.5 `framework/modules`

这一层把 "一个设备需要的驱动访问 + 协议解析 + 状态管理 + 必要算法" 组合成设备能力对象。

当前可见模块包括：

- `imu/` - IMU 采样、加热、姿态
- `remotes/` - 遥控器接收机和协议自动探测
- `motors/` - DJI C6xx、DM 电机
- `powermeter/` - 功率计

它们通常会发布到 `framework/topic`，再被业务线程消费。

### 2.6 `framework/topic`

这一层只定义跨模块、跨线程的数据契约。

当前能看到的 topic 有：

- `imu_to`
- `remote_to`
- `to_can_tx`

这一层不写业务逻辑，只定义消息形状和流向。

### 2.7 `framework/cmd`

这一层提供横切辅助能力。

当前实际内容有：

- `shell/` - 调试 shell、变量、日志
- `buzzer/` - PWM 蜂鸣器
- `flash/` - W25Q128 分区和操作
- `fatal/` - 致命错误处理
- `build/` - `dust` 构建脚本
- `linker/dust_init.ld` - `.user_init`、`.user_thread`、CAN/topic 等链接段

这里的特点是：业务通过宏注册，链接器收集，启动层统一遍历。

### 2.8 `framework/init`

这一层把 `main()`、注册表、初始化阶段和线程阶段串起来。

当前启动链很清楚：

```text
main.c -> System_Startup()
```

`Init_entry.hpp` 定义了：

- `InitStage`
- `ThreadStage`
- `InitLevel`
- `InitFailPolicy`
- `REGISTER_INIT()`
- `REGISTER_THREAD()`

`Init_entry.cpp` 按阶段和优先级遍历 `.user_init` / `.user_thread`，并用 `InitFailPolicy` 决定失败后停机还是继续。

### 2.9 `platform/cmsis`

这一层只处理平台兼容。

当前实现是 `platform/cmsis/cmsis_core.h` 和 `platform/cmsis/Kconfig`，主要解决 ARM Cortex-M 下 Zephyr 空桩和 CMSIS core 头的差异。

### 2.10 `project`

这一层是当前仓库的维护者验证工程。

`project/CMakeLists.txt` 把 `framework/`、`platform/cmsis/` 和 `drivers/` 挂进 Zephyr 构建，`project/Kconfig` 决定当前验证哪些能力，`project/thread/` 只保留需要的线程样例。

当前线程样例是：

- `project/thread/gpio/trd_gpio.cpp`
- `project/thread/test/trd_test.cpp`

`trd_test.cpp` 当前做的是 USB CDC ACM 回环测试，不是通用业务线程模板。

---

## 3. 配置规则

### 3.1 架构层默认都是 `n`

`framework` 各层的 `DUST_*` 符号默认值都应保持为 `n`。产品不显式启用，就不进镜像。

### 3.2 业务层用 `select` 拉起架构能力

业务开关在 `project/Kconfig` 里写，靠 `select DUST_*` 拉起架构能力，而不是在 `prj.conf` 里硬写 `CONFIG_DUST_XXX=y`。

当前例子：

```kconfig
config TRD_GPIO
    bool "GPIO thread task"
    select DUST_DEV_GPIO_OUTPUT
    select DUST_CTL_TIMER

config TRD_TEST
    bool "User test add config"
    default y
    select DUST_COM_USB
```

### 3.3 `project/prj.conf` 只放通用运行配置

`project/prj.conf` 里当前放的是 C++、FPU、线程、堆栈和总门禁这类通用配置，不放具体功能开关。

### 3.4 板卡特殊配置放在板卡目录

`project/boards/<vendor>/<board_cfg>/` 里的 `board.cmake`、`*.conf`、`*.overlay` 放板卡特殊配置。

当前现状里，板卡 `conf` 也会承载底层 HAL 开关，例如：

- `CONFIG_DUST_USB_DEVICE_HAL_HPM=y`
- `CONFIG_DUST_USB_DEVICE_HAL_STM32=y`

这类符号是板卡/SoC 后端，不是业务线程开关。

---

## 4. 层与层怎么配合

依赖方向是单向的：

```text
Zephyr
  -> platform/cmsis
  -> boards / dts
  -> framework/drivers
  -> framework/algorithm
  -> framework/modules
  -> framework/topic
  -> framework/cmd
  -> framework/init
  -> project
```

一个典型数据流可以写成：

```text
USB/UART/CAN  -> framework/drivers -> framework/modules
             -> framework/topic    -> project/thread
             -> framework/algorithm -> framework/modules / project/thread
```

一层只处理自己的问题。越靠下越偏通用能力，越靠上越偏当前产品语义。

---

## 5. 当前装配链

`project/CMakeLists.txt` 的实际行为是：

1. 先处理板卡的 `overlay`、`conf` 和 `board.cmake`
2. 把 `framework/`、`platform/cmsis/`、`drivers/` 加进 `ZEPHYR_EXTRA_MODULES`
3. `find_package(Zephyr REQUIRED ...)`
4. `add_subdirectory()` 装配 `framework` 各层
5. `add_subdirectory(project/thread)`

这意味着：

- 平台层和板卡层先于业务层出现
- 业务线程只消费已经装配好的能力
- `thread.hpp`、`Init_entry.hpp` 这类入口头文件来自 `framework/init`

---

## 6. 功能如何走通

### 6.1 GPIO 线程

```text
TRD_GPIO
  -> select DUST_DEV_GPIO_OUTPUT + DUST_CTL_TIMER
  -> 编译 framework/drivers 的 output.cpp
  -> project/thread/gpio/trd_gpio.cpp 里 REGISTER_INIT / REGISTER_THREAD
  -> dust_init.ld 收集 .user_init / .user_thread
  -> System_Startup() 按阶段初始化和启动
```

`trd_gpio.cpp` 里当前是 PA18 心跳输出，初始化失败策略是 `HaltOnFail`。

### 6.2 USB 测试线程

```text
TRD_TEST
  -> select DUST_COM_USB
  -> framework/drivers 里把 USB 栈和板卡 HAL 装配进来
  -> project/thread/test/trd_test.cpp 做 USB CDC ACM 回环
```

当前 `project/boards/hpm/hpm5361icb/` 和 `project/boards/st/puzhong/` 都已经把 USB HAL 配好了。

---

## 7. 维护者层与使用者层

### 7.1 维护者层

维护者层就是这棵 `zephyr_user` 子树本身。它负责公共能力的维护、验证和发布，不负责某个具体产品的完整业务线程。

### 7.2 使用者层

如果另建用户工作区，建议放在同级 workspace 里单独维护。用户层只组合自己的业务线程和板卡配置，不直接改公共子模块。

### 7.3 公共修改流程

```text
发现问题
  -> 提 issue
  -> 维护者在 framework 内修复
  -> 评审后更新 submodule 指针
  -> 用户工作区同步父级版本
```

---

## 8. 当前板卡事实

这棵子树里当前实际可见的两套板卡配置是：

- `project/boards/hpm/hpm5361icb/`
- `project/boards/st/puzhong/`

对应现状：

- HPM 板卡的 overlay 暴露 `shell-uart` 和 `dustusb_usb0`
- STM32 板卡的 overlay 也暴露 `shell-uart` 和 `dustusb_usb0`
- HPM 板卡 conf 启用 `CONFIG_DUST_USB_DEVICE_HAL_HPM=y`
- STM32 板卡 conf 启用 `CONFIG_DUST_USB_DEVICE_HAL_STM32=y`

---

## 9. 推荐阅读顺序

1. `README.md`
2. `子树架构介绍.md`
3. `framework/init` -> `framework/cmd` -> `framework/topic` -> `framework/algorithm` -> `framework/drivers` -> `framework/modules`
4. `project/CMakeLists.txt`、`project/Kconfig`、`project/thread/*`
